import { Injectable } from '@angular/core';

/**
 * @deprecated SupabaseService is no longer used in the frontend.
 * All data operations go through the API layer (apps/api).
 * This stub is kept to avoid breaking any potential remaining references.
 */
@Injectable({ providedIn: 'root' })
export class SupabaseService {}

/**
 * 管理端**整頁列表**的每頁筆數。
 *
 * 這個值原本在十三個頁面各寫一份，而且已經漂成**四種**：8（九處）、15（聯絡簿）、
 * 20（報名進出、金流、課務異動）。四種都沒有註解、commit 訊息也沒說為什麼 ——
 * **已經漂過的值會繼續漂**，所以收成單一來源。
 *
 * 為什麼是 20 而不是 8：桌機一頁只顯示 8 列，121 筆課程要翻 16 頁、27 名學生要翻
 * 4 頁，而 1100px 高的螢幕塞得下 15–20 列。8 是手機的合理值，不是桌機的。
 *
 * **對話框裡的挑選器刻意不用這個值。** `student-picker-dialog` 與
 * `class-picker-dialog` 的清單區只有 340–380px（約 7–8 列），那裡的 8 是對的 ——
 * 它們受限於強制的高度而不是視窗高度，是不同的情境。
 */
export const LIST_PAGE_SIZE = 20;

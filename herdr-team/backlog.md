# 常備工作佇列(零 idle 制)

> **席位工單已遷到 GitHub issues,本檔不再是派工載體。**
> 查自己的佇列:`gh issue list --label seat:<席位> --state open`
> 遷移日 2026-09-06;理由是 PR 合併時 `Closes #N` 會自動關掉工單,而本檔靠人記得刪除。
> 本檔開頭原本就寫著「做完的由計畫席直接刪除」並註明那條紀律已失效五次 ——
> 2026-09-06 早上失效第六次:ops-warden 讀到過時的 teacher-pages §0,
> 把一個剛上任的席位派去做 #358(十七小時前就已合併)。
> **一份需要人記得刪除的清單,回答「還剩什麼」時永遠偏多 —— 它只會累積,不會自己收斂。
> 而偏多的清單比空的清單危險,因為它看起來像有在運作。**
>
> 本檔保留的是**不是工單的東西**:裁決紀錄、明記的技術債、等觸發條件的決策、交接與夜間班報告。
> 這些不靠「做完就刪」維持正確,所以留在這裡不會腐化成假派工。
> 讀本檔前先 `git pull`;PR 狀態一律 `gh pr view --json state` 現查,不信文字。


## 窗口裁決落地(2026-09-05 11:15,使用者六項全裁)

1. #295 **批准動工**+附帶:activeRole 進 API context=要 / 遮蔽=排名+內部備註 / is_primary=不影響
2. #295 補「孩子帳號」擴充節:使用者提議納入 —— v1 **不做帳號**,但模型天然相容
   (學生帳號=scope 只含自己的 student_id),寫成擴充節供未來
3. 及格線欄位:**准開** —— migration+API+UI 一條線動工,migration PR 照保留類**由使用者合**
4. 科目平均:**改「幾分之幾」** —— 不做百分比化;平均列顯示 總得分/總滿分(如 130/150),
   API 回 sum 與 totalSum,消滅無意義平均的同時保留原始分數感

## 學生帳號線(使用者 2026-09-05 確認要做,排家長端 03 片之後)

- 架構已備:#316 擴充節(學生=scope 只含自己,同一套 childDb/studentScope)
- 到設計階段的產品決定:登入方式(LINE?子登入?)/可見範圍(繳費給不給?)/
  user_roles 加 student 第四角色(select-role 天然支援)
- 流程照舊:探索→設計文件→使用者批准→動工;頁面重用家長端(同頁換 scope 的薄層)

## Tester(desktop-44) —— 寫入授權已下(2026-09-05 18:5x,使用者批准)

**授權範圍**:`demo.clessia.cc`,沿用使用者 admin session。
- **可以**:建立資料(所有「新增 XXX」表單真填真送)、**點名流程走完整條**
- **規矩**:①假資料一律 `UX測試` 開頭 ②只准建立,不准刪改「你來之前就存在」的任何一筆
  (自己剛建的可以改) ③**刪除按鈕先不要按**,即使是自己建的
- **③ 的理由**:M8 的破壞範圍還沒查清(billing-api 正在做),
  **未知範圍的動作即使在 demo 上也不該亂按**;那張表出來再放行評刪除防呆(清單第 8 項)
- **這次要評的是「送出之後」**:成功回饋(知不知道成功、回到哪、新建的看得到嗎)、
  錯誤與驗證(錯誤訊息說得出「哪一欄、為什麼」還是只說「請檢查輸入」)、
  點名(存檔回饋、改了又改、離開沒存會不會提醒)
- **順便複驗 P1-7**:#359 已修但**未合進 main,demo 上仍是舊版** —— 正好可以確認當初的判讀,
  並補上他上一輪評不到的「按下去之後發生什麼」

## Tester 啟動裁決(2026-09-05 17:20)

三件裁決結論(計畫席 clessia-c8 裁,已發啟動令 msg 141901df):
①**session**:沿用使用者現有 admin session 唯讀(零設定;Chrome 殘留 session 直進後台
是瀏覽器行為非漏洞) ②**寫入**:零寫入維持,碰到「必須送出才評得到」的地方列成
「待授權才能評」清單,累積後一次向使用者換授權 ③**範圍**:僅管理端(老師/家長 fixture
過早,家長端 API #351 未合)

- 產出:問題清單(哪一頁/怎麼重現/看到什麼/為什麼是問題),嚴重度自標,計畫席排序
- 已交線索:儀表板「載入中」粗體佔數字位 4-5 秒像狀態值(單點已排設計席改骨架條)——
  要 tester 找的是**同一模式還在哪些頁**,不是複驗這一點
- 產品觀察(待使用者決):系統沒有「把家長角色加到現有帳號」的 UI 路徑

## Tester 跨頁模式 M2-M8 分流(2026-09-05 18:5x)

- **M1 告警只報數字** → design-web,#361 設計已過、#365 已交前三處
- **M2「狀態」欄整欄同值**(課堂管理 19 列全「正常」/考試全「進行中」/餐費全「未處理」;
  加重:課堂管理「出勤狀態」與「狀態」兩欄並排名字太像)→ **未派,要先查證**:
  是資料就長這樣,還是欄位接錯?**一個永遠同值的欄位跟一個壞掉的欄位長得一樣**
- **M3 日期範圍框截斷結束日**(課堂管理/聯絡簿/營收報表三頁)→ design-web[已派]。
  **最陰險的是 `2026/09/3` 看起來像合法日期**(9月3日),不像被切掉的 09/30
- **M4 同一概念多種說法** → design-web[已派,先提清單再動]。
  **已裁:分校 vs 校區 → 一律「分校」**(AGENTS.md 與 user_roles 的既有詞彙)。
  **提醒別把有理由的差異當不一致清掉**:日期格式該訂「什麼情境用哪種」不是「統一成一種」
  (`09/01 (週二)` 在課堂管理有意義);容量三種講法裡只有 `👥 20` 真的有問題(P1-5)
- **M5 動畫殘影與延遲** → design-web-2(載入態骨架化)
- **M6 同一個 `›` 兩種行為**(課程列=就地展開、班級列=跳頁)→ 與「課程管理三層表格是否重畫」
  綁在一起,**已報使用者窗口**(方向級)
- **M7 布林值控制項不統一** → design-web[已派]。**要訂規則不是逐處修**:
  什麼用 checkbox/toggle/filter chip。後兩個實例是同一病的變體 ——**看不出目前是開還是關**
- **M8 破壞性動作裸露** → **billing-api 查資料面[已派]**,產出「每個刪除路徑實際連動什麼、
  可不可逆」的表(要看 migration `ON DELETE` **與**應用層兩邊,只看一層會得到錯答案)。
  **確認文案的強度應該對應實際破壞範圍,而非對應「它是個刪除按鈕」** ——
  目前那些按鈕長得一樣但毀掉的東西不一樣。UI 半等表出來再派 admin-pages。
  **順帶要答:哪些刪除根本不該存在**(專案已有先例:班級歸檔是設 end_date 不是刪除;
  會毀掉歷史紀錄的大概該是封存不是刪除)

## 合併順序（有依賴，不能亂合）

**`#368` → `#363`（互不依賴）→ `#376`（base 收斂成 main 後轉 ready）→ `#378`**

- `#376`（`GET /api/sessions` 補 `endedOnly`）base 指向 `feat/attendance-ended-only`，**draft**
- `#378`（儀表板→課堂管理 handoff，P1-6）base 指向 `#368`，**draft**
- **`#378` 絕對不能先於 `#376` 進 main** —— 那會恰好落進被否決的選項 A：
  卡片說 15、點下去含今天還沒到點名時間的課，數字對不上。
  **不是設計選擇，是合併順序造成的**，但結果一樣
- 疊 PR 鐵律：base 指到別人分支就一律 **draft**，等 base 收斂到 main 才轉 ready ——
  讓「還不能合」變成 GitHub 擋得住的狀態，不靠人搶時間差（#89/#129 事故）。
  下層合併後上層 base 立刻人工轉 main、下層分支即刪

## Tester P2 分流（admin-pages 逐條 path:line 查證，計畫席 2026-09-05 裁）

**堆 1 —— 現在修（10 條，admin-pages，分兩支 PR）**
- PR A（純機制，零視覺判斷，快速合）:#35 sidebar 補 `[multi]="true"`、
  #29 補 import `GRADE_LEVEL_LABELS`、#33 兩個 container 的 max-width 常數、
  #15 補 H1、#22 文案。**五條都是「已存在的東西沒被接上」**，同 P1-1 族
- PR B（要看畫面）:#8 composer 寬度、#9 加 disabled pill（**只調視覺，
  「發送對象寫死」是有註解背書的刻意決策，不動**）、#16 layout 分組、
  #18 補三個 datepicker props、#14（**先實測確認是不是硬編碼預設值再改**）、
  #11 的 tooltip 半

**堆 2 —— 已分派**
- #12 sticky 表頭 / #13 點兩次才跳頁 / #27 排序 → **tester**（讀碼看不出，要實際操作）
- #17 請假只能刪不能改（前後端都沒做，需新 endpoint）/ #19 考試已登錄沒分母
  → **billing-api**。#19 的分母**業務規則未定**（依班級在籍人數？退班算不算？）→ 使用者窗口
- #21 繳費視覺層級 / #24 頭像兩種 / #30 分校 tab 水平捲動 / #31 課程列表無排序
  → **design-web，排 M4 之後**。#30/#31 與「課程管理三層表格是否重畫」綁定（使用者窗口）

**堆 3 —— 不修（12 條，每條有理由）**
- #10/#20/#36/#37/#38 → 全押 M4 詞彙統一，**理由明寫「避免重演 M2 重工」**
- #23 hero 黑框 = **刻意設計**（scss 註解:先前透明度底色對比只有 1.3:1，改用描邊）——
  **「刻意不做要寫理由」這條通則最好的證明:沒有那句註解，這條會被修掉然後對比問題原地重現**
- #25 同名家長=真實世界資料 / #26 seed 缺手機不是顯示邏輯 / #32 重複資訊有助掃描
- #27 **查無對應排序邏輯 → 回去問提報者，不升級成「提報者錯了」**（正確處理）

## 明記的技術債

- **「這張帳單逾期了嗎」有兩個獨立實作 —— 觸發:下次有人動這兩處任一處的逾期判定**。
  `invoices.ts:140`(#417 修過時區)與 `reports.ts:156`(第三批要修)是**同一份判斷邏輯的
  第二個獨立實作,兩處從來沒共用過**。今天一致是因為兩邊寫得一樣,
  不是因為結構上不可能不一致。**漂移的話:繳費頁說某筆逾期、營收報表說沒有** ——
  使用者看到兩個畫面對同一筆帳單給相反結論(同 `passScore` 的形狀:
  行政端不及格、家長端及格)。出處:billing-api 查第三批語意時發現。(有觸發條件,不靠人記得)

- **`campusNames` 沒像 `classNames` 一樣濾掉退班的報名 —— 觸發:下次有人動這兩支任一支**。
  admin-pages 查 #28 時撞到的附帶發現。又是「兩個地方對同一個概念各做一次」，
  而它們在多數資料下輸出相同（出處:P2 分流，2026-09-05）。
- **「已結束」有兩份實作 —— 觸發:下一次有人要動這兩支任何一支的日期判定**。
  `apps/api/src/lib/session-end-time.ts` 的 `hasSessionEndedByNow()` 與
  `apps/web/.../shared/utils/session-time.util.ts` 的 `hasSessionEnded()`。
  目前靠「逐案照抄 12 個測試案例」偵測漂移 = **弱形式**
  (計畫席同日對 #363 的標準是「讓漂移不可能」)。
  **不統一的理由是成本結構不同,不是標準放寬**:#363 兩個呼叫端都在 apps/api,
  抽進 `lib/` 是純搬移零行為變更;這一支跨 apps/web 與 apps/api,
  要共用得進 `packages/`(現有 `shared-types`/`validators` 都不是自然歸屬),
  **且前端用瀏覽器本地時區、後端用顯式 +08:00 —— 統一意味著改前端行為,是另一個切片**。
  **償還是加分不是維持原狀**:顯式 Taipei 比瀏覽器本地更正確
  (使用者裝置時區設錯時只有後端版是對的)。

## 等觸發條件的決策（新增三筆，2026-09-05 晚）

- **學生列表的分校顯示區分「在籍」與「已退班」—— 觸發:下次有人為別的理由動學生列表的
  分校顯示時**。#438 裁定 C(維持四處都不濾,把「分校歸屬=曾經在這個分校報過名」寫進
  `enrollment-rules`)之後留下的正解。現在做等於為一個沒有人抱怨過的問題改三層
  (API 形狀 + UI + 篩選)。**排除 B(四處全濾)的理由要一起記住**:退班學生對分校主任
  不是歷史資料,是現在進行式的工作對象(退費、回流招生、家長投訴查紀錄)。
  出處:api-2 查 #438,四處推導全查過。
- **`invoices.ts:271-272` 的預設到期日手算收斂 —— 觸發:下次有人為別的理由動
  invoices.ts 的到期日邏輯時**。同一條規則在 `billing-runs.ts:258` 走
  `addDaysToDateString()`,這裡是 `new Date + setUTCDate + toISOString`。
  **行為等價**(純日曆日加減、不取「現在」,沒有時區 bug),所以是純風格收斂,
  3 行換 1 行。出處:api-2 做 #437 時順手查到,因 #437 要求純搬移所以沒動。

- **其餘列表頁要不要跟進橘帶 band-anchor —— 觸發:下一批列表頁改動時**。
  統計摘要列現有三種形態,橘帶是**有文件、還在分批 rollout 的新方向**,
  元件 docstring 寫明「**每頁該秀哪個數字是產品判斷,不是同一份資料換殼**」。
  **不推給使用者的理由**:它不阻塞任何人,而晚點問成本更低(下一批改動時
  「要不要順手跟進」是當下就要答的問題)。**計畫席的職責之一是決定什麼該進
  使用者的佇列** —— 推一個不阻塞的問題上去只會稀釋真正需要他的那幾件。
- **`schools.page.html` 接 `app-page-actions` —— 觸發:下次有人為別的理由動它的結構時**。
  這頁完全沒有 header/title 結構(只有 breadcrumb + 搜尋列),要接得先補 header 區塊,
  範圍比換一顆按鈕大一截,而收益是純一致性。
- **`pTooltip` 在觸控裝置看不到 —— 觸發:下次做觸控/平板適配時**。
  #390 的課程刪除用 `[disabled] + [pTooltip]` 說明不能刪的原因,
  但 tooltip 需要 hover。**同排的「新增班」按鈕本來就是這個模式**,所以 #390 是
  跟既有慣例對齊不是新增問題 —— 要修該整排一起修。
  同族:`.courses__badge` 的 `cursor: pointer` 也只在 hover 出現。
  **把資訊掛在 hover 上,在觸控裝置等於沒有。**

## 等觸發條件的決策(到條件成立時要回頭檢查,不是等它礙事才想起)

- **hover-only 可點性稽核 —— 觸發:tester 手機直向那輪跑完**。
  現況信號:admin 底下 28 個 SCSS 含 `cursor:pointer`,但**那不是違規數**
  (沒篩掉「本來就是 button、hover 只是錦上添花」的正常情況)。
  真正的問題是「幾個可互動元素的可點性**只**靠 hover 表達」——
  靜態掃描難做又難信(要判斷視覺語意夠不夠像按鈕,那是人眼的工作),
  **而實機一測就有答案**。tester 若回報一批「看起來不能點其實能點」,
  從那份實證清單出發;一個都沒撞到,優先序自然下去。
  手機直向那輪卡在 Chrome 全螢幕(需使用者手動退出)。

## 夜間班報告(2026-09-05 深夜 → 09-06 凌晨,計畫席 clessia-c8)

**使用者睡前開了三件**:權限規則(計畫席可直接合併,deny `--admin`)、Tester 寫入授權、
`sudo pmset -c sleep 0`。這一夜的產出幾乎都建立在第一件上。

### 進 main 的(約 17 支)
時區根因 #402、UUID 驗證 #414、家長選擇器 #399、教務日誌 v1b 老師半 #396、
disabled 對比 #406、`[object Object]` #411、人員數字磚 #367、科目確認對話框 #389、
家長表單按鈕 #412、排課衝突警告 #401、點名雙重× #407、`endedOnly` #363/#415、
kb 缺口 #356、三份 charter 蒸餾 #397/#404/#409

### 三條真 bug,兩條會產生錯資料
1. **`leaves.ts:532` 用 UTC 算「昨天」** —— 台北 00:00-08:00 截短請假會多砍一天,
   **一天有 16 小時是對的、8 小時是錯的**,沒人在凌晨合併過所以一直沒被抓到。
   而 `lib/taipei-date.ts` 的檔頭警告一字不差就是那一行。
   **掃出還有 16 處同款寫法**(含 `invoices.ts:140` 的 overdue 過濾 —— 那是**過濾條件**
   算錯一天,凌晨看繳費頁會有帳單被錯誤列為逾期)。已排三批,見 billing-api 佇列
2. **`z.uuid()` 比 Postgres 嚴格** —— seed 的佔位 ID(`61000000-0000-0000-0000-...`)
   Postgres 收、zod 拒。**餐費管理在 demo 上 100% 不可用**,而 30 支 route 都有這個洞。
   **只有 seed 產生的實體會失敗,app 新建的不會** —— 所以測試在結構上看不見它
   (既有測試用 `4000-8000` 的 RFC 合法佔位)。`DbUuidSchema` 本來就在 `lib/validation.ts`,
   只是沒被接上
3. **點名漏掉請假學生** —— 有請假單但紀錄未套用的人「不被鎖卻被算成豁免」,
   footer 的第二分支多了 `markedCount() === 0` 所以混合情況落到「全部標記完成」。
   前端 #413、後端 #416 都已出

### 使用者早上可能想先看的
- **#416**(點名補寫 `on_leave`)與 **#413**(footer 不再誤報完成)—— 那條 P0 的兩半
- **時區三批**還沒動,`invoices.ts:140` 是其中唯一碰錢的過濾條件
- **Tester 的手機清單**在產 —— 點名在手機上長什麼樣仍然完全未知,
  而桌機端入口要走五步、藏在 ⋮ 選單第二項

### 計畫席自己犯的(留給下一任)
- **`--delete-branch` 一晚關掉四支疊 PR,兩次** —— 第二次是在寫下防範指示之後用
  `for` 迴圈跳過了它。**已改流程:不再用那個旗標**。人為檢查在批次操作時會被跳過
- **兩次把過期的 CI 狀態當成當前的**(把 SKIPPED 當紅燈、把舊 run 的 FAILURE 當真紅)
- **兩次「等 X 成立的決策在 X 成立時沒回頭檢查」**(M8 UI 擋著等一張早就交了的表、
  backlog 四處過期的「等 #351」)

## 使用者窗口積壓

- **#351 待使用者親合**(家長端授權 scope 第一實例=保留類「授權/權限邏輯」;CI 綠、
  計畫席本人驗收留言已補)
- 及格線欄位 migration / 640px 視覺 / iOS 抽屜確認(#282,已向 teacher-pages 索步驟
  msg 3a94d501)
- 誰先 db:reset 誰驗 seed 雙身分帳號
- 心跳機制 A雲端cron/B手機捷徑/C手動 未選(機器睡眠本機無解)
- 堂數制主動提醒

## 計畫席交接(2026-09-05 clessia-c8 輪替)

### 計畫席退化事件(進 herdr playbook)
context 過長導致工具輸出不可信:bash 退出碼/URL、gh pr comment 回傳、SendMessage msg_id
都會污染,且會「以為送出了」。#350/#351 驗收留言查無=此故障證據(兩不同 PR 回傳共用
同一 comment id)。教訓:**計畫席也要輪替,不能無限跑**;症狀是工具輸出自我矛盾。
**新任第一件事**:查一支 PR comment 確認 API 回傳合理不自我矛盾,再開始裁決。

### 交接第一刀 —— 已完成(2026-09-05 17:1x)
#351-#355 五則計畫席本人驗收留言已補齊並回驗落地(印整個 comments 陣列,非 grep)。
#351 非蓋章:對 diff 重驗三要點(isChildAllowed 回 403 非空清單 / childDb `.in(scope)`
第二層 / recordedBy·recordedByRole 有 not.toHaveProperty 正面斷言)。

**新任自己犯的驗證器錯誤,留給下一任**:這五支的 `sync-feature-map` conclusion 是
**SKIPPED**;用「非 SUCCESS 即紅燈」過濾會得到「五支全紅」的假結論。過濾要放行
SKIPPED/NEUTRAL。又一次「驗證失敗時先懷疑驗證器」。

### 在飛線
家長端 #351 合後 admin-pages 接 03 片前端(複用 #344 child-switcher);教務日誌 v1b 等
teacher-pages(同 childDb 線);chunk 復原/及格線/堂數包已上線。
#352/#353/#354/#355 待窗口回覆後授權 steward 合(docs+CI,不碰保留類)。

### 使用者準則(MEMORY 有)
問題找解方不用工程理由結案;架構/換供應商級先討論不排單;UX至高原則;只用繁中。

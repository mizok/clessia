---
title: Herdr 多席調度
summary: 計畫席用 herdr + SendMessage 調度 domain 席的實戰手法 —— 開席序列、送達驗證、席名對位、廣度掃描的分派形狀、帳面漂移，以及兩條反覆咬人的規則:交付要有落點、掃描要掃結構不掃命名。
category: lessons
tags: [lessons, herdr, orchestration, agent-team]
status: active
updated: 2026-09-04
---

# Herdr 多席調度

> 操作性的細節(開席指令、席位表)在 [`.claude/team/README.md`](../../../.claude/team/README.md);
> 這頁收的是**可遷移的調度手法與踩過的坑** —— 2026-09-02 已整套傳授給 fvg 專案的指揮台。

## 分工:herdr 管生命週期,SendMessage 管內容

- **herdr**:開席(`worktree open` 成子 space → `agent start`)、救援(`agent read` 看卡在哪、
  `agent send-keys` 處理權限彈窗)、狀態(`agent get`)。
- **SendMessage**(Claude 內建跨 session):派工、回報、裁決 —— 所有雙向內容都走這條。
- 兩者不要混:用 herdr 的 `agent prompt` 派複雜工單只適合席位剛起、還沒有對話位址時。

## 六個要付過學費才知道的坑

1. **`agent prompt --wait` 逾時不代表沒送到,回傳成功也不代表送到了。**
   一律跟一發 `herdr agent get <席名>` 確認 status 轉 `working`。曾有 pipeline
   無條件印 ✅ 而 prompt 根本沒進去(夜班事故),也曾有設定彈窗把 prompt 吃掉。
2. **席名 ≠ session 名。** session 輪替(重開機、斷線)後 SendMessage 位址會變
   (`billing-api` 席可能叫 `pin-better-auth-3c`),每次派工前 `ListAgents` 對名,
   位址絕不寫死在任何文件 —— 為這個誤投過三次。
3. **帳面漂移是最貴的坑。** 各席對「哪支 PR 合了沒」的認知永遠過期(squash 後
   SHA 全變、MERGED 不等於進 main)。計畫席必須當唯一真相源:驗收用**內容 grep**,
   不用 commit 清單、不信 PR 狀態標籤;各席回報的狀態節照單校正。

   **回報席也有一半責任:回報狀態前重抓,不要憑記憶列表。** 2026-09-04 有一席
   報了五支 PR「在飛」,其中四支半小時前就進 main 了 —— 那是**第三席**犯同一條。
   規則很簡單:任何「現在的狀態」在送出前重跑一次查詢,而且只列查得到的。
   **讀過的狀態不等於現在的狀態**,而回報一份舊快照比不回報更糟 ——
   它會讓計畫席照著錯的帳面派工。

4. **共享資源(Docker、本機 DB、port)開是加法、關要經計畫席** —— 一席收環境
   曾殺掉另一席進行中的驗證。同時跑重 build 的席 ≤2(磁碟與 CPU 是同一台)。

5. **交付要有落點:commit ≠ 交付,訊息 ≠ 紀錄。** 同一個根因在一天內長出四種形狀,
   而**四種都回報成功**:

   | 形狀                     | 症狀                                                                  |
   | ------------------------ | --------------------------------------------------------------------- |
   | 投錯信箱                 | 用了過期的 peer 清單,整晚的回報投進另一個 session。兩邊都以為自己正常 |
   | 根本沒寄                 | 本機 commit 完就當交付,分支從未 push。對方查不到,而工具沒有錯誤       |
   | 寫在只有自己看得到的地方 | 認領標記進不了 main(worktree 推不動),別席 `git pull` 看不到           |
   | 回報舊快照               | 見坑 3                                                                |

   **判準:交付物要有一個「別人不需要你在線也拿得到」的落點** —— PR、main 上的檔案、
   或計畫席落檔的紀錄。訊息只用來通知「東西在哪」,它本身不是交付。

   推論:**認領與完成必須走同一條路**。只有認領落檔、完成沒落檔的話,佇列會累積
   假待辦 —— 而假待辦會變成重工派單(實際發生過)。

6. **掃描要掃結構,不要掃命名。** 「掃全站找 X」最常見的失敗不是漏了某個檔案,
   是**掃錯訊號**:命名慣例是後來才形成的,而存量比慣例老。

   實例(2026-09-03 的表格掃描,連錯兩輪):要找「表格在手機上另做了一份」,
   四支的手機版分別叫 `__mobile-list` / `__record-cards` / `__mobile-row` /
   `__mobile-subjects` —— **沒有共同的命名**。`grep mobile` 兩次都漏。

   可靠的訊號是它**做了什麼**:

   ```scss
   @include respond-to(…) {
     .block__table-wrap {
       display: none;
     } // ← 這個
   }
   ```

   換成這條,一次命中全部四支。

   **而這一類最難掃的原因不是技術,是它看起來像已經處理好了** —— 它確實處理了
   原本的症狀,只是用了會分岔的方式。所以掃描結果要分三類而不是兩類:
   **違規 / 正確 / 「處理了但用了會腐化的方式」**。第三類最容易被歸進第二類。

## 廣度掃描類任務的分派形狀

「掃全站找 X」不要按目錄切,**按 domain 擁有權切** —— 判斷「這處該不該改」需要
領域知識。序列:計畫席(或唯讀導航員)先跑便宜的收斂 → 命中的分派給該 domain 的席。
工單裡必附:

- **統一評審框架**(每處回答同一組問題)—— 不然多份結果合不起來
- 要求回報含 **path:line 證據**與「**查過但不是問題**」清單 —— 省 reviewer 重查,
  並逼真的開檔驗證(有人 grep 到 `subscribe` 就推論成非同步載入,錯)
- **先問「這個 X 有幾種訊號」再開工**(坑 6):只掃一種一定會漏,而差集比交集更需要
  解釋。「查過但不是問題」那份清單要再分「不適用」與「是例外」—— 前者代表判準
  根本不涵蓋它,混在一起會讓下一個人以為那一類都該修

## 席位的形狀

長壽 domain 席(配 charter,「session 會死,席位不死」)優於 per-task 蜂群 ——
廣度任務給既有席加工單,比開新席便宜,而且知識會沉澱回 charter。
合併授權、疊 PR 鐵律、通用協定見 README;本頁不複述(c11)。

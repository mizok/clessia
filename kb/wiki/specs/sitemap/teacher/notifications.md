---
title: 通知中心（/teacher/notifications）
summary: 老師端通知中心：整頁就是共用的公告收件匣，只換一個標題。
category: spec
status: developing
tags: [sitemap, teacher]
created: 2026-09-12
updated: 2026-09-13
---

# 通知中心（老師端）

<!-- generated:route-facts start —— 這一段由 tools/sitemap 生成，不要手改 -->

**路由**：`/teacher/notifications`
**角色**：老師（`teacher`）
**選單位置**：（無群組）通知中心
**額外權限**：無

<!-- generated:route-facts end -->

**進入方式**：選單「通知中心」/ 直接網址

**外框**見 [[specs/sitemap/_shared/shell-layout]]。

## 這一頁只有一行模板

```ts
template: `<app-announcement-inbox [heading]="page().label" />`;
```

整頁的內容、互動元素與狀態全部在 → [[specs/sitemap/_shared/announcement-inbox]]。
**這一層只提供標題文字**（`通知中心`）。

> 元件註解：#291 之前這裡是自己的一份實作（212 行 spec），抽成共用件時 14 條測試
> 逐條搬過去，html / scss / spec 一起刪 —— 留著只會兩份各自長。

## 互動元素

| 元素     | 說明                                            |
| -------- | ----------------------------------------------- |
| （全部） | 見 [[specs/sitemap/_shared/announcement-inbox]] |

**這一頁自己 0 個互動元素。**

## 老師端與家長端的差別（本輪確認）

**元件相同，資料不同** —— 共用元件不代表兩邊看到一樣的東西：

|                      | 家長端（labor-5 量）     | **老師端（本輪量）**                 |
| -------------------- | ------------------------ | ------------------------------------ |
| 公告則數             | 1                        | **4**                                |
| 未讀                 | 1                        | 1                                    |
| 帶 `campusName` 的行 | **沒有**（該則沒有校區） | **有** —— `· 示範分校01`（3 則）     |
| 內容                 | 全體家長的公告           | 給老師的公告（停課／代課／會議通知） |

**版面與互動元素完全一致**，差異只在資料。
`campusName` 那一行家長端標成未驗，**老師端補驗到了**（已回填共用頁）。

## 狀態

見 [[specs/sitemap/_shared/announcement-inbox]]。這條路由沒有 `permission`。

## 驗證紀錄

- **日期**：2026-09-12 ／ **帳號**：`teacher0003@demo.clessia.app`（張品妍）
- **前端版本**：port 4202（本 worktree），`7622b0c4` ／ **寬度**：1504 CSS px
- **資料狀態**：4 則公告、1 則未讀

### 兩向比對

| 方向        | 結果                                                           |
| ----------- | -------------------------------------------------------------- |
| 地圖 ⊆ 畫面 | 共用頁列的元素都找得到（未讀徽章、全部標為已讀、每則一顆按鈕） |
| 畫面 ⊆ 地圖 | 5 個可見元素 = `全部標為已讀` 1 + 公告 4                       |

**差異：0 筆。** DOM 5、可見 5。

### 補驗了共用頁兩個「未驗」項（而且沒有寫入）

`_shared/announcement-inbox` 留了「一次只能展開一則」與「再按一次收合」兩個未驗，
理由是家長端只有一則公告。老師端有 4 則，**其中 3 則已讀** ——
**展開已讀的那幾則不會產生任何寫入**（未讀才會被標記），所以可以安全地驗：

| 動作                    | 結果                                                                                    |
| ----------------------- | --------------------------------------------------------------------------------------- |
| 展開第 2 則（已讀）     | 內文出現；**未讀徽章仍是 `1 則未讀`、紅點仍是 1 顆** → 沒有寫入 ✓                       |
| 接著展開第 3 則（已讀） | `.announcement-inbox__body` **仍然只有 1 個**，內容換成第 3 則 → **一次只能展開一則** ✓ |
| 對同一則再按一次        | `body` 變 0 個，徽章與紅點不變 → **再按一次是收合** ✓                                   |

發布者那一行也量到了：`—— Demo Admin`。

### 未驗到的

| 項目                     | 原因                                                     |
| ------------------------ | -------------------------------------------------------- |
| 「全部標為已讀」         | **寫入，不按**                                           |
| 展開**未讀**的那一則     | 會寫入（家長端已經證明過那個行為，不必在老師端再做一次） |
| 空狀態、查詢失敗、載入中 | 本機有資料、沒有製造手段                                 |
| 手機寬度                 | 只量 1504px                                              |

## 390px

- **量測**：390 × 844 ／ 768 × 1024 ／ 1024 × 768 ／ 1504 × 752
- **前端**：主 checkout 的 dev server（port 4200），`543e5eda`
- **身分**：`teacher0026@demo.clessia.app`（劉語涵，`roles:["teacher"]`）——
  量測前後各打一次 `GET /api/me`，兩次相同
- **取樣器**：方法頁的四道濾網版（含 ④a 的可視區修正與 ④b 的 `visibleFast`）
- **手段**：同源 iframe 當 viewport

### 版面怎麼變

**沒有斷點**，只有公告摘要的寬度在變：

| 元素 | 390 | 768 | 1024 | 1504 |
| --- | --- | --- | --- | --- |
| `button.announcement-inbox__summary` | `334 × 47` | `432 × 47` | `560 × 47` | `560 × 47` |
| `button.announcement-inbox__mark-all`（全部標為已讀） | `108 × 44` | 同左 | 同左 | 同左 |

**1024 起達到上限 `560`**，所以 1024 與 1504 完全相同。

> **這四個數字跟 [[specs/sitemap/parent/notifications]] 逐字相同** —— 兩端共用
> `app-announcement-inbox`，本輪兩邊各量一次，四個寬度的尺寸與元素集合完全一致。
> **共用元件的響應式行為因此是量過的，不是假設的。**

### 差集（1504 ↔ 390）

**零差異。**

### 水平溢出

**無**（四個寬度）。**整頁不產生捲動容器**（本機老師端只有 1 則公告）。

### 觸控目標 < 44px

**0 筆** —— 兩顆都達標（`44` 與 `47`）。

### 鍵盤可達性

- 全頁**沒有正數 `tabindex`** → Tab 序列 = DOM 序。
- **Tab 序列**：`全部標為已讀` → 公告摘要（2 個，四寬度相同）。
- **click-only 而鍵盤到不了**：`<main>` 內 **0 筆**
  （`span.announcement-inbox__item-title` / `__dot` / `__meta` 都在 `<button>` 裡面）。

### 未驗與原因

| 項目 | 原因 |
| --- | --- |
| **按「全部標為已讀」** | **寫入類動作，一律不按**（會改 `announcement_reads`） |
| 展開公告摘要 | 會送出已讀（同上），沒按 |
| 未讀 0 則 / 多則的樣子 | 本機老師端只有 1 則未讀公告。**要造更多只能寫入**，不做 |
| 焦點環看不看得見 | **這個環境量不到**（`document.hasFocus()` 恆 `false`） |
| `Escape` / 真的 Tab 鍵 | 需要前景分頁（方法頁坑 12） |

## 載入中 / 錯誤

- **量測**：390 × 844 ／ dev server（port 4200）`543e5eda`
- **手段**：把 XHR 的 URL 從 `:8787` 改指到沒人監聽的 `:8799`（等同 server 掛掉，
  **只影響那一個 iframe**，不動共享資源）。方法見[方法頁 Phase 2-D](../README.md)
- **證據**：每一輪都確認攔截清單不是空的（**請求數 0 的一律作廢**）
- ⚠️ **本輪只量「錯誤」那一半**

### 錯誤（所有 API 都失敗）

| 判定 | 重試鈕 | toast | 攔到的請求數 |
| --- | --- | --- | --- |
| ✅ 誠實 | 否 | 0 | 1 |

主體有失敗字樣。

### 未驗與原因

| 項目 | 原因 |
| --- | --- |
| 載入中的樣子 | 本輪只量錯誤態（做法已驗過：把 XHR 的 `send` 延後 3 秒） |
| 重試鈕按了會不會真的重打 | 需要真滑鼠（方法頁坑 12） |
| 其他寬度 | 錯誤態與寬度無關（是狀態不是版面），只量 390 |

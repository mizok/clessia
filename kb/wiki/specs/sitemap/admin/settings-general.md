---
title: 一般設定（/admin/settings/general）
summary: 系統設定的「一般」tab：整頁只有一個設定 —— 出勤紀錄模式（隨堂點名／日到班）。
category: spec
status: developing
tags: [sitemap, admin]
created: 2026-09-12
updated: 2026-09-13
---

# 一般設定

<!-- generated:route-facts start —— 這一段由 tools/sitemap 生成，不要手改 -->

**路由**：`/admin/settings/general`
**角色**：管理員（`admin`）
**選單位置**：**選單不露出**（只能從別頁導過來或直接打網址）
**額外權限**：無

<!-- generated:route-facts end -->

**權限變體**：任何 admin 角色都進得去（不論 `permissions`），頁內的寫入鈕也不會因為缺權限而消失 —— 按下去由 API 擋。逐權限的量測見 [[specs/sitemap/_shared/permission-matrix]]。

**進入方式**：[[specs/sitemap/admin/settings]] 的「一般」tab / 直接網址

**外框**見 [[specs/sitemap/_shared/shell-layout]]；**tab 列屬於** [[specs/sitemap/admin/settings]]。

## 畫面區塊

### 1. 頁首

`一般設定`（h2，用 `page().label`）。**沒有副標。**

### 2. 出勤紀錄模式（整頁唯一的設定）

說明兩行：

> **隨堂點名**：老師在每堂課結束後逐一標記學生出席狀態。
> **日到班**：學生抵達分校打卡後，系統自動勾選當天所有課堂為出席。

控制列：二選一（`隨堂點名` / `日到班`）+ 一條分隔線 + `儲存`。

資料來源：`GET /api/org` 的設定；儲存是 `PATCH`（`OrgSettingsService.updateSettings`）。

> **這個設定會改變別的頁面**：`/admin/dashboard` 的課表列能不能按、
> `/admin/sessions` 的點名入口，都看 `mode === 'per_session'`
> （見 [[specs/sitemap/admin/dashboard]]）。**本機是「隨堂點名」。**

## 互動元素

| 元素（畫面上的字） | 類型                  | 出現條件 | 按了之後                                           |
| ------------------ | --------------------- | -------- | -------------------------------------------------- |
| 隨堂點名 / 日到班  | select button（單選） | 取數成功 | **只改本地表單狀態**，不寫入（要按「儲存」才送出） |
| 儲存               | 按鈕                  | 取數成功 | **寫入** `PATCH /api/org` → 本輪未按               |

> **`disabled`**：這一頁沒有任何 `[disabled]` 繫結；`儲存` 只有 `[loading]`。
> **取數失敗時這三個元素根本不渲染**（#805）—— 不是 disabled，是不存在。

## 狀態

| 狀態   | 畫面                                                              |
| ------ | ----------------------------------------------------------------- |
| 載入中 | 兩塊 `p-skeleton`（2rem + 3rem），**整段設定不畫**                |
| 錯誤   | `app-load-failed`（「載入失敗」＋重試），**表單不畫**；不發 toast |
| 空     | 不適用 —— 這一頁沒有清單                                          |
| 無權限 | 不適用 —— 這條路由沒有 `permission`                               |

## 驗證紀錄

- **日期**：2026-09-12 ／ **帳號**：`admin@demo.clessia.app`
- **前端版本**：port 4202（本 worktree），`8c286802` ／ **寬度**：1504 CSS px
- **目前設定**：`隨堂點名` 被選中（`p-togglebutton-checked` + `aria-pressed="true"`），
  `日到班` 是 `aria-pressed="false"`

### 兩向比對

| 方向        | 結果                                                   |
| ----------- | ------------------------------------------------------ |
| 地圖 ⊆ 畫面 | 三個元素全部找得到                                     |
| 畫面 ⊆ 地圖 | 7 個可見元素 = tab 4（屬殼）+ 隨堂點名 + 日到班 + 儲存 |

**差異：0 筆。** DOM 7、可見 7 —— 沒有隱藏元素。**這是本批最小的一頁。**

### 未驗到的

| 項目                           | 原因                                                                                                   |
| ------------------------------ | ------------------------------------------------------------------------------------------------------ |
| 切到「日到班」                 | 切了不寫入，但**會讓表單狀態與後端不一致地留在畫面上**；且真正要驗的是「儲存之後別頁跟著變」，那是寫入 |
| 「儲存」與它的成功／失敗 toast | 寫入                                                                                                   |
| 載入中、錯誤                   | 沒有製造手段                                                                                           |
| 手機寬度                       | 只量 1504px                                                                                            |

## 390px

- **量測**：390 × 844 / 768 × 1024 / 1024 × 768 / 1504 × 752 ／ 前端 `3f2197f8`（4200，主 checkout ＝ `origin/main`）／ 身分 `admin@demo.clessia.app`（量測前後各打一次 `GET /api/me`）
- **手段**：同源 iframe 當 viewport；`< 44px` 的「真機」欄是**把 `(pointer: coarse)` 區塊注進去重量一次**得到的，不是推測（方法頁 Phase 2）
- **外框**（頂列、側欄 ↔ 底欄）不屬於這一頁，見 [[specs/sitemap/_shared/shell-layout]]

### 版面怎麼變

**沒有變。** 四個寬度掃描全部可見容器的版面訊號，**沒有任何一個容器改變**。
這一頁是一段說明文字 + 兩顆二選一 + 一顆儲存。

### 差集

**零差異。** 四個寬度都是同樣 7 個：分頁籤 ×4、
`p-togglebutton` ×2（隨堂點名 / 日到班）、`儲存` ×1。

### 水平溢出

**無**（四個寬度皆然）。

### 四個分頁籤在每一個寬度都一樣

`分校 / 學校 / 科目 / 一般` 四顆 `p-tab`，**各 `62 × 53`，四個寬度逐項相同**，
沒有橫向捲、沒有收成下拉。（對照 [[specs/sitemap/admin/courses]] 的分校頁籤：
那裡有 13 顆，390 下只有 4 顆在畫面內。**顆數少的時候不需要捲，這是資料量的差別不是設計的差別。**）

### 觸控目標 < 44px（390）

**一個都沒有** —— 這是整批 admin 30 頁裡**唯一一頁 390 下所有互動元素都已經 ≥ 44px 的**。

（分頁籤 `62 × 53`；兩顆 togglebutton 與儲存鍵都沒有進 `< 44` 清單。
注意這一頁的 togglebutton 跟 [[specs/sitemap/admin/meals]]、
[[specs/sitemap/admin/reports]] 那幾顆**尺寸不同** —— 那幾頁是 `p-inputfield-sm` 或
更小的字級，這一頁不是。）

### 鍵盤可達性

- 全頁**沒有正數 `tabindex`**；390 下 **Tab 站 4 個**（四個分頁籤）
  / 可見互動元素 7 個
- **`cursor: pointer` 但 Tab 不到的元素：0 個**

### 未驗與原因

| 項目                                | 原因                                                                                                                                                                   |
| ----------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 切換出勤模式並儲存                  | **會寫入**，零寫入邊界。⚠️ 而且 seed 的註解明講過：把 `attendance_retroactive_days` 之類的設定翻掉會讓老師課表上所有舊課堂變成「點名已截止」，**那是翻掉最後一個實例** |
| 財務設定欄位（要 `manage_finance`） | 這一輪用的是 `["*"]` 的帳號，看得到全部；單一權限帳號的樣子屬 #759 的矩陣                                                                                              |
| `Escape` / 真的 Tab 鍵              | 需要前景分頁                                                                                                                                                           |

## 載入中 / 錯誤

- **量測**：390 × 844 ／ 主 checkout 的 dev server（port 4200）`7e9da649` ／
  `admin@demo.clessia.app`（permissions `["*"]`，量測前後各打一次 `/api/me`）
- **手段**：同一個 XHR 包裝 —— 錯誤態改指 `:8799`，載入中把 `send` 延後 3 秒
  （**只影響那一個 iframe，不動 8787**）。方法見[方法頁 Phase 2-D](../README.md)
- ⚠️ **導航方式跟其他頁不同，見下** —— 這四個頁籤**進不去**，只能「完整載入進 shell、再切頁籤」

> 🔴 **怎麼進來，決定你量不量得到這一頁。**
>
> `/admin/settings/*` **用 SPA 導航進不去 —— 會整頁空白**（issue #804）：
> 側邊選單的連結、`router.navigateByUrl()`、直接指子路由，三條路都一樣，
> `SettingsShellPage` 建構時炸在 `Cannot read properties of undefined (reading 'routeConfig')`。
>
> **所以本輪的量法是**：先**完整載入** `/admin/settings/<某個頁籤>`（這條路正常），
> shell 建好之後**在 shell 內切頁籤**（`/admin/settings/schools` ↔ `campuses` …，這條路也正常）。
> 兩段都實測過。
>
> 先前 #760 把這四頁記成「頁籤不是 `<a href>`，SPA 導航進不去，沒有硬鑽」——
> **前半是對的，但真正擋住的是那個 crash**，而 crash 的樣子（畫面空白、網址不動）
> 跟「導航沒有發生」一模一樣。

### 載入中（3 秒延遲）

| skeleton | spinner | `<main>` 互動元素 | 判定 |
| --- | --- | --- | --- |
| **2**（`p-skeleton 268×32` + `p-skeleton 268×48`） | 0 | 1 → 4 | ✅ 誠實 |

延遲期間只有頁籤列與「一般設定」標題，設定區是兩條骨架。

⚠️ `animation-name: skeleton-wave` —— 看得見，波紋不動（坑 12）。

### 錯誤（所有 API 都失敗）

| 判定 | 重試鈕 | toast | 攔到的請求數 |
| --- | --- | --- | --- |
| 🔴 **渲染硬編碼的預設值，而且儲存鈕可按** | 否 | 1 | 1 |

toast「**錯誤／無法載入系統設定**」（會自己消失）。主體**照常渲染整個表單**：

| 量到的 | 值 |
| --- | --- |
| `隨堂點名` | `aria-checked="true"`、`p-togglebutton-checked` |
| `日到班` | `aria-checked="false"` |
| `儲存` 按鈕 | **`disabled === false`** |

機制（`settings.page.ts:31` / `:38-50`，`settings.page.html:8`）：

```ts
protected attendanceModeValue: AttendanceMode = 'per_session';   // 硬編碼預設
...
error: () => { this.messageService.add(...); this.loading.set(false); }   // 沒有 failed 旗標
```

模板只有兩態 `@if (loading()) { 骨架 } @else { 表單 }` —— **沒有錯誤態**，
所以失敗時走 `@else`，選中的是那個沒讀到的預設值，而 `saveAttendanceMode()`
會把它送進 `updateSettings()`。

> **這比「謊稱沒資料」更危險**：那一族是讀的問題，這一支**接到寫入路徑上** ——
> 一間實際設定為 `daily_checkin` 的補習班，在讀取失敗時會看到「隨堂點名」被選中，
> 按下儲存就靜靜換掉出勤模式（那個設定決定老師端有沒有點名入口）。
>
> ⚠️ **本機看不出來**：`select attendance_mode from organizations` = **`per_session`**，
> 剛好等於硬編碼的預設值 —— **所以在這台機器上「錯誤態顯示的值」與真實值永遠一致。**
> 這是「兩邊一樣可能是比較本身沒有鑑別力」的又一個實例。

**已開 issue #805 給計畫席**（不順手修）。

### 未驗與原因

| 項目 | 原因 |
| --- | --- |
| 按下儲存真的會寫入嗎 | **沒有按**（Phase 2-D 零寫入）。那一半從原始碼推得，留給 #758 的寫入窗口 —— 要驗的話先記下 `attendance_mode`，驗完 `db:reset` |
| toast 停留多久 | 沒量 |
| 其他寬度 | 只量 390 |

## 寫入類動作（實按）

- **量測**：1024 × 768 ／ 主 checkout 的 dev server（port 4200）`9744f0f6…（主 checkout，本輪期間為 `#810`／`#807` 合併後的 main）` ／
  `admin@demo.clessia.app`（`["*"]`，量測前後各打一次 `/api/me`）
- **輪次**：#758 **第 1 輪（系統設定）／本頁是第 4 頁**
- ⚠️ **這一輪沒有 `db:reset`** —— `npm run db:reset` 被 `.claude/settings.json` 的 `deny` 擋住
  （那是刻意的全隊安全欄，`.claude/settings.md:72` 寫明「會清掉本機資料與 seed」）。
  計畫席因此裁定改走**「只按可自行復原的動作」**：建立一筆自己的測試資料、對它走完整生命週期、
  最後刪掉，**不碰任何 seed 資料**。識別字統一是 `QA-758-R1*`
- **證據**：**每一顆按完都查一次 DB**（`psql`），畫面說成功不等於資料落地
- ⚠️ **證據等級**：真滑鼠在這個環境整片失效（方法頁坑 6／坑 12），所以按鈕用
  `element.click()`（合成事件，`isTrusted: false`）、欄位用「原生 setter + 派 `input` 事件」。
  **每一下都記了 click 監聽器的計數差**（`hitDelta`），計數沒動就作廢。
  驗不到的：`pointerdown`、hover、失焦才關的浮層

### 按了哪一顆（1 顆，來回各一次）

唯一的寫入是「儲存」，它送 `PATCH /api/org/settings`（`organizations.attendance_mode`）。
**這是組織級設定，沒有 `db:reset` 可用，所以做成來回**：`per_session` → `daily_checkin` → 改回 `per_session`。

| 步驟 | 畫面 | toast | DB |
| --- | --- | --- | --- |
| 點「日到班」 | 該顆變 `--checked`，另一顆取消 —— **只是本地狀態，還沒送出** | — | 沒變 |
| 按「儲存」 | 畫面不變（表單本來就長那樣） | `已儲存／出勤模式已更新` | `attendance_mode` = **`daily_checkin`** |
| 點「隨堂點名」→ 再按「儲存」 | 同上 | `已儲存／出勤模式已更新` | 改回 **`per_session`** |

**兩次的 toast 逐字相同** —— 訊息裡沒有說改成了哪一個模式。

### 沒有確認框

**這是這一輪唯一一個「按下去直接生效、沒有二次確認」的寫入。** 分校的停用／刪除、學校的刪除、
科目的刪除都有確認框，而**影響面最大的這一顆沒有** ——
`attendance_mode` 決定老師端有沒有點名入口、出勤紀錄怎麼產生。

### 稽核

🔴 **沒有 `audit_logs`。** `grep -c "audit" apps/api/src/routes/org-settings.ts` → **0**。

> **「沒有確認框」＋「沒有稽核」＋（已修的 #805）「取數失敗時渲染硬編碼預設值且儲存鈕可按」
> 是同一顆按鈕上的三件事。** 前兩件仍然成立，已開 issue #828。

### 未驗與原因

| 項目 | 原因 |
| --- | --- |
| 切換出勤模式之後老師端會怎麼變 | 那是跨角色的行為，要換身分重量；本輪只驗這一顆鈕本身寫進了什麼 |
| 儲存失敗的樣子 | 要製造失敗（改指死 port），而那會跟這一輪的寫入交錯。**留給錯誤態那一族** |

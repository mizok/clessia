---
title: 學生詳情（/admin/students/:id）
summary: /admin/students/:id 的實際 UI 地圖：五個區塊（兩個標「開發中」）、在籍班級的條件式計費鈕與三支對話框。
category: spec
status: developing
tags: [sitemap, admin]
created: 2026-09-12
updated: 2026-09-13
---

# 學生詳情

<!-- generated:route-facts start —— 這一段由 tools/sitemap 生成，不要手改 -->

**路由**：`/admin/students/:id`
**角色**：管理員（`admin`）
**選單位置**：**選單不露出**（只能從別頁導過來或直接打網址）
**額外權限**：無

<!-- generated:route-facts end -->

**進入方式**：`/admin/students` 列選單的「學生詳情」（**唯一入口** —— 那一頁的列本身不可點）／直接網址。

**外框**見 [[specs/sitemap/_shared/shell-layout]]。

## 畫面區塊

### 0. 頁首

麵包屑 `學生`（連 `/admin/students`）、`學生列表` 返回鈕、學生姓名（h1）、`編輯資料`。

**條件式**：頁面頂端另有兩種浮出內容 ——
`notice()` 提示條、`conflictPrompt()` 排課衝突提示（含 `取消`／`仍要報名` 兩顆）。
**兩者都未驗到**（需要觸發加班衝突，而那會寫入）。

### 1. 基本資料

年級、學校、生日、性別、電話、Email、居住地址、緊急聯絡人、備註。

- **電話**是 `tel:` 連結、**Email** 是 `mailto:` 連結 —— 各自只在有值時渲染
- 緊急聯絡電話、備註同樣條件式

### 2. 關聯家長

**兩個分支都驗到了**：

| 分支                   | 畫面                                                                                                                                          |
| ---------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- |
| `parents.length === 0` | 「尚未關聯家長 —— 到 **家長管理** 找到該家長，在詳情裡用「綁定既有學生」把這位學生加進去。」（`家長管理` 是連到 `/admin/parents` 的行內連結） |
| 有家長                 | 每位一列：姓名、關係（`parent`）、`mailto:` Email；`主要` 徽章（`parent.isPrimary`）與電話為條件式                                            |

> **這個空狀態不給「新增家長」按鈕，而是指路。** 綁定的入口在家長那一側
> （[[specs/sitemap/admin/parents]] 的詳情對話框裡的「輸入姓名綁定既有學生」）。

### 3. 在籍班級

標題列右側 `加入班級` —— **只在 `student()?.isActive`**（展示資料 69 名全在籍，
**停用學生的畫面未驗到**）。

每一列：班名 · 分校 · 科目 · 自 `<起始日>`。**整列可點**，導向
`/admin/courses/:courseId/classes/:classId`。

**條件式 `設定計費`**：只在 `needsBillingSetup(enrollment)` 為真的那幾列。
實測兩名學生分別是 **3 列中 1 列**、**3 列中 2 列** —— 確認它是逐列判斷不是全有全無。

### 4-5. 出缺席紀錄 ／ 成績紀錄

**兩塊都標著「開發中」徽章**（`--deferred`），區塊在但沒有內容。

## 互動元素

| 元素（畫面上的字）  | 類型     | 出現條件                       | 按了之後                                                        |
| ------------------- | -------- | ------------------------------ | --------------------------------------------------------------- |
| `學生`（麵包屑）    | 連結     | 永遠                           | `/admin/students`                                               |
| `學生列表`          | 按鈕     | 永遠                           | `goBack()` → `/admin/students`                                  |
| `編輯資料`          | 按鈕     | 永遠                           | 開 [[specs/sitemap/_shared/student-form-dialog]]（編輯模式）    |
| 電話號碼            | 連結     | **有電話時**                   | `tel:`                                                          |
| Email               | 連結     | **有 Email 時**                | `mailto:`                                                       |
| `家長管理`          | 行內連結 | **只在無關聯家長時**           | `/admin/parents`                                                |
| `加入班級`          | 按鈕     | **只在 `isActive`**            | 開班級挑選對話框（[[specs/sitemap/admin/students]] 同一支元件） |
| 在籍班級列          | 可點列   | 每一筆在籍                     | `/admin/courses/:courseId/classes/:classId`                     |
| `設定計費`          | 按鈕     | **只在 `needsBillingSetup()`** | 開報名計費對話框（**未驗** —— 見下）                            |
| `取消` / `仍要報名` | 按鈕     | **只在排課衝突提示出現時**     | **未驗** —— 觸發它需要製造衝突                                  |

## 子頁面

- [[specs/sitemap/_shared/student-form-dialog]] —— `編輯資料`（編輯模式，無家長欄）
- **班級挑選對話框**（`ClassPickerDialogComponent`）—— `加入班級`。
  搜尋框有 `debounceTime(300)` + `distinctUntilChanged` + `switchMap`（#661／PR #690）。
  **未在這一頁實測**（挑一個班就會寫入報名）
- **報名計費對話框**（`EnrollmentBillingDialogComponent`）—— `設定計費`，**未驗**

## 驗證紀錄

- **狀態**：已驗（主要路徑）
- **前端版本**：`650d257a`，自架 `:4210`
- **兩向比對**：取樣兩名學生 ——
  - `傅千惠`（無家長、3 列在籍、1 顆設定計費）：**10 個可見互動元素，0 不可見**
  - `出勤測試學生01`（有家長、3 列在籍、2 顆設定計費）：**11 個**
  - 兩次都 **0 差異**；兩者的差額逐項對得上（家長 Email 連結 +1、設定計費 +1、電話↔Email 互換）
- **未驗，附原因**：
  - `notice()` 與 `conflictPrompt()` 兩種浮出內容 —— 觸發條件需要寫入
  - 停用學生的畫面（`加入班級` 會消失）—— 展示資料 69 名**全部在籍**
  - `主要` 家長徽章（`isPrimary`）—— 取樣到的那位沒有
  - 班級挑選、報名計費兩支對話框 —— **開啟即可，但選下去就是寫入**，本輪只到入口
- **刻意不做**：`加入班級` 與 `設定計費` 都沒有按。
  它們不像確認框有「取消」可退，**開啟本身就在一個會寫入的流程裡**。

## 390px

- **量測**：390 × 844 / 768 × 1024 / 1024 × 768 / 1504 × 752 ／ 前端 `3f2197f8`（4200，主 checkout ＝ `origin/main`）／ 身分 `admin@demo.clessia.app`（量測前後各打一次 `GET /api/me`）
- **手段**：同源 iframe 當 viewport；`< 44px` 的「真機」欄是**把 `(pointer: coarse)` 區塊注進去重量一次**得到的，不是推測（方法頁 Phase 2）
- **外框**（頂列、側欄 ↔ 底欄）不屬於這一頁，見 [[specs/sitemap/_shared/shell-layout]]

- **量的那一筆**：`出勤測試學生01`（報名數最多的學生，3 筆在籍班級）—— 挑它是因為
  **在籍班級列有資料**（方法頁第 0 步）

### 版面怎麼變

**只有一件事在變：基本資料是一欄還是兩欄。**

| 容器                        | 390             | 768             | 1024                      | 1504          |
| --------------------------- | --------------- | --------------- | ------------------------- | ------------- |
| `student-detail__info-grid` | `316px`（單欄） | `414px`（單欄） | **`303px 303px`（兩欄）** | `393px 393px` |

橘帶、在籍班級列、家長區塊在四個寬度是同一個形狀。

### 差集

**零差異。** 四個寬度都是同樣 11 個互動元素：麵包屑 ×1、email 連結 ×2、
`p-button-outlined` ×2、`p-button-secondary` ×1、`p-button-sm` ×2、在籍班級列 ×3。

### 水平溢出

**無**（四個寬度皆然）。

### 觸控目標 < 44px（390）

| 元素                                        | 390 實測                | 真機         |
| ------------------------------------------- | ----------------------- | ------------ |
| `breadcrumb__item--link`                    | `28 × 21`               | `28 × 21`    |
| `student-detail__email-link` ×2             | `316 × 18` / `242 × 18` | 不變         |
| `p-button-secondary` / `p-button-outlined`  | `114 × 41`              | `114 × 45` ✓ |
| `p-button-sm` ×2、`p-button-outlined`（小） | `100 × 34`              | `100 × 44` ✓ |

### 鍵盤可達性

- 全頁**沒有正數 `tabindex`**；390 下 **Tab 站 11 個 ＝ 可見互動元素 11 個**（完全對齊）
- **`cursor: pointer` 但 Tab 不到的元素：0 個**

> 🟢 **方法頁引用這一頁的那個例子已經過時，要訂正。**
> `kb/wiki/specs/sitemap/README.md` 的「元素清單以 DOM 為準」一節寫著：
> 「`/admin/students/:id` 的在籍班級列是 `<div>` 帶 `(click)`、**沒有 `tabindex`** ——
> 可以點但鍵盤到不了」。
>
> **2026-09-13 實測：那三列現在是 `<div tabindex="0" role="button">`**，
> 取樣器抓得到、鍵盤也到得了。**那條規則本身仍然成立**（側欄群組標題與頂列使用者區塊
> 就是活的例子），只是**這個舉例的對象已經被修好了**。
> 沒有動方法頁的那一段 —— 交給計畫席裁定要不要換一個例子。

### 未驗與原因

| 項目                                  | 原因                                            |
| ------------------------------------- | ----------------------------------------------- |
| 停用學生的詳情頁（`加入班級` 會消失） | 本機只有 1 名停用學生，這一輪量的是在籍的那一位 |
| 主要家長徽章                          | 這一位沒有 `isPrimary` 家長                     |
| 各種對話框在 390 的版面               | 對話框內容不在 `<main>` 裡                      |
| `Escape` / 真的 Tab 鍵                | 需要前景分頁                                    |

---
title: 學生視角（/admin/grades/overview/student）
summary: 成績總覽的學生視角：可篩選的學生清單，點一位開成績明細對話框。
category: spec
status: developing
tags: [sitemap, admin]
created: 2026-09-12
updated: 2026-09-13
---

# 學生視角

<!-- generated:route-facts start —— 這一段由 tools/sitemap 生成，不要手改 -->

**路由**：`/admin/grades/overview/student`
**角色**：管理員（`admin`）
**選單位置**：**選單不露出**（只能從別頁導過來或直接打網址）
**額外權限**：無

<!-- generated:route-facts end -->

**進入方式**：[[specs/sitemap/admin/grades-overview]] 的「學生視角」卡片 / 直接網址

**外框**見 [[specs/sitemap/_shared/shell-layout]]。

## 畫面區塊

- 麵包屑：`成績總覽 › 學生視角`
- 篩選列：四個下拉（`全部分校` / `全部年級` / `全部學校` / `啟用`）＋ 搜尋框 `搜尋學生姓名...`
- 學生清單：一位學生一列（姓名、年級、就讀學校、分校），整列可按
- 分頁器：`<< < 1 2 3 4 > >>`，每頁 20 位

## 互動元素

| 元素（畫面上的字）                    | 類型    | 出現條件                              | 按了之後                                   |
| ------------------------------------- | ------- | ------------------------------------- | ------------------------------------------ |
| 成績總覽（麵包屑）                    | 連結    | 永遠                                  | 回 [[specs/sitemap/admin/grades-overview]] |
| 全部分校 / 全部年級 / 全部學校 / 啟用 | 下拉 ×4 | 永遠                                  | 篩選清單（**未展開選項**）                 |
| 搜尋學生姓名…                         | 搜尋框  | 永遠                                  | **300ms debounce 後**重新取數（見下）      |
| 篩選                                  | 按鈕    | **只有手機版**（桌機在 DOM 但不可見） | —                                          |
| 學生列（整列）                        | 按鈕    | 每位學生一列                          | 開對話框 →「子頁面：學生成績明細」         |
| 分頁 << < 1…4 > >>                    | 按鈕    | 永遠（首頁時 `<<`、`<` disabled）     | 換頁                                       |

> **搜尋的取消行為**：這一支在 #661 / PR #679 被收進 `switchMap` 管線（debounce 300ms +
> 舊請求取消）。它的取數會 `forkJoin` 扇出到 `totalPages` 支，所以那一支修正在這一頁
> 省掉的請求量比別頁大。**本輪沒有重驗那個行為**，只記它存在。

## 子頁面：學生成績明細

**開啟方式**：點任何一位學生。**只有這一頁開得到**（`grep -rl StudentScoreDetailDialogComponent`
只命中 student-view 自己），所以不進 `_shared/`。

- 標頭：學生姓名 + 年級
- 篩選：考試型態下拉（`全部`）、科目下拉（`全部科目`）、期間 toggle（`近1月` / `近3月` / `近半年` / `全部`）
- 內容：成績紀錄
- 結束動作：`關閉`

**空狀態（實測到）**：`尚無成績紀錄` / `此學生目前沒有符合篩選條件的成績`

## 狀態

| 狀態                   | 畫面                                |
| ---------------------- | ----------------------------------- |
| 明細對話框空           | 見上                                |
| 清單載入中 / 空 / 錯誤 | **未驗**                            |
| 無權限                 | 不適用 —— 這條路由沒有 `permission` |

## 驗證紀錄

- **日期**：2026-09-12 ／ 帳號 `admin@demo.clessia.app` ／ 前端 `897d691f`（port 4201）
- **資料狀態**：4 頁學生；第一位「傅千惠」沒有任何成績

### 兩向比對

|                          | 數量   |
| ------------------------ | ------ |
| `<main>` 內 DOM 互動元素 | 35     |
| 不可見（手機版「篩選」） | 1      |
| **可見**                 | **34** |

34 = 麵包屑 1 + 下拉 4 + 搜尋 1 + 學生列 20 + 分頁 8。

| 方向        | 結果                      |
| ----------- | ------------------------- |
| 地圖 ⊆ 畫面 | 表列元素都找得到          |
| 畫面 ⊆ 地圖 | 34 個可見元素都有對應條目 |

**差異：0 筆。**

明細對話框內可見互動元素 **7** 個（關閉 + 2 下拉 + 4 期間 toggle），地圖列 7 項，差異 0 筆。

### 未驗到的

| 項目                       | 原因                                         |
| -------------------------- | -------------------------------------------- |
| 四個下拉的選項             | 未展開                                       |
| 搜尋的 debounce / 取消行為 | 已在 #679 驗過，本輪沒重驗                   |
| 有成績的學生的明細長什麼樣 | 只開了第一位（零成績）；**未換一位有成績的** |
| 清單載入中 / 空 / 錯誤     | 沒有製造手段                                 |

## 390px

- **量測**：390 × 844 / 768 × 1024 / 1024 × 768 / 1504 × 752 ／ 前端 `3f2197f8`（4200，主 checkout ＝ `origin/main`）／ 身分 `admin@demo.clessia.app`（量測前後各打一次 `GET /api/me`）
- **手段**：同源 iframe 當 viewport；`< 44px` 的「真機」欄是**把 `(pointer: coarse)` 區塊注進去重量一次**得到的，不是推測（方法頁 Phase 2）
- **外框**（頂列、側欄 ↔ 底欄）不屬於這一頁，見 [[specs/sitemap/_shared/shell-layout]]

### 版面怎麼變：跟班級視角同形，但連搜尋框都收起來

| 東西                                  | 390        | 768        | 1024   | 1504   |
| ------------------------------------- | ---------- | ---------- | ------ | ------ |
| `p-button-outlined`（篩選）           | **在**     | **在**     | 不存在 | 不存在 |
| `student-view__search`（搜尋框）      | **不存在** | **不存在** | **在** | **在** |
| 4 個下拉（分校 / 年級 / 學校 / 科目） | 不存在     | 不存在     | **在** | **在** |
| 完整分頁器（首末頁 + 頁碼 ×4）        | 不存在     | 不存在     | **在** | **在** |

> **比 [[specs/sitemap/admin/grades-overview-class]] 多收一樣東西**：
> 班級視角的搜尋框四個寬度都在，學生視角的**搜尋框在 ≤768 是收在「篩選」裡的**。
> 同一組頁面、同一個設計語彙，兩頁的收法不一樣。

分頁器的門檻也跟別頁不同 —— 這一頁**在 1024 就是完整的**，而
[[specs/sitemap/admin/students]] 要到 1504。（`responsive-table` 看的是容器寬，坑 10。）

### 差集

| 簽名                                            | 390   | 768   | 1024      | 1504   |
| ----------------------------------------------- | ----- | ----- | --------- | ------ |
| `button.p-button-outlined`（篩選）              | **1** | **1** | 0         | 0      |
| `input.student-view__search-input`              | 0     | 0     | **1**     | **1**  |
| `span.p-placeholder.p-select-label`             | 0     | 0     | **3**     | **3**  |
| `span.p-select-label` / `div.p-select-dropdown` | 0     | 0     | **1 / 4** | 1 / 4  |
| `p-paginator-page`                              | 0     | 0     | **4**     | **4**  |
| `p-paginator-first` / `p-paginator-last`        | 0     | 0     | **1 / 1** | 1 / 1  |
| 合計                                            | 24    | 24    | **38**    | **38** |

四個寬度都有的：麵包屑、**20 個學生列 `student-view__row`**、上一頁 / 下一頁。

### 水平溢出

**無**（四個寬度皆然）。

### 觸控目標 < 44px（390）

| 元素                        | 390 實測  | 真機        |
| --------------------------- | --------- | ----------- |
| `p-button-outlined`（篩選） | `82 × 41` | `82 × 45` ✓ |
| `breadcrumb__item--link`    | `56 × 21` | 不變        |
| 分頁器上一頁 / 下一頁       | `40 × 40` | `40 × 40`   |

> 這一頁的「篩選」**有**被 coarse 接住（`p-button-outlined` 吃得到 token），
> 而班級視角那顆自刻的 `class-view__filter-button` 沒有 ——
> **兩顆在畫面上做同一件事，一顆在真機上是 45px、一顆是 34px。**

### 鍵盤可達性

- 全頁**沒有正數 `tabindex`**；390 下 Tab 站 22 / 可見互動元素 24
- **`cursor: pointer` 但 Tab 不到的元素：0 個**

### 未驗與原因

| 項目                     | 原因                                    |
| ------------------------ | --------------------------------------- |
| 「篩選」按鈕展開後的面板 | 展開靠動畫，**分頁在背景不跑**（坑 12） |
| 學生列點下去的成績明細   | 這一輪只量了清單本身                    |
| `Escape` / 真的 Tab 鍵   | 需要前景分頁                            |

## 載入中 / 錯誤

- **量測**：390 × 844 ／ 主 checkout 的 dev server（port 4200）`7e9da649` ／
  `admin@demo.clessia.app`（permissions `["*"]`，量測前後各打一次 `/api/me`）
- **手段**：同一個 XHR 包裝 —— 錯誤態把 URL 從 `:8787` 改指到沒人監聽的 `:8799`，
  載入中把 `send` 用 `setTimeout` 延後 3 秒（**只影響那一個 iframe，不動 8787**）。
  方法全文見[方法頁 Phase 2-D](../README.md)
- **證據**：每一輪都確認攔截清單不是空的，**請求數 0 的一律作廢**（例外要自己附正控）
- **導航**：`ng.ɵgetRouterInstance(...)` 拿 Router 本人再 `navigateByUrl()`

### 載入中（3 秒延遲）

| skeleton | spinner | `<main>` 互動元素 | 判定 |
| --- | --- | --- | --- |
| 0 | **2**（`div.student-view__loading 334×82` + `i.pi.pi-spinner 16×16`） | 11 → 39 | ✅ 誠實 |

主體逐字「**載入學生名單...**」。

⚠️ spinner 的 `animation-name` 是 `fa-spin` —— 看得見，不會轉（坑 12）。

### 錯誤（所有 API 都失敗）

| 判定 | 重試鈕 | toast | 攔到的請求數 |
| --- | --- | --- | --- |
| 🔴 **謊稱沒資料，而且把責任推給篩選條件** | 否 | **0** | 2 |

主體逐字「**無符合的學生／請嘗試調整篩選條件或搜尋關鍵字**」。
攔到 `GET /api/schools?isActive=true` 與學生清單兩支。

> 跟 [[specs/sitemap/admin/grades-overview-class]] **是同一個形狀的兩個實例** ——
> 兩個分頁各寫了一份「無符合的 X／請嘗試調整篩選條件」，而兩份都沒有失敗分支。

### 未驗與原因

| 項目 | 原因 |
| --- | --- |
| 重試 | **沒有重試鈕** |
| 其他寬度 | 只量 390 |

---
title: 班級視角（/admin/grades/overview/class）
summary: 成績總覽的班級視角：依課程分組的班級清單，點一班開考試統計對話框。
category: spec
status: developing
tags: [sitemap, admin]
created: 2026-09-12
updated: 2026-09-13
---

# 班級視角

<!-- generated:route-facts start —— 這一段由 tools/sitemap 生成，不要手改 -->

**路由**：`/admin/grades/overview/class`
**角色**：管理員（`admin`）
**選單位置**：**選單不露出**（只能從別頁導過來或直接打網址）
**額外權限**：無

<!-- generated:route-facts end -->

**進入方式**：[[specs/sitemap/admin/grades-overview]] 的「班級視角」卡片 / 直接網址

**外框**見 [[specs/sitemap/_shared/shell-layout]]。

## 畫面區塊

- 麵包屑：`成績總覽 › 班級視角`
- 篩選列：分校（`文山旗艦校`）、`全部年級`、`全部科目`、搜尋框 `搜尋課程或班級...`，
  以及一顆**條件式**的 `待登錄 N` 籌碼
- 班級清單：**依課程分組**（例如「國中數學 · 數學 · 國一～國三」底下掛班級），
  每個班一列：班名、年級範圍、`上限 N 人`，**有待登錄時多一個 `待登錄 N` 徽章**

**沒有分頁器**（實測 6 個班全部列出）。

## 互動元素

| 元素（畫面上的字）         | 類型    | 出現條件                                                         | 按了之後                                   |
| -------------------------- | ------- | ---------------------------------------------------------------- | ------------------------------------------ |
| 成績總覽（麵包屑）         | 連結    | 永遠                                                             | 回 [[specs/sitemap/admin/grades-overview]] |
| 全部年級 / 全部科目        | 下拉 ×2 | 永遠                                                             | 篩選（**未展開選項**）                     |
| 搜尋課程或班級…            | 搜尋框  | 永遠（另有一顆手機版不可見）                                     | 過濾清單                                   |
| 待登錄 N（頁首籌碼）       | 按鈕    | **有待登錄的成績時**（桌機這一顆可見，另有一顆手機版不可見）     | **未驗**                                   |
| 班級列（整列）             | 按鈕    | 每個班一列                                                       | 開對話框 →「子頁面：班級考試統計」         |
| 待登錄 N（班級列上的徽章） | 按鈕    | **只在該班有待登錄成績時**（實測 6 個班只有「國二自然 A 班」有） | **未驗**                                   |
| 篩選                       | 按鈕    | **只有手機版**                                                   | —                                          |

## 子頁面：班級考試統計

**開啟方式**：點任何一個班級列。**只有這一頁開得到**（`grep -rl ClassScoresDialogComponent`
只命中 class-view 自己），所以不進 `_shared/`。

- 標頭：班名、年級範圍、`上限 N 人`
- 第一層 toggle：`待登錄` / `全部`
- 考試下拉：選一場考試（實測 `第一次數學小考 (2026-08-26)`）
- 統計區：`平均分數` / `最高分` / `最低分`（實測 67.25 / 91 / 40）
- 第二層 toggle：`全部` / `已登錄` / `缺考` / `補考`
- 結束動作：`關閉`

**唯讀** —— 對話框內沒有寫入動作（要登錄成績走
[[specs/sitemap/admin/grades-exams-type-id-scores]]）。

## 狀態

| 狀態               | 畫面                                |
| ------------------ | ----------------------------------- |
| 載入中 / 空 / 錯誤 | **未驗**                            |
| 無權限             | 不適用 —— 這條路由沒有 `permission` |

## 驗證紀錄

- **日期**：2026-09-12 ／ 帳號 `admin@demo.clessia.app` ／ 前端 `897d691f`（port 4201）
- **資料狀態**：文山旗艦校 6 個班，其中 1 個班（國二自然 A 班）有 1 筆待登錄

### 兩向比對

|                                            | 數量   |
| ------------------------------------------ | ------ |
| `<main>` 內 DOM 互動元素                   | 16     |
| 不可見（手機版搜尋框、篩選鈕、待登錄籌碼） | 3      |
| **可見**                                   | **13** |

13 = 麵包屑 1 + 下拉 2 + 搜尋 1 + 另一個輸入 1 + 待登錄籌碼 1 + 班級列 6 + 班級列徽章 1。

| 方向        | 結果                      |
| ----------- | ------------------------- |
| 地圖 ⊆ 畫面 | 表列元素都找得到          |
| 畫面 ⊆ 地圖 | 13 個可見元素都有對應條目 |

**差異：0 筆。**

對話框內可見互動元素 **8** 個（關閉 + 2 toggle + 考試下拉 + 4 狀態 toggle），地圖列 8 項，差異 0 筆。

### 未驗到的

| 項目                                   | 原因                                                                                                                      |
| -------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- |
| 頁首與班級列上的「待登錄 N」按下的效果 | 兩顆都沒按                                                                                                                |
| 兩個下拉的選項、對話框裡的考試下拉     | 未展開                                                                                                                    |
| 分校那一格是下拉還是純文字             | **沒有確認** —— 文字層看得到「文山旗艦校」，但 dropdown trigger 只數到 2 個（年級、科目），**兩者對不起來**，下一輪要釐清 |
| 載入中 / 空 / 錯誤                     | 沒有製造手段                                                                                                              |

## 390px

- **量測**：390 × 844 / 768 × 1024 / 1024 × 768 / 1504 × 752 ／ 前端 `3f2197f8`（4200，主 checkout ＝ `origin/main`）／ 身分 `admin@demo.clessia.app`（量測前後各打一次 `GET /api/me`）
- **手段**：同源 iframe 當 viewport；`< 44px` 的「真機」欄是**把 `(pointer: coarse)` 區塊注進去重量一次**得到的，不是推測（方法頁 Phase 2）
- **外框**（頂列、側欄 ↔ 底欄）不屬於這一頁，見 [[specs/sitemap/_shared/shell-layout]]

### 版面怎麼變：篩選列在 ≤768 收成一顆按鈕

| 東西                                | 390         | 768         | 1024         | 1504     |
| ----------------------------------- | ----------- | ----------- | ------------ | -------- |
| `class-view__filter-button`（篩選） | **在**      | **在**      | 不存在       | 不存在   |
| 分校 / 年級 / 科目三個下拉          | 不存在      | 不存在      | **在**       | **在**   |
| `class-view__mobile-main`           | **在**      | **在**      | 不存在       | 不存在   |
| `class-view__toolbar`               | `column`    | `column`    | **`row`**    | `row`    |
| `class-view__course-header`         | `flex wrap` | `flex wrap` | **`nowrap`** | `nowrap` |

**跟 [[specs/sitemap/admin/grades-exams]] 同一個形狀**：窄寬度換上一顆「篩選」，
桌機才把控制項攤在工具列上。

### 差集

| 簽名                                                        | 390   | 768   | 1024      | 1504   |
| ----------------------------------------------------------- | ----- | ----- | --------- | ------ |
| `button.class-view__filter-button`                          | **1** | **1** | 0         | 0      |
| `span.p-select-label` / `span.p-placeholder.p-select-label` | 0     | 0     | **1 / 1** | 1 / 1  |
| `div.p-select-dropdown`                                     | 0     | 0     | **2**     | **2**  |
| 合計                                                        | 12    | 12    | **15**    | **15** |

四個寬度都有的：麵包屑、搜尋框、7 個班級列 `class-view__class-row`、
`class-view__class-todo` ×1、`class-view__todo-chip` ×1。

### 水平溢出

**無**（四個寬度皆然）。

### 觸控目標 < 44px（390）

**被 coarse 抬起來的**：

| 元素                         | 390 實測   | 真機            |
| ---------------------------- | ---------- | --------------- |
| `class-view__todo-chip`      | `116 × 40` | `116 × 44` ✓    |
| **`class-view__class-todo`** | `56 × 18`  | **`56 × 44`** ✓ |
| 搜尋框                       | `252 × 38` | `252 × 42`      |

**沒被抬起來的**：

| 元素                                    | 390 實測  | 真機          |
| --------------------------------------- | --------- | ------------- |
| **`class-view__filter-button`（篩選）** | `74 × 34` | **`74 × 34`** |
| `breadcrumb__item--link`                | `56 × 21` | 不變          |

> ⚠️ **沒被抬起來的那顆正好是手機專用的那顆。**
> `filter-button` 只在 ≤768 存在 —— 也就是**只有手指會碰到它**，
> 而它是這一頁窄寬度下唯一的篩選入口。記現況，不在這裡判定是不是缺陷。

### 鍵盤可達性

- 全頁**沒有正數 `tabindex`**；390 下 **Tab 站 12 個 ＝ 可見互動元素 12 個**
- **`cursor: pointer` 但 Tab 不到的元素：0 個**

### 未驗與原因

| 項目                     | 原因                                                                                  |
| ------------------------ | ------------------------------------------------------------------------------------- |
| 「篩選」按鈕展開後的面板 | 展開靠動畫，**分頁在背景不跑**（坑 12）；面板可能 `appendTo="body"`，不在 `<main>` 內 |
| 班級列點下去的成績對話框 | 對話框內容不在 `<main>` 裡，要另外量                                                  |
| `Escape` / 真的 Tab 鍵   | 需要前景分頁                                                                          |

## 載入中 / 錯誤

- **量測**：390 × 844 ／ 主 checkout 的 dev server（port 4200）`7e9da649` ／
  `admin@demo.clessia.app`（permissions `["*"]`，量測前後各打一次 `/api/me`）
- **手段**：同一個 XHR 包裝 —— 錯誤態把 URL 從 `:8787` 改指到沒人監聽的 `:8799`，
  載入中把 `send` 用 `setTimeout` 延後 3 秒（**只影響那一個 iframe，不動 8787**）。
  方法全文見[方法頁 Phase 2-D](../README.md)
- **證據**：每一輪都確認攔截清單不是空的，**請求數 0 的一律作廢**（例外要自己附正控）
- **導航**：`ng.ɵgetRouterInstance(...)` 拿 Router 本人再 `navigateByUrl()`

### 載入中（3 秒延遲）

| skeleton | spinner | `<main>` 互動元素 | 判定 |
| --- | --- | --- | --- |
| 0 | **2**（`div.class-view__loading 334×82` + `i.pi.pi-spinner 16×16`） | 11 → 18 | ✅ 誠實 |

主體逐字「**載入課程列表...**」。篩選列與「待登錄 0」在延遲期間就已經在了
（**那個 `0` 在資料回來前是假的**，但它旁邊就是 spinner 與「載入課程列表...」，
所以讀起來不像結論）。

⚠️ `i.pi.pi-spinner` 的 `animation-name` 是 `fa-spin` —— **看得見，但不會轉**（坑 12）。

### 錯誤（所有 API 都失敗）

| 判定 | 重試鈕 | toast | 攔到的請求數 |
| --- | --- | --- | --- |
| 🔴 **謊稱沒資料，而且把責任推給篩選條件** | 否 | **0** | 1 |

主體逐字「**無符合的課程／請嘗試調整篩選條件**」，上方「待登錄 **0**」。

> **這比單純的空狀態更糟**：一般的空狀態只是說「沒有」，這一句**指示使用者去做一件
> 不會有用的事**（調整篩選）。而本機實際有 5 個以上的課程群組（見「載入中」那一列的
> 載入後畫面）—— 所以那句話兩層都是假的。

### 未驗與原因

| 項目 | 原因 |
| --- | --- |
| 重試 | **沒有重試鈕** |
| 其他寬度 | 錯誤／載入是狀態不是版面，只量 390 |

---
title: 聯絡簿（/admin/contact-book）
summary: /admin/contact-book 的實際 UI 地圖：缺漏名單、三個篩選、聯絡簿列表與編輯對話框；預設區間下整頁是空的。
category: spec
status: developing
tags: [sitemap, admin]
created: 2026-09-12
updated: 2026-09-13
---

# 聯絡簿

<!-- generated:route-facts start —— 這一段由 tools/sitemap 生成，不要手改 -->

**路由**：`/admin/contact-book`
**角色**：管理員（`admin`）
**選單位置**：學務管理 › 聯絡簿
**額外權限**：無

<!-- generated:route-facts end -->

**進入方式**：選單「學務管理 › 聯絡簿」／直接網址。

**外框**見 [[specs/sitemap/_shared/shell-layout]]。

## ⚠️ 先看這一條：預設區間下整頁是空的

日期區間**預設是最近 7 天**（實測 `2026-09-06 - 2026-09-12`），而展示資料的 6 則聯絡簿
都在 `2026-08-28 ~ 08-29` —— **預設畫面兩個區塊都是空狀態**。

> 照方法頁的「第 0 步」：**空資料的兩向比對會在錯的數字上收斂，而且是 0 差異。**
> 本頁的取樣**把區間手動拉到 `2026-08-25 - 2026-08-31` 之後才做**。
> 空狀態下可見互動元素是 **6**，有資料時是 **12**。

## 畫面區塊

### 1. 頁首

`聯絡簿`（h1）+ 副標「看老師寫了什麼、家長簽了沒。**撰寫在老師端** —— 這裡可以修正已經寫好的內容。」

### 2. 「還沒寫的」缺漏名單

自己一個日期輸入（`缺漏名單的日期`，預設今天），**跟下方列表的區間是兩個獨立的篩選**。

| 狀態          | 畫面                                                                                                               |
| ------------- | ------------------------------------------------------------------------------------------------------------------ |
| 沒有缺漏      | `這天該寫的都寫了。`（**實測到的**）                                                                               |
| 有缺漏        | 每筆一列 + `補寫` 按鈕；超過門檻時另有展開／收合鈕（`hiddenMissingCount() > 0 \|\| missingExpanded()`）—— **未驗** |
| 載入中 / 失敗 | 骨架列 ／ `重試` —— **未驗**                                                                                       |

### 3. 篩選列

| 控制項             | 預設      | 備註                                |
| ------------------ | --------- | ----------------------------------- |
| 日期區間           | 最近 7 天 | 附 `Choose Date` 日曆鈕             |
| `輸入學生姓名篩選` | 空        | —                                   |
| `只看未簽收`       | 關        | toggle                              |
| `清除篩選`         | —         | **只在 `hasFilters()`**，預設不顯示 |

### 4. 統計句

`這段期間共 N 則， M 則家長還沒簽收。`（**只在 `!loading() && !failed() && summary().total > 0`**）

### 5. 列表

表頭：`日期`｜`學生`｜`內容`｜`最後編輯`｜`簽收`

**每一列是 `<tr role="button" tabindex="0">`** —— 可點也可鍵盤操作，開編輯對話框。
`簽收`格顯示 `已簽收` / `未簽收`。

### 6. 三種空狀態（依條件而異）

| 條件             | 文案                                                                                                |
| ---------------- | --------------------------------------------------------------------------------------------------- |
| `failed()`       | 錯誤態 + `重試`                                                                                     |
| `unsignedOnly()` | 空 + `顯示全部`                                                                                     |
| `hasFilters()`   | 空 + `清除篩選`                                                                                     |
| 其餘             | `這段期間沒有聯絡簿` / 「聯絡簿由帶班老師撰寫，只有開了「使用聯絡簿」的班級才會有」（**實測到的**） |

## 互動元素

| 元素                       | 類型                          | 出現條件                             | 按了之後                        |
| -------------------------- | ----------------------------- | ------------------------------------ | ------------------------------- |
| `缺漏名單的日期`           | 日期                          | 永遠                                 | 重查缺漏名單                    |
| `日期區間` + `Choose Date` | 日期區間                      | 永遠                                 | 重查列表                        |
| `輸入學生姓名篩選`         | 文字                          | 永遠                                 | 重查                            |
| `只看未簽收`               | toggle                        | 永遠                                 | 重查                            |
| `清除篩選`                 | 按鈕                          | **只在 `hasFilters()`**              | `clearFilters()`                |
| `補寫`                     | 按鈕                          | **每筆缺漏一顆**                     | 開編輯對話框（空白）—— **未驗** |
| 展開／收合缺漏             | 按鈕                          | **`hiddenMissingCount() > 0`**       | **未驗**                        |
| `重試`                     | 按鈕                          | **載入失敗時**（缺漏區與列表各一顆） | 重載 —— **未驗**                |
| `顯示全部`                 | 按鈕                          | **空 + 只看未簽收**                  | 關掉 toggle —— **未驗**         |
| 每一列                     | `tr[role=button][tabindex=0]` | 每一筆                               | 開編輯對話框                    |

## 子頁面：聯絡簿編輯對話框

→ [[specs/sitemap/_shared/contact-book-entry-dialog]]（**2 頁共用** —— 這一頁與
`/teacher/schedule` 的 `contact-book-roster`，兩邊傳的 `data` 形狀還不一樣）。

以下是**管理端這一條路徑**實測到的：

- **標題**：學生姓名 + `YYYY-MM-DD · 最後由 X 編輯`
- 簽收狀態徽章（`未簽收` / `已簽收`）
- `內容` 多行輸入（佔位字 `今天的上課狀況…`）
- 說明：「每生每日一則，不分科目。**共同編輯會直接覆寫並記下最後編輯者。**」
- 底部 `取消` ／ `儲存`（**剛開啟時 disabled**，內容未改動）

## 驗證紀錄

- **狀態**：已驗（有資料的區間）
- **前端版本**：`650d257a`，自架 `:4210`
- **取樣器**：方法頁版本 + `[tabindex]:not([tabindex="-1"])`
  （這一頁的列同時帶 `role="button"`，既有選擇器抓得到；加了那段是為了跟同批次其他頁一致）
- **兩向比對**（區間 `2026-08-25 - 2026-08-31`）：可見 **12**
  = 2（兩個日期輸入）+ 2（兩顆 `Choose Date`）+ 1（姓名篩選）+ 1（只看未簽收）+ 6（列）。**0 差異。**
- **未驗，附原因**：
  - **缺漏名單的有資料狀態** —— 展示資料在「今天」沒有缺漏（`這天該寫的都寫了。`），
    連帶 `補寫`、展開／收合都摸不到
  - 三種條件式空狀態中的兩種（`failed()`、`unsignedOnly()`）與兩顆 `重試`
  - `儲存` —— **會寫入**
- **方法上的一則**：這一頁是本批次唯一**預設畫面就是空的**那一支。
  如果照預設區間就開始畫，會得到一份「6 個互動元素、兩個空狀態」的地圖，
  **而兩向比對照樣 0 差異** —— 那正是方法頁第 0 步在講的事。

## 390px

- **量測**：390 × 844 / 768 × 1024 / 1024 × 768 / 1504 × 752 ／ 前端 `3f2197f8`（4200，主 checkout ＝ `origin/main`）／ 身分 `admin@demo.clessia.app`（量測前後各打一次 `GET /api/me`）
- **手段**：同源 iframe 當 viewport；`< 44px` 的「真機」欄是**把 `(pointer: coarse)` 區塊注進去重量一次**得到的，不是推測（方法頁 Phase 2）
- **外框**（頂列、側欄 ↔ 底欄）不屬於這一頁，見 [[specs/sitemap/_shared/shell-layout]]

### 版面怎麼變

**沒有變。** 四個寬度掃描全部可見容器的版面訊號，**沒有任何一個容器改變**。

⚠️ **但這句話的強度有限，因為量測當下清單是空的** —— 見下。

### 差集

**零差異。** 四個寬度都是同樣 6 個：學生自動完成輸入框、
兩組日期輸入 + 日曆鍵（起訖）、`filter-chip`（只看未簽收）。

### ⚠️ 這一頁量到的是空狀態，不是清單

畫面上是：

> 還沒寫的 → 「這天該寫的都寫了。」
> 清單 → 「這段期間沒有聯絡簿」

**查過 DB，不是抽樣**（坑 11）：`contact_book_entries` **全庫 6 筆，
最新的是 2026-08-29**（另 2 筆在 08-28），而**今天 2026-09-13 是 0 筆**。
預設期間內沒有任何一筆，所以：

- **列的版面（收不收合、有沒有展開鍵）四個寬度都量不到**
- 「還沒寫的」缺漏名單也是空的（今天沒有課，見 [[specs/sitemap/admin/dashboard]] 的同一節）

**「四個寬度零差異」在這一頁只涵蓋篩選列那 6 個元素**，不涵蓋清單本身。

### 水平溢出

**無**（四個寬度皆然，空狀態下）。

### 觸控目標 < 44px（390）

| 元素                                 | 390 實測                | 真機                     |
| ------------------------------------ | ----------------------- | ------------------------ |
| `input.p-autocomplete-input`（學生） | `318 × 38`              | **`318 × 38`（沒被抬）** |
| `input.p-datepicker-input` ×2        | `208 × 38` / `212 × 38` | `× 42` / `× 44` ✓        |
| `button.p-datepicker-dropdown` ×2    | `40 × 38`               | `44 × 42` / `44 × 44` ✓  |

`filter-chip`（只看未簽收）不在清單裡，代表它已經 ≥ 44px。

> 自動完成輸入框沒被抬起來 —— 跟 [[specs/sitemap/admin/notifications]] 的標題框同一個成因族：
> `styles.scss` 的 coarse 區塊只列了 `--p-inputtext-*` 等既有 token，
> 而它自己的註解就寫著「幽靈 token 不會有任何錯誤訊號，只會靜靜地什麼都不做」。

### 鍵盤可達性

- 全頁**沒有正數 `tabindex`**；390 下 **Tab 站 6 個 ＝ 可見互動元素 6 個**
- **`cursor: pointer` 但 Tab 不到的元素：0 個**

### 未驗與原因

| 項目                                   | 原因                                                                                                      |
| -------------------------------------- | --------------------------------------------------------------------------------------------------------- |
| **整份清單的列在窄寬度的版面**         | 預設期間內 0 筆（查過 DB：全庫 6 筆、最新 2026-08-29）。**要補驗得把日期改到 08-28 ～ 08-29**，這一輪沒做 |
| 缺漏名單有資料時的 `補寫` 與展開／收合 | 今天沒有課，缺漏名單是空的                                                                                |
| `Escape` / 真的 Tab 鍵                 | 需要前景分頁                                                                                              |

## 載入中 / 錯誤

- **量測**：390 × 844 ／ 主 checkout 的 dev server（port 4200）`543e5eda` ／ `admin@demo.clessia.app`
- **手段**：把 XHR 的 URL 從 `:8787` 改指到沒人監聽的 `:8799`（等同 server 掛掉，
  **只影響那一個 iframe**，不動共享資源）。方法全文見[方法頁 Phase 2-D](../README.md)
- **證據**：每一輪都確認攔截清單不是空的（`請求數`），**請求數 0 的那幾輪一律作廢**
- **載入中的量測**：390 × 844 ／ 主 checkout 的 dev server（port 4200）`7e9da649` ／
  `admin@demo.clessia.app`（permissions `["*"]`，量測前後各打一次 `/api/me`）
- **手段（載入中）**：同一個 XHR 包裝，把 `send` 用 `setTimeout` 延後 3 秒。
  ⚠️ **背景分頁的 `setTimeout` 被節流**，實際延遲 ≥ 3 秒（見[方法頁](../README.md)）
- **導航**：`ng.ɵgetRouterInstance(...)` 拿 Router 本人再 `navigateByUrl()`（見[方法頁](../README.md)）

### 載入中（3 秒延遲）

| skeleton | spinner | `<main>` 互動元素 | 判定 |
| --- | --- | --- | --- |
| **10**（`div.skeleton-list 284×120`） | 0 | 6 | ✅ 誠實 |

「還沒寫的」與聯絡簿清單**兩區各有骨架**。

> ⚠️ 本機預設區間內沒有聯絡簿，所以載入後主體仍是空狀態（坑 8）。
> **要看到有資料的樣子，把區間挪到 `2026-08-25 ~ 2026-09-01`**（`contact_book_entries`
> 全庫 6 筆，`entry_date` 落在 08-28 / 08-29）。

⚠️ 骨架的 `animation-name` 是 `skeleton-wave`、`opacity` 是 `1` —— **看得見，只是波紋不會動**（坑 12）。

### 錯誤（所有 API 都失敗）

| 判定 | 重試鈕 | toast | 攔到的請求數 |
| --- | --- | --- | --- |
| ✅ 誠實 | **有** | 0 | 2 |

空狀態說載入失敗＋重試。

### 未驗與原因

| 項目 | 原因 |
| --- | --- |
| **`Escape` / 真滑鼠重試** | 需要前景分頁（方法頁坑 12）。**「重試鈕按了會不會真的重打」沒有驗** |
| 其他寬度 | 錯誤態與寬度無關（是狀態不是版面），只量 390 |

---
title: 人員管理（/admin/staff）
summary: /admin/staff 的實際 UI 地圖：管理員與老師帳號清單、四個篩選、每列五項動作，四支對話框（其中一支關不掉）。
category: spec
status: developing
tags: [sitemap, admin]
created: 2026-09-12
updated: 2026-09-12
---

# 人員管理

<!-- generated:route-facts start —— 這一段由 tools/sitemap 生成，不要手改 -->

**路由**：`/admin/staff`
**角色**：管理員（`admin`）
**選單位置**：人事管理 › 人員管理
**額外權限**：無

<!-- generated:route-facts end -->

**進入方式**：選單「人事管理 › 人員管理」/ 直接網址

**外框**見 [[specs/sitemap/_shared/shell-layout]]。

> ⚠️ **這條路由沒有 `permission`** —— 任何 admin 都進得來。
> 細部權限（`manage_staff` 等）是**表單裡可以勾的東西**，不是進這一頁的門檻。

## 畫面區塊

### 1. 頁首

`人員管理` + 副標「管理管理員、老師帳號」。右側「操作紀錄」與主要行動「新增人員」。

### 2. 工具列

搜尋框（`搜尋姓名或 Email...`）+ 四個可清除的下拉：

| 下拉     | 選項                           |
| -------- | ------------------------------ |
| 角色篩選 | `ROLE_OPTIONS`（管理員／老師） |
| 分校篩選 | 分校清單                       |
| 科目篩選 | 科目清單                       |
| 全部狀態 | `啟用中` / `已停用` / `已封存` |

### 3. 統計列（三格，有資料才出現）

`N 位人員`、`N 管理員 ・ N 老師`、`N 啟用中`。

> **管理員與老師刻意合成同一格**，而不是兩塊獨立的磚。模板註解說明理由：
> 一個人可以同時是管理員又是老師，兩邊各算一次，**加起來本來就會比總人數多** ——
> Tester 抓到 `13+89≠101` 讓人以為系統算錯。同一格再加一句「（N 位身兼兩者）」。
> **本機 `13 + 88 = 101`，兼任 0 人，所以那句話不出現**（條件式元素）。

### 4. 表格

`姓名`（頭像字 + 顯示名 + Email 兩行）、`角色`（chip，可多個）、`教學科目`、`服務分校`、
`電話`、`狀態`、每列 ⋮。每頁 20 筆。

- 頭像底色由 `getPersonHue(staff.id)` 決定；管理員多一個 `--admin` 樣式
- `教學科目`只在**該人是老師且有科目**時列出，超出的收成 `+N`（tooltip 是完整清單）；
  否則顯示 `—`
- 電話沒填顯示 `未填寫`，有填是 `tel:` 連結
- 狀態：`啟用中` / `已停用` / `已封存`

資料來源：`GET /api/staff`。

## 互動元素

| 元素（畫面上的字）   | 類型           | 出現條件                             | 按了之後                                                     |
| -------------------- | -------------- | ------------------------------------ | ------------------------------------------------------------ |
| 操作紀錄             | 按鈕           | 永遠                                 | 開 [[specs/sitemap/_shared/audit-log-dialog]]（`['staff']`） |
| 新增人員             | 按鈕           | 永遠                                 | 開子頁面「人員表單」                                         |
| 搜尋姓名或 Email     | 文字輸入       | 永遠                                 | 重新取數                                                     |
| 四個篩選下拉         | 下拉（可清除） | 永遠                                 | 重新取數                                                     |
| 清除篩選             | 文字按鈕       | **只在「找不到符合的人員」空狀態裡** | 清掉全部篩選                                                 |
| 新增人員（空狀態裡） | 按鈕           | **只在「尚未建立人員」空狀態裡**     | 同上面那顆                                                   |
| 電話                 | 連結           | 該列有填電話                         | `tel:` 撥號                                                  |
| `+N`（科目）         | 文字           | 科目超過可顯示數                     | 只有 tooltip，不可按                                         |
| 每列 ⋮               | 按鈕           | 每一列                               | 開該列動作選單（見下）                                       |
| 分頁控制             | 按鈕組         | 總數 > 20                            | 換頁                                                         |

> **`disabled`**：**頁面本體沒有任何 `[disabled]` 繫結**；全部集中在人員表單裡（見下）。

### 每列 ⋮ 的動作選單

`PopupMenuComponent`。**項目依該人的 `status` 而異**：

| 項目         | 出現條件                | 行為                                                  |
| ------------ | ----------------------- | ----------------------------------------------------- |
| 編輯         | 永遠                    | 開子頁面「人員表單」（編輯模式）                      |
| 授課紀錄     | 永遠                    | 開子頁面「授課紀錄」                                  |
| 產生登入連結 | 永遠                    | **直接寫入**（`POST /api/login-links`）→ **本輪未按** |
| 重新啟用     | `status === 'inactive'` | 開確認（**未驗** —— 本機 101 人全部啟用中）           |
| 停用帳號     | `status === 'active'`   | 開確認（實測）                                        |
| 封存帳號     | `status !== 'archived'` | 開確認（實測）                                        |

> **「重新啟用」與「停用帳號」呼叫的是同一個 `confirmDeactivate()`** ——
> 看起來像複製貼上的錯，其實那個函式**自己依 `status` 分岔**（`inactive` → 走啟用那一支）。
> 讀原始碼時差一點記成缺陷，**進去看第二層才確定沒事**。

兩個確認都走 [[specs/sitemap/_shared/confirm-dialog]]：

| 動作     | 標頭       | 內文（實測）                                                                                            | 主鍵             |
| -------- | ---------- | ------------------------------------------------------------------------------------------------------- | ---------------- |
| 停用帳號 | `確認停用` | 確定要停用「X」嗎？停用後帳號將暫時無法使用，但角色與課堂指派會保留。                                   | `停用`（warn）   |
| 封存帳號 | `確認封存` | 確定要封存「X」嗎？封存後無法取消，帳號將永久停用且無法登入，未來課堂指派將自動解除，但歷史紀錄會保留。 | `封存`（danger） |

## 子頁面：人員表單

**開啟方式**：「新增人員」，或列 ⋮ ›「編輯」。`width: 600px`，inline 標頭 + ✕。

| 欄位            | 型別                   | 備註                                                                |
| --------------- | ---------------------- | ------------------------------------------------------------------- |
| `姓名*`         | 文字                   | placeholder `真實姓名`                                              |
| `Email*`        | 文字                   | placeholder `用來產生登入連結的 Email`；**編輯時 disabled**（實測） |
| `電話`          | 文字                   | `手機或市話`                                                        |
| `生日`          | datepicker             | `選擇生日`                                                          |
| `角色*`         | 兩個 checkbox          | `管理員`（`role_admin`）／`老師`（`role_teacher`）                  |
| `服務分校*`     | 多選                   | `選擇服務分校`                                                      |
| **`教學科目*`** | 多選 +「管理科目」按鈕 | **只在勾了「老師」時出現**（實測）                                  |
| **`管理權限`**  | 8 個 checkbox          | **只在勾了「管理員」時出現**（實測）                                |
| `備註`          | textarea               | `其他備註資訊...`                                                   |

結束動作：`取消`、`建立`／`儲存`。

### 管理權限的 8 個選項（實測全文）

| 勾選項   | 說明                                 | id                      |
| -------- | ------------------------------------ | ----------------------- |
| 日常行政 | 查詢與處理報名、出勤、請假           | `perm_basic_operations` |
| 課程管理 | 課程與排課管理                       | `perm_manage_courses`   |
| 學生管理 | 學生與家長資料管理                   | `perm_manage_students`  |
| 財務管理 | 財務與收費管理                       | `perm_manage_finance`   |
| 帳號管理 | 系統帳號與權限管理                   | `perm_manage_staff`     |
| 角色管理 | 指派或變更帳號角色與權限             | `perm_manage_roles`     |
| 報表查看 | 查看營收與統計報表                   | `perm_view_reports`     |
| 跨分校   | 不受指派分校限制，看得到全機構的資料 | `perm_all_campuses`     |

> **這 8 個就是 `user_roles.permissions` 的來源** —— `manage_finance` 與 `view_reports`
> 正是 [[specs/sitemap/admin/fee-templates]] 與 [[specs/sitemap/admin/reports]] 的門檻。

### 再疊一層：管理科目

「教學科目」右上的「管理科目」→ **疊第二層對話框**（`.p-dialog` 兩個），
內容是 [[specs/sitemap/_shared/subject-manager]] 的對話框形態。關掉只收上面那一層（實測）。

## 子頁面：授課紀錄

**開啟方式**：列 ⋮ ›「授課紀錄」。`width: 760px`，**用 PrimeNG 自己的標頭**
（`授課紀錄 · <姓名>`），不是自畫的。

內容：一個月份選擇器 + 兩個數字（`N 小時` / `N 堂`），下方依資料多寡分區
（正常課堂、代課、被代課「不計入時數」）。實測本機第一位是
`2026 年 9 月 / 0 小時 / 0 堂 / 這個月沒有課堂紀錄。`

底部有一顆 `關閉`（#727 修正後；**在所有資料分支外面**，載入中／載不出來時照樣按得到）。

## 狀態

| 狀態         | 畫面                                                                                        |
| ------------ | ------------------------------------------------------------------------------------------- |
| 載入中       | 五根 skeleton bar                                                                           |
| 空（有篩選） | `找不到符合的人員` /「沒有找到符合篩選條件的人員，請嘗試其他條件」+「清除篩選」（**未驗**） |
| 空（無篩選） | `尚未建立人員` /「開始建立管理員或老師帳號」+「新增人員」（**未驗**）                       |
| 錯誤         | **未驗**                                                                                    |
| 無權限       | 不適用 —— 這條路由沒有 `permission`                                                         |

## 已知的落差（記錄現況，不在這裡修）

### ~~「授課紀錄」對話框沒有任何關閉控制項~~ —— 已修（#727）

> **2026-09-12 更新**：已修，底部補了一顆 `關閉`。
> 保留原始描述是因為**它解釋了為什麼那顆按鈕要放在所有資料分支外面** ——
> 沒有這一段，下一個人會覺得把它收進 `@if` 裡比較整齊。

**修正前**：`document.querySelector('.p-dialog').querySelectorAll('button').length === **0**` ——
**整個對話框一顆按鈕都沒有**（月份選擇器是 `p-select`）。PrimeNG 標頭的
`.p-dialog-header-actions` 是空的（`<!--container--><!--container-->`），**沒有 ✕**。

`dialogService.open(TeachingLogDialogComponent, …)` 沒有傳 `closable`，
模板底部也沒有「關閉」按鈕 —— 全站其他對話框都有 `取消` / `關閉` / 自畫的 ✕，**只有這一支沒有**。

#### 為什麼標頭的 ✕ 不渲染（機制，#714 查到的）

`DynamicDialogComponent` 一律把 `[closable]="ddconfig.closable"` 綁給內層 `p-dialog`，
而 `p-dialog` 的模板是 `*ngIf="closable"`、**自己的預設是 `true`**。
開啟設定**沒帶 `closable` 就是 `undefined`，把那個預設蓋掉** —— 所以沒帶的一律沒有 ✕。

（**注意**：這不蘊含「帶了 `closable: true` 也不會有 ✕」。那句話曾經被寫進 #714 的工單，
但依據的兩半證據都不成立，見該 issue 的更正留言。）

**還沒能證明的那一半**：`Escape` 與點遮罩本輪都關不掉它，**但這一輪真鍵盤/真滑鼠事件
整片送不進頁面**（`__keys` / `__hits` 全空，換新分頁重試也一樣，見
[[specs/sitemap/README|方法頁]]坑 6），所以那兩條路徑**不算驗過**。
合成的 `Escape`（三個目標都試過、監聽器確認收到）與合成的遮罩點擊都沒有反應。

**修法**：注入 `DynamicDialogRef` + 內容區補一顆 `關閉`（照 `parent-detail` / `invoice-detail`
的形狀），**不是去開 `closable`** —— 這個 app 其餘對話框都是內容區關閉鈕，
而動全域預設會碰到十幾個開啟點。

→ issue #727 已關。**「還沒能證明的那一半」（真實 Escape、真實點遮罩）仍然沒有驗過** ——
補了關閉鈕之後它的嚴重度降到「順手」，但如果有人手上有可用的真滑鼠／真鍵盤，
那兩條路徑值得補一次。

## 驗證紀錄

- **日期**：2026-09-12 ／ **帳號**：`admin@demo.clessia.app`
- **前端版本**：port 4202（本 worktree），`8c286802` ／ **寬度**：1504 CSS px
- **資料狀態**：101 位人員（13 管理員・88 老師、兼任 0、101 啟用中），
  `staff` 操作紀錄 6 筆（`人員林行政` / `人員王行政`，動作 `新增` / `編輯`）

### 兩向比對

| 方向        | 結果                                                                                     |
| ----------- | ---------------------------------------------------------------------------------------- |
| 地圖 ⊆ 畫面 | 這個資料狀態下該出現的元素全部找得到；**「（N 位身兼兩者）」如地圖所述不出現**（兼任 0） |
| 畫面 ⊆ 地圖 | 40 個可見元素 = 操作紀錄 + 新增人員 + 搜尋 + 4 個下拉 × 2 節點 + 20 個列 ⋮ + 分頁 9      |

**差異：0 筆。** DOM 41、可見 40 —— 少的是 `page-actions__dock` 的手機版「新增人員」。

### 逐項實地驗證

| 動作                     | 結果                                                                            |
| ------------------------ | ------------------------------------------------------------------------------- |
| 「操作紀錄」按一次       | **有資料**（6 筆），`對象` 欄是 `人員林行政` 這種帶名字的形式 ✓                 |
| 「新增人員」按一次       | 表單如上；**勾「管理員」→ 管理權限 8 項出現**；**再勾「老師」→ 教學科目出現** ✓ |
| 表單裡「管理科目」按一次 | 疊第二層（科目管理對話框），關掉只收上層 ✓                                      |
| 列 ⋮ 按一次              | `編輯 / 授課紀錄 / 產生登入連結 / 停用帳號 / 封存帳號` ✓                        |
| 列 ⋮ ›「編輯」           | `編輯人員`，欄位帶值，**Email 是 disabled** ✓，主鍵文字變 `儲存` ✓              |
| 列 ⋮ ›「停用帳號」       | 確認對話框（`確認停用`）✓，`取消` 關閉 ✓                                        |
| 列 ⋮ ›「封存帳號」       | 確認對話框（`確認封存`）✓，`取消` 關閉 ✓                                        |
| 列 ⋮ ›「授課紀錄」       | 開啟 ✓ —— **然後關不掉**（見上）                                                |

**零寫入。** 「產生登入連結」一次都沒按 —— 它不開對話框、直接 `POST /api/login-links`。

### 未驗到的

| 項目                                   | 原因                                                                 |
| -------------------------------------- | -------------------------------------------------------------------- |
| `已停用` / `已封存` 的人與他們的列選單 | 本機 101 人全部啟用中                                                |
| 「重新啟用」與它的確認                 | 同上                                                                 |
| 「產生登入連結」                       | **寫入**（會真的發一組一次性 token）                                 |
| 表單送出（建立／儲存）                 | 寫入                                                                 |
| 授課紀錄的月份切換與有資料時的分區     | 對話框關不掉，本輪只能靠重新整理脫身                                 |
| 授課紀錄能不能用真實 `Escape` 關掉     | 本輪真鍵盤事件送不進頁面                                             |
| 四個篩選、搜尋、分頁                   | 沒有實地操作（同型別行為已在 [[specs/sitemap/admin/payments]] 驗過） |
| 三種空狀態、錯誤                       | 本機有資料                                                           |
| 手機寬度                               | 只量 1504px                                                          |

---
title: 家長管理（/admin/parents）
summary: /admin/parents 的實際 UI 地圖：家長表格、七項列動作選單（依帳號狀態而異）與五支對話框，含一支開了關不掉的。
category: spec
status: developing
tags: [sitemap, admin]
created: 2026-09-12
updated: 2026-09-13
---

# 家長管理

<!-- generated:route-facts start —— 這一段由 tools/sitemap 生成，不要手改 -->

**路由**：`/admin/parents`
**角色**：管理員（`admin`）
**選單位置**：學務管理 › 家長管理
**額外權限**：無

<!-- generated:route-facts end -->

**權限變體**：任何 admin 角色都進得去，而且**清單沒有被分校窄化**（`routes/parents.ts` 沒有 import campus-scope；是不是該窄化見 [[specs/sitemap/_shared/permission-matrix]]）。

**進入方式**：選單「學務管理 › 家長管理」／直接網址。

**外框**見 [[specs/sitemap/_shared/shell-layout]]。

## 畫面區塊

### 1. 橘帶

大標 `家長管理`、副標「管理家長帳號、關聯學生及帳號狀態」。
右側 `匯入` 與 `新增家長` 兩顆，以及**條件式**錨點 `N 位家長 ／ N 啟用中`
（只在 `!loading() && parents().length > 0`；`・ 停用 M` 那段只在 `inactiveCount() > 0`，**未驗到**）。

### 2. 篩選列

搜尋框 `搜尋姓名、Email、手機...` + 狀態下拉。

狀態下拉選項：`全部狀態`／`啟用`／`停用`／`封存`（**4 項，「全部狀態」是真的選項**
—— 跟 [[specs/sitemap/admin/students]] 的狀態下拉不同，那邊「全部狀態」只是佔位字）。

### 3. 家長表格

表頭：`姓名`｜`手機`｜`Email`｜`關聯學生`｜`狀態`｜（動作，無標題）

- 沒有手機／Email 時顯示 `—`
- `關聯學生`：`parent.studentCount > 0` 時列出姓名
- 列尾 `⋮` 開列動作選單

### 4. 分頁器

**實測資料 16 筆、單頁，所以分頁器不渲染** —— 它是條件式的，不是缺席。

### 資料來源

`ParentsService.list({ search, status, page, pageSize })`，所有取數走同一條
`switchMap` 管線（#661）。

## 互動元素

| 元素（畫面上的字）         | 類型     | 出現條件                     | 按了之後                                                         |
| -------------------------- | -------- | ---------------------------- | ---------------------------------------------------------------- |
| `匯入`                     | 按鈕     | 永遠                         | 開「批次匯入家長」對話框                                         |
| `新增家長`                 | 按鈕     | 永遠                         | 開家長表單對話框（新增模式）                                     |
| `搜尋姓名、Email、手機...` | 文字輸入 | 永遠                         | 300ms 後重查，回第 1 頁                                          |
| 狀態下拉                   | 下拉     | 永遠                         | 重查，回第 1 頁                                                  |
| `清除篩選`                 | 文字鈕   | **只在空結果且有篩選條件時** | `onSearchChange(''); onStatusChange(null)` —— **這一頁兩個都清** |
| `新增家長`（空狀態內）     | 按鈕     | **只在空結果且無篩選條件時** | 同上                                                             |
| 列尾 `⋮`                   | 圖示鈕   | 每一列                       | 開列動作選單                                                     |

> 對照：[[specs/sitemap/admin/students]] 的 `清除篩選` **不清狀態篩選**，這一頁清。
> 兩頁行為不同，記成現況不調和。

### 列動作選單（`.popup-menu__panel`）

| 項目           | 出現條件             | disabled 條件           | 按了之後                                                                  |
| -------------- | -------------------- | ----------------------- | ------------------------------------------------------------------------- |
| `查看詳情`     | 永遠                 | 永不                    | 開家長詳情對話框（底部有 `關閉`；**#717 之前沒有任何關閉入口**，見 #714） |
| `新增學生`     | 永遠                 | `status === 'archived'` | 開學生表單（**帶 `parentId`，無家長欄**）                                 |
| `編輯`         | 永遠                 | `status === 'archived'` | 開家長表單（編輯模式）                                                    |
| `產生登入連結` | 永遠                 | `status === 'archived'` | **寫入** —— 未按，見驗證紀錄                                              |
| （分隔線）     | 永遠                 | —                       | —                                                                         |
| `停用帳號`     | **只在 `active`**    | —                       | 開確認對話框                                                              |
| `啟用帳號`     | **只在 `inactive`**  | —                       | 開確認對話框（**未驗到**）                                                |
| `封存帳號`     | **`!== 'archived'`** | —                       | 開確認對話框                                                              |

**實測 `active` 列**：查看詳情／新增學生／編輯／產生登入連結／（分隔線）／停用帳號／封存帳號
—— 七項，逐項對上原始碼。`inactive` 與 `archived` 兩種列**未驗到**（見驗證紀錄）。

## 子頁面

| 對話框           | 標題／內容                                                                                         |
| ---------------- | -------------------------------------------------------------------------------------------------- |
| **家長詳情**     | 標題是家長姓名；Email、`關聯學生` 清單（每人一顆 `報名班級`）、`輸入姓名綁定既有學生`，底部 `關閉` |
| **批次匯入家長** | `點擊或拖曳 Excel 檔案至此` / `支援 .xlsx / .xls / .csv` / `下載範本` / `取消`                     |
| **家長表單**     | `新增家長`：`姓名*`、`Email*`、`手機號碼*`、`備註` + `取消`／`建立家長`                            |
| 學生表單         | [[specs/sitemap/_shared/student-form-dialog]]（**帶 `parentId`，家長欄不出現**）                   |
| 確認對話框       | [[specs/sitemap/_shared/confirm-dialog]]，例：「確定要停用「X」嗎？停用後該家長將無法登入系統。」  |
| 登入連結對話框   | **未驗** —— 開啟它就是一次寫入                                                                     |

### 送出鍵：三支表單現在一致了

| 表單     | 送出鍵的 disabled                               |
| -------- | ----------------------------------------------- |
| 家長表單 | `[disabled]="loading()"`                        |
| 學生表單 | `[disabled]="loading()"`（**PR #718 改的**）    |
| 人員表單 | `[disabled]="loading()"`（PR #664，方向的起點） |

方向逐字出自 #664：「**拿掉讓「按建立沒反應」成真的那個 disabled**」，改成欄位級錯誤。

> **這一頁的地圖是那次對齊的觸發點。** 畫地圖時把兩支表單擺在一起，才看得出
> 家長表單合乎新方向而學生表單還是舊的 —— 計畫席據此開 #716，當天由 #718 補齊。
>
> 記在這裡的理由：**下一個人看到三支一致時，會不知道它曾經不一致、也不知道為什麼一致。**

## 狀態

| 狀態        | 畫面                                               |
| ----------- | -------------------------------------------------- |
| 載入中      | 骨架列                                             |
| 空 + 有篩選 | `找不到符合的家長` + `清除篩選`                    |
| 空 + 無篩選 | `尚未有家長資料` + `新增家長`                      |
| 載入失敗    | toast「載入失敗 / 無法載入家長列表」，列表維持原狀 |

## 驗證紀錄

- **狀態**：已驗
- **前端版本**：`650d257a`，自架 `:4210`（`:4200` 服的是主 checkout）
- **兩向比對**：可見互動元素 **20**（總 21，唯一不可見的是手機版 `page-actions__cta`）
  = 1（匯入）+ 1（新增家長）+ 1（搜尋）+ 1（狀態下拉）+ 16（每列一顆 `⋮`）。**0 差異。**
- **未驗，附原因**：
  - `inactive` / `archived` 兩種列的選單 —— 展示資料 16 位**全部 `active`**
    （`summary.inactiveCount === 0 && archivedCount === 0`）。
    **繫結存在但這輪量不到，是「未驗」不是「不會發生」。**
  - `產生登入連結` 與所有確認鍵 —— **會寫入，地圖驗證不做寫入**
  - 兩個空狀態、`・ 停用 M` 錨點片段
- **量測過程中犯的兩個錯，都被斷言抓到**：
  1. 探完「查看詳情」沒關掉就去探「匯入」，第二支讀回來的內容**一字不差** ——
     不是殘留，是 `querySelector('.p-dialog')` 只回**第一個**，而當時有**兩個**疊著。
     改成 `querySelectorAll` 逐個列舉才看清楚。
  2. 「沒有 X」的機制我判斷錯了兩次，**第二次錯在正控本身**。
     先以為是 `openDetailDialog` 漏傳 `closable: true`；接著拿
     `/admin/sessions` 的「查看異動紀錄」當正控，看到它 header 也空，
     就結論成「這個 app 從不渲染 header 的 `×`」。
     **而那支根本也沒傳 `closable`** —— `sessions.page.ts` 那三處 `closable: true`
     是給別支對話框的。**我拿兩個同類互相比較，它們當然一樣。**
     真正的機制由 #717 找出來：**沒帶 `closable` 就是 `undefined`，
     而 `DynamicDialogComponent` 一律把它綁給內層 `p-dialog`，蓋掉 `p-dialog` 自己的預設 `true`。**
     補做真的正控（`SessionAdvancedFiltersDialog`，確實有傳 `closable: true`）——
     **× 有渲染**。
     這是本 repo 記過的兩條同時命中：**「兩邊一樣」可能是比較分不出來**，
     以及**正控要挑真的具備待測屬性的那個位置**。

## 390px

- **量測**：390 × 844 / 768 × 1024 / 1024 × 768 / 1504 × 752 ／ 前端 `3f2197f8`（4200，主 checkout ＝ `origin/main`）／ 身分 `admin@demo.clessia.app`（量測前後各打一次 `GET /api/me`）
- **手段**：同源 iframe 當 viewport；`< 44px` 的「真機」欄是**把 `(pointer: coarse)` 區塊注進去重量一次**得到的，不是推測（方法頁 Phase 2）
- **外框**（頂列、側欄 ↔ 底欄）不屬於這一頁，見 [[specs/sitemap/_shared/shell-layout]]

> **這一頁走的是 admin 清單頁的共同形狀**（見 [[specs/sitemap/admin/sessions]] 的同一節）：
> `responsive-table` 在容器塞不下時把欄位收起來、每列長出一顆展開鍵；
> 主要行動在 390 從頁首移到畫面底部的固定行動列。
> **門檻不是共用的** —— 各頁欄位數與 min-width 合計不同，所以攤開的寬度各不相同。

### 版面怎麼變

| 東西                                   | 390                    | 768      | 1024    | 1504         |
| -------------------------------------- | ---------------------- | -------- | ------- | ------------ |
| 表格欄位                               | **收合**（展開鍵 ×18） | 收合     | 收合    | **不收合**   |
| 「新增家長」                           | **底部固定行動列**     | 頁首按鈕 | 同      | 同           |
| `parents__header` / `parents__toolbar` | `column`               | column   | **row** | row          |
| 狀態欄 `status-dot`                    | 收在展開區內           | 同       | 同      | **列上顯示** |
| 綁定學生標籤 `parent-table__students`  | 收在展開區內           | 同       | 同      | **列上顯示** |

**跟 [[specs/sitemap/admin/students]] 同一個形狀、同一組門檻**（工具列 1024、欄位 1504）。

### 差集

| 簽名                              | 390    | 768    | 1024   | 1504   |
| --------------------------------- | ------ | ------ | ------ | ------ |
| `responsive-table__expand-button` | **18** | **18** | **18** | 0      |
| `p-button`（頁首「新增家長」）    | 0      | **1**  | 1      | 1      |
| `page-actions__cta`（底部）       | **1**  | 0      | 0      | 0      |
| 合計                              | 41     | 41     | 41     | **23** |

四個寬度都有的：搜尋框、狀態篩選下拉、列動作鍵 ×18、`p-button-secondary` ×1。
**這一頁沒有分頁器** —— 18 位家長一頁放得下。

### 水平溢出

**無**（四個寬度皆然）。

### 觸控目標 < 44px（390）

**被 coarse 抬起來的**：展開鍵 ×18 `26×26 → 44×44` ✓、列動作鍵 ×18 `40×40 → 44×44` ✓、
新增家長 `114×41 → 114×45` ✓、狀態篩選 `36 → 42`。

**沒被抬起來的**：`input.clessia-filter-search-input` `332 × 38`（真機不變）。

### 鍵盤可達性

- 全頁**沒有正數 `tabindex`**；390 下 Tab 站 35 / 可見互動元素 41
- **`cursor: pointer` 但 Tab 不到的元素：0 個**

### 未驗與原因

| 項目                                            | 原因                                                                                                                                                  |
| ----------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| 展開區內容在 390 的版面                         | 展開靠動畫，**分頁在背景不跑**（坑 12）。**狀態與綁定學生在窄寬度都住在這裡面**                                                                       |
| `inactive` / `archived` 兩種狀態列在 390 的樣子 | **資料存在了**（查過 DB：active 16、inactive 1、archived 1 —— labor-2 在 Phase 1 記的「沒有這兩種狀態」已經不成立），但這一輪沒有切篩選去把它們叫出來 |
| 各種對話框在 390 的版面                         | 對話框內容不在 `<main>` 裡                                                                                                                            |
| `Escape` / 真的 Tab 鍵                          | 需要前景分頁                                                                                                                                          |

## 載入中 / 錯誤

- **量測**：390 × 844 ／ 主 checkout 的 dev server（port 4200）`543e5eda` ／ `admin@demo.clessia.app`
- **手段**：把 XHR 的 URL 從 `:8787` 改指到沒人監聽的 `:8799`（等同 server 掛掉，
  **只影響那一個 iframe**，不動共享資源）。方法全文見[方法頁 Phase 2-D](../README.md)
- **證據**：每一輪都確認攔截清單不是空的（`請求數`），**請求數 0 的那幾輪一律作廢**
- **載入中的量測**：390 × 844 ／ 主 checkout 的 dev server（port 4200）`7e9da649` ／
  `admin@demo.clessia.app`（permissions `["*"]`，量測前後各打一次 `/api/me`）
- **手段（載入中）**：同一個 XHR 包裝，把 `send` 用 `setTimeout` 延後 3 秒。
  ⚠️ **背景分頁的 `setTimeout` 被節流**，實際延遲 ≥ 3 秒（見[方法頁](../README.md)）
- **導航**：`ng.ɵgetRouterInstance(...)` 拿 Router 本人再 `navigateByUrl()`（見[方法頁](../README.md)）

### 載入中（3 秒延遲）

| skeleton | spinner | `<main>` 互動元素 | 判定 |
| --- | --- | --- | --- |
| **6**（`div.skeleton-list 332×192`） | 0 | 6 → 42 | ✅ 誠實 |

⚠️ 骨架的 `animation-name` 是 `skeleton-wave`、`opacity` 是 `1` —— **看得見，只是波紋不會動**（坑 12）。

### 錯誤（所有 API 都失敗）

| 判定 | 重試鈕 | toast | 攔到的請求數 |
| --- | --- | --- | --- |
| 🔴 謊稱沒資料 | 否 | 1 | 1 |

空狀態說「**尚未有家長資料**／點選「新增家長」來建立第一位家長帳號」。

### 未驗與原因

| 項目 | 原因 |
| --- | --- |
| **`Escape` / 真滑鼠重試** | 需要前景分頁（方法頁坑 12）。**「重試鈕按了會不會真的重打」沒有驗** |
| 其他寬度 | 錯誤態與寬度無關（是狀態不是版面），只量 390 |

## 寫入實按（Phase 2-B 第 5 輪，2026-09-13）

環境與基線見 [[specs/sitemap/admin/students]] 同名一節（同一輪、同一組）。開按前
`parents=29`（啟用 27 · 停用 1 · 封存 1）、`parent_student_relations=51`。

| 鈕 | 端點 | 結果 |
| --- | --- | --- |
| 列選單 `停用帳號` → `停用` | `PATCH /api/parents/{id}/deactivate` | ✅ `status=inactive`；留 `parent/deactivate`（`details` 空 `{}`） |
| 列選單 `啟用帳號` → `啟用` | `PATCH /api/parents/{id}/activate` | ✅ 回 `active`，統計回到 27/1；留 `parent/activate` |
| 列選單 `編輯` → `儲存` | `PUT /api/parents/{id}` | ✅ 電話落地，**寫的是 `ba_user.phone`**（見下）；**成功無 toast**；留 `parent/update`（`details` 空） |
| 列選單 `新增學生` → `建立學生` | `POST /api/students`（帶 `presetParentId`） | ✅ `students` +1、`parent_student_relations` +1（`is_primary=true`）；toast「學生已建立…已關聯至」 |
| 詳情對話框的 `報名班級` | `POST /api/enrollments` | ✅ `enrollments` +1；**成功訊息是對話框內的 inline notice，不是 toast** |
| `匯入` → 上傳 → 檢查 | `POST /api/parents/batch-check` | ✅ 解析 + 逐列狀態（「可匯入」） |
| `匯入` → `確認匯入 N 筆` | `POST /api/parents/batch-import` | ✅ `parents` +1、`students` +1、`psr` +1 |
| 列選單 `產生登入連結` | `POST /api/login-links` | 🔴 **422，對 UI 建立的家長必然失敗**，見下 |
| 列選單 `封存帳號` | `PATCH /api/parents/{id}/archive` | ⛔ **不可逆**（`parents.page.ts:303-309` 的註解明講），本輪不按 |

### 🔴 UI 建立的家長沒有 `parent` 角色，登入連結永遠產不出來

`POST /api/login-links` 回 **422**（toast 只說「產生失敗／請稍後再試」，看不出原因）。
路由的三個 422 出口是 `NO_ROLES` / `NO_EMAIL` / `LINK_FAILED`（`login-links.ts:68/96/102`），
查 DB 分得出來是哪一個：

```
QA-758-R5-匯入家長 | qa-758-r5-import@example.test | (roles 空)
劉俊賢（seed）      | parent07@demo.clessia.app     | parent
```

**`apps/api/src/routes/parents.ts` 全檔沒有任何 `user_roles` 的寫入**
（`grep -rn user_roles apps/api/src/routes/` 只命中 `login-links` / `me` / `sessions` / `staff`）。
seed 家長的 `parent` 角色是 `seed.sql` 直接 INSERT 的 —— **所以「有角色」是 seed 的性質，不是產品的**。

推論範圍：批次匯入與單筆新增走的是同一段建帳號程式碼，**兩條路建出來的家長都沒有角色**。
（本輪只實按了匯入那條；單筆新增未按，但程式碼路徑相同。）

**流程上這是斷的**：匯入完成畫面逐字寫著「請到家長管理頁，對每一位家長使用『產生登入連結』」——
那句指示指向一條對它自己建出來的家長走不通的路。

已開 issue 給計畫席。

### 家長的電話住在 `ba_user`，而那是 c2 的已登記存量債

`parents` 表沒有 `phone` 欄（欄位只有 `id, org_id, name, notes, created_at, updated_at, user_id, status`）。
編輯表單的電話寫進 `ba_user.phone`（`parents.ts:678`），email 寫進 `ba_user.email`（`:674`）。

**這不是新發現的違憲** —— c2 的 gate A15 帶 allowlist，
`constitution-enforcement.md:54` 記「剩 4 筆真債待 billing-api 驗證 API 路徑」。
全 API 的直寫點共 8 處（`parents.ts:436/674/678/1593`、`me.ts:131/169`、`staff.ts:961/1199`）。

> **判準：宣告違憲之前，先查它是不是已登記的債。** 這是「宣告缺陷之前先排除自己」在**規則**上的形式。

### 上一輪列為未驗、本輪驗到的

- **`inactive` 列的選單**：`停用帳號` 換成 `啟用帳號`，其餘五項不變
  （`查看詳情` / `新增學生` / `編輯` / `產生登入連結` / `封存帳號`）
- **`archived` 列存在**（seed 有 1 筆），但它的選單本輪未展開
- **登入連結對話框**：**開不起來**（422 就結束了），所以它長什麼樣仍然未驗

### 匯入對話框的兩件現況

- 檔案解析 `slice(2)` —— **前兩列都當表頭跳過**（`parent-import-dialog.component.ts:302`），
  欄序是 家長姓名 / 電話 / Email / 家長備註 / 學生姓名 / 年級 / 學校 / 生日 / 性別
- **匯入完成後、按「完成」之前，背後的統計不會更新**（仍顯示舊的 29）

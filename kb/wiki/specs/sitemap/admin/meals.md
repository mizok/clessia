---
title: 餐費管理（/admin/meals）
summary: /admin/meals 的實際 UI 地圖：當日名單（可編輯）與區間查詢（唯讀）兩種模式，一支月結對話框。
category: spec
status: developing
tags: [sitemap, admin]
created: 2026-09-12
updated: 2026-09-13
---

# 餐費管理

<!-- generated:route-facts start —— 這一段由 tools/sitemap 生成，不要手改 -->

**路由**：`/admin/meals`
**角色**：管理員（`admin`）
**選單位置**：行政財務 › 餐費管理
**額外權限**：`manage_finance`（`permissionGuard` + 選單隱藏）

<!-- generated:route-facts end -->

**進入方式**：選單「行政財務 › 餐費管理」/ 直接網址（沒有其他頁面連過來）

**外框**見 [[specs/sitemap/_shared/shell-layout]]。

## 這一頁有兩種模式，畫面差很多

工具列第一個元素是一組 `p-selectbutton`：**當日名單** ／ **區間查詢**。
**當日名單可編輯、區間查詢唯讀**，而且**兩種模式的欄位與工具列都不一樣**：

|            | 當日名單                                | 區間查詢                                  |
| ---------- | --------------------------------------- | ----------------------------------------- |
| 日期控制   | 單日 datepicker                         | 區間 datepicker（`readonlyInput`）        |
| 額外篩選   | 無                                      | 學生自動完成（`輸入學生姓名篩選`）        |
| 表格首欄   | —                                       | **多一欄「日期」**                        |
| 「班級」欄 | 有                                      | **沒有**                                  |
| 列上的輸入 | 可編輯（未結算的列）                    | **全部 disabled**                         |
| 表格分頁   | 無                                      | **有**（每頁 50 筆）                      |
| 底部       | 「共 N 位」+「確認名單」+ 人工判斷提示  | 無                                        |
| 摘要句     | `訂 N 份，其中 M 份要收費，合計 X 元。` | `這段期間 N 筆餐記錄…（含已結算的 K 筆）` |

> 摘要句**只在有資料時才出現**（`rows().length > 0`）。**空清單時改由空狀態自己寫出查詢區間**
> （#710 修正後）—— 那是畫面上唯一說明「剛才查了什麼」的地方。

## 畫面區塊

### 1. 頁首

`餐費管理` + 副標「每日勾選誰要訂餐，月底一次結算成帳單。有上課不一定訂餐 —— 課表只給候選名單。」
右側「月結」（`app-page-actions` 的 primary action）。

### 2. 工具列

模式切換 → 日期控制 → （當日模式且有列時）「全部訂」「全部不訂」／（區間模式）學生自動完成。

### 3. 摘要句

見上表。當日模式若有已結算的列，會多一段「（N 筆已結算，鎖住不能改）」。

### 4. 表格

欄位（當日模式）：`學生`、`班級`、`訂餐`、`單價`、`收費`、`備註`、`狀態`。

- `訂餐`：checkbox；**該列已結算就 disabled**
- `單價`：金額輸入（TWD 整數）；**沒訂餐或已結算就 disabled**，預設值是後端回的 `defaultUnitPrice`（本機 65）
- `收費`：checkbox；**沒訂餐或已結算就 disabled**
- `備註`：文字（`maxlength=200`，placeholder `如：素食`）；已結算就 disabled
- `狀態`：三選一
  | 條件                | 顯示     | 備註                                                                 |
  | ------------------- | -------- | -------------------------------------------------------------------- |
  | `settled`           | `已結算` | 帶 tooltip「已經開進帳單。要改得走帳單作廢或下期調整」               |
  | `recordId === null` | `未處理` | **樣式有時間維度**：當天以前是 `overdue`，當天（含）以後是 `pending` |
  | 其餘                | `已確認` | —                                                                    |

### 5. 底部（只有當日模式）

左邊固定提示「「收不收費」是人工判斷 —— 便當送到了才請假要不要收，由你決定，系統不自動判斷。」，
右邊「共 N 位」+「確認名單」。

超過後端一次寫入上限（`MEAL_BATCH_MAX_ROWS = 300`）時，「確認名單」變 disabled 並在下方
多一段紅字提示（**未驗** —— 本機當日最多 22 人）。

## 資料來源

| 模式     | 請求                                                                                  |
| -------- | ------------------------------------------------------------------------------------- |
| 當日名單 | `GET /api/meals?date=YYYY-MM-DD`                                                      |
| 區間查詢 | `GET /api/meals?dateFrom=…&dateTo=…&page=1&pageSize=50`（有選學生才多帶 `studentId`） |

**沒有選滿區間時的預設是「這個月 1 號到目前這一天」** —— 不帶區間後端會回 400。
實測切到區間模式當下送出的就是 `dateFrom=2026-09-01&dateTo=2026-09-12`。

寫入只有兩個出口：`POST /api/meals/batch`（確認名單）與月結。

## 互動元素

| 元素（畫面上的字）  | 類型                                        | 出現條件                                | 按了之後                                                 |
| ------------------- | ------------------------------------------- | --------------------------------------- | -------------------------------------------------------- |
| 月結                | 按鈕                                        | 永遠                                    | 開對話框 → 子頁面「月結」                                |
| 當日名單 / 區間查詢 | select button（二選一，`allowEmpty=false`） | 永遠                                    | 切換模式並重新取數（已實按驗證，兩個方向都試過）         |
| 名單日期            | datepicker + 日曆鍵                         | **只在當日名單模式**                    | 選日期後重新取數（已實按驗證：切到 2026-09-11 資料換掉） |
| 日期區間            | 區間 datepicker（輸入框唯讀）               | **只在區間查詢模式**                    | 選滿兩個日期才重新取數（**未驗**）                       |
| 輸入學生姓名篩選    | 自動完成                                    | **只在區間查詢模式**                    | 選到學生才重新取數（打字中不查）（**未驗**）             |
| 全部訂              | 文字按鈕                                    | **當日模式 + 非載入中 + 非失敗 + 有列** | **只改本地草稿**（已結算的列跳過），不寫入（已實按驗證） |
| 全部不訂            | 文字按鈕                                    | 同上                                    | 同上（已實按驗證）                                       |
| 每列「訂餐」        | checkbox                                    | 每一列                                  | 改本地草稿；**勾掉會連帶讓「單價」「收費」變 disabled**  |
| 每列「單價」        | 金額輸入                                    | 每一列                                  | 改本地草稿                                               |
| 每列「收費」        | checkbox                                    | 每一列                                  | 改本地草稿                                               |
| 每列「備註」        | 文字輸入                                    | 每一列                                  | 改本地草稿                                               |
| 確認名單            | 按鈕                                        | **只在當日模式且有列**                  | **寫入** `POST /api/meals/batch` → **本輪未按**          |
| 分頁控制            | 按鈕                                        | **只在區間模式且有資料**                | 換頁重新取數（**未驗** —— 本機區間查詢 0 筆）            |

> **`disabled`**：模板共 5 處 `[disabled]` 繫結 —— 列上四個輸入（`row.settled || isRange()`，
> 單價與收費再加 `!row.ordered`）與「確認名單」（`overBatchLimit()`）。**上表五處都列到了。**

## 子頁面：月結

**開啟方式**：頁首「月結」

`width: 520px`、`showHeader: false`（標題由元件自己畫）。**有兩種面貌**：

### 執行前（實測到的）

- 標題 `月結`，副標「把這個月「要收費且尚未結算」的餐費與學費加總成帳單。」
- 欄位 `結算月份`：月份 datepicker（`yy-mm`、`readonlyInput`）。**實測預設值是 `2026-08`（上個月）**
- 固定說明：「**同一個月可以重複跑。** 月結只撈「尚未結算」的記錄，處理完就蓋上結算標記 ——
  所以跑第二次不會重複開帳單，之後補登的舊記錄下次會自動被撈進來。」
- 結束動作：`取消`（text）、`執行月結`

### 執行後（**未驗** —— 會寫入，本輪不跑）

讀原始碼：四個數字（`開立帳單` / `學費明細` / `餐費明細` / `結算餐記錄`）；
有金額對不上的明細時多一段「N 筆金額對不上，要人工核對」清單；
底部按鈕變成 `關閉`，**開了帳單才多一顆「查看這批帳單」**（導向繳費紀錄）。

## 狀態

| 狀態         | 畫面                                                                                                                      |
| ------------ | ------------------------------------------------------------------------------------------------------------------------- |
| 載入中       | 五根 skeleton bar                                                                                                         |
| 載入失敗     | `載入失敗` /「沒有讀到當天的名單，可能是連線問題」+「重試」（**已實測** —— session 被別席踢掉後 API 回 401 就是這個畫面） |
| 空（當日）   | `這天沒有候選名單` /「候選名單來自當天有課的班級 —— 這天可能沒有排課」（**未驗**，本機每天都有課）                        |
| 空（區間）   | `這段期間沒有餐記錄` /「`<起日> ~ <迄日>` 沒有任何學生被登記過餐」（#710 修正後；**日期是實際送出去的那一段**）           |
| 超過批次上限 | 「確認名單」disabled + 紅字提示（**未驗**）                                                                               |
| 無權限       | 沒有 `manage_finance` 看不到選單項，直接打網址被 `permissionGuard` 導回 `/admin`（**未驗**）                              |

## 已知的落差（記錄現況，不在這裡修）

### 1. ~~區間查詢的空狀態講的是「這天」~~ —— 已修（#710）

> **2026-09-12 更新**：已修。保留原始描述是因為**它解釋了為什麼空狀態要把日期寫進去** ——
> 沒有這一段，下一個人會覺得那串日期是多餘的裝飾而把它拿掉。

**修正前**：切到「區間查詢」而區間內沒有任何餐記錄時，畫面顯示

```
這天沒有候選名單
候選名單來自當天有課的班級 —— 這天可能沒有排課
```

**兩句話都是當日模式的字**。實測當下查的是 `dateFrom=2026-09-01&dateTo=2026-09-12`
（API 回 200、`meta.total = 0`，**不是錯誤**）。而「這段期間 N 筆…」那句摘要
**只在有資料時才印**，所以那個畫面上**沒有任何東西告訴使用者剛才查的是哪一段期間**。

**修正後**：區間模式有自己的空狀態 ——
`這段期間沒有餐記錄` / `<起日> ~ <迄日> 沒有任何學生被登記過餐`。

#### ⚠️ 那串日期是「送出去的那一段」，不是選擇器現在顯示的那一段

元件新增了一個 `queriedRange` signal，**在 `load()` 裡跟送出去的參數用同一個物件**。

理由是 `onRangeChange` **先寫入 `dateRange` 才 early-return**（使用者只選了起日時），
所以那一刻選擇器已經變了、而畫面上的資料還是上一次查詢的結果。
**從 `dateRange` 即時算的話，空狀態會講一段從來沒有被查過的期間。**

有一支測試釘著這件事（「只選了起日（還沒送出）時，空狀態仍然講上一次實際查的區間」）。

### 2. 日期選擇器是英文

`September 2026` / `Su Mo Tu We Th Fr Sa` —— PrimeNG 的預設語系沒有換掉。
這不是這一頁的問題（每一支 `p-datepicker` 都一樣），記在這裡是因為它是這一頁量到的。

## 驗證紀錄

- **日期**：2026-09-12
- **角色帳號**：`admin@demo.clessia.app`（管理員，具 `manage_finance`）
- **前端版本**：4200（主 checkout，`9f08061a`）。量測當下 `git diff 9f08061a origin/main -- apps packages` 為空；
  **之後 main 進了三支動到 `apps/web` 的 commit（#690 / #691 / #697），但沒有一支碰到
  `pages/meals/`** —— `git diff 9f08061a origin/main -- apps/web/src/app/features/admin/pages/meals` 為空，
  所以這一頁量到的仍等於現在的 `origin/main`
- **視窗寬度**：1504px
- **資料狀態**：2026-09-12 有 16 位候選、`defaultUnitPrice = 65`、當天 0 筆餐記錄

> ⚠️ **這裡原本寫「整個本機資料庫沒有任何一筆餐記錄」，那是錯的。**
> 當時抽查了 09-05 / 09-08 / 09-10 / 09-11 / 08-28 五天都是空的，就寫成了全庫沒有。
> 實際 `select count(*) from meal_records` = **18**，落在 **2026-08-19 / 08-20 / 08-21**
> 各 6 筆。**抽五天推論全庫，跟坑 8 是同一族的反方向**：那個是資料太少所以漏列元素，
> 這個是查太少所以誤判「不可能」。補驗見下。

### 兩向比對（當日名單模式）

| 方向        | 結果                                                                 |
| ----------- | -------------------------------------------------------------------- |
| 地圖 ⊆ 畫面 | 當日模式該出現的 8 種元素全部找得到                                  |
| 畫面 ⊆ 地圖 | 72 個可見元素 = 8 個工具列/頁首元素 + 16 列 × 4 個列內輸入，全部有列 |

**差異：0 筆。**

### ⚠️ DOM 73 個、可見 72 個 —— 那一個是手機版的「月結」

`app-page-actions` **同時渲染兩份主要行動**：標頭裡那顆（桌機）與 `page-actions__dock`
那顆（手機停靠列）。桌機寬度下後者在 DOM 裡但看不見。

**這不是 `/admin/sessions` 特有的現象** —— [[specs/sitemap/README|方法頁]]舉的例子是
session 篩選列的三個手機元素，但**任何用 `app-page-actions` 帶 primary action 的頁面
都會多這一顆**。裸 DOM 查詢會多報，`visible` 濾網不能省。

### 區間查詢模式的清點

切過去之後 DOM 7 個、可見 6 個（月結 ×2 其中一個隱藏、模式二選一 ×2、區間 datepicker + 日曆鍵、學生自動完成）。
**表格整個不見**（0 筆），所以列內輸入與分頁都沒有出現。

### 逐項實地驗證

| 動作                | 結果                                                                                                          |
| ------------------- | ------------------------------------------------------------------------------------------------------------- |
| 「月結」按一次      | 對話框開啟，執行前面貌如上，預設月份 `2026-08` ✓；按 `取消` 後 `.p-dialog === null` ✓                         |
| 「區間查詢」按一次  | 工具列與表格換成區間模式 ✓，API 送出 `dateFrom=2026-09-01&dateTo=2026-09-12` ✓                                |
| 「當日名單」按一次  | 切回，16 列復原 ✓                                                                                             |
| 「全部訂」按一次    | 摘要變「訂 16 份，其中 16 份要收費，合計 1,040 元」；**32 個 disabled 的輸入（16 列 × 單價/收費）全部解鎖** ✓ |
| 「全部不訂」按一次  | 回到「訂 0 份…」，32 個輸入重新 disabled ✓。**兩顆都只改本地草稿，沒有任何請求送出**                          |
| 日期切到 2026-09-11 | 資料換掉，狀態點的樣式由 `status-dot--pending` 變成 **`status-dot--overdue`** ✓                               |

### 補驗：`已確認` 狀態（2026-08-19）

把日期切到 **2026-08-19**（有 6 筆餐記錄的那一天）：

| 量的東西   | 結果                                                                           |
| ---------- | ------------------------------------------------------------------------------ |
| 列數       | 22（候選名單）                                                                 |
| 狀態欄     | **兩種同時出現**：6 列 `已確認`、16 列 `未處理`                                |
| 狀態點樣式 | `status-dot--done`（已確認）與 `status-dot--overdue`（未處理，過去日期）       |
| 摘要句     | `訂 6 份，其中 0 份要收費，合計 0 元。`                                        |
| API 對照   | `GET /api/meals?date=2026-08-19` → 6 筆 `recordId` 非 null、`settled` 全 false |

**這同時把「未處理的 overdue 樣式」與「已確認」兩種狀態放在同一個畫面上驗到了。**

### 補驗：`已結算`（2026-09-13，seed 之後）

`seed.sql` 把 6 筆餐記錄掛到一張真的帳單明細上。切到 **2026-08-19**：

| 量的東西               | 結果                                                                                                        |
| ---------------------- | ----------------------------------------------------------------------------------------------------------- |
| 狀態欄出現的種類       | **三種同框**：`未處理` / `已確認` / `已結算` ✓                                                              |
| 已結算那一列（張宇軒） | 四個輸入（訂餐／單價／收費／備註）**全部 `disabled`** ✓                                                     |
| 摘要句                 | `訂 7 份，其中 1 份要收費，合計 65 元。` **＋「（1 筆已結算，鎖住不能改）」** —— 那個條件式片段第一次出現 ✓ |

### 未驗到的

| 項目                                             | 原因                                                                                                                                                                                                             |
| ------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `已確認` / `已結算` 兩種狀態                     | **本機一筆餐記錄都沒有**；要製造得先寫入                                                                                                                                                                         |
| 已結算列的 tooltip 與鎖住效果                    | 同上                                                                                                                                                                                                             |
| 「確認名單」（寫入）與它的成功／失敗 toast       | 寫入                                                                                                                                                                                                             |
| 月結執行後的畫面（含異常清單、「查看這批帳單」） | 寫入                                                                                                                                                                                                             |
| 區間模式的日期區間選擇、學生篩選、分頁           | 區間查詢 0 筆，控制項按了也看不出差別                                                                                                                                                                            |
| 超過 300 筆的批次上限提示                        | 本機單日最多 22 人                                                                                                                                                                                               |
| 空的當日名單                                     | 本機每天都有排課                                                                                                                                                                                                 |
| **日期選好之後會不會留住**                       | 本輪兩種點法（合成 `click`、真實滑鼠）都沒能穩定重現：合成點擊選得到日期但**不會觸發 PrimeNG 靠 mousedown 做的外點關閉**，真實滑鼠的座標又數次落空。**量測方式本身不可信，所以不下結論** —— 下一輪用真實滑鼠重驗 |
| 手機寬度版面（含 `page-actions__dock`）          | 本輪只量 1504px                                                                                                                                                                                                  |

## 390px

- **量測**：390 × 844 / 768 × 1024 / 1024 × 768 / 1504 × 752 ／ 前端 `3f2197f8`（4200，主 checkout ＝ `origin/main`）／ 身分 `admin@demo.clessia.app`（量測前後各打一次 `GET /api/me`）
- **手段**：同源 iframe 當 viewport；`< 44px` 的「真機」欄是**把 `(pointer: coarse)` 區塊注進去重量一次**得到的，不是推測（方法頁 Phase 2）
- **外框**（頂列、側欄 ↔ 底欄）不屬於這一頁，見 [[specs/sitemap/_shared/shell-layout]]

### ⚠️ 這一頁量到的是空狀態

畫面上是：

> 「這天沒有候選名單」
> 「候選名單來自當天有課的班級 —— 這天可能沒有排課」

**量測當天（2026-09-13，週日）沒有排課**，所以「當日名單」那一分頁的清單是空的。

**查過 DB，不是抽樣**（坑 11）：`meal_records` **全庫 24 筆，日期落在
2026-08-17 ～ 2026-08-22**（六天），**今天 0 筆**。

所以這一頁的差集只涵蓋**工具列那 5 個元素**，**不涵蓋名單列**。
要補驗得把名單日期改到 08-17 ～ 08-22 其中一天。

### 版面怎麼變

| 東西     | 390                                       | 768             | 1024 | 1504 |
| -------- | ----------------------------------------- | --------------- | ---- | ---- |
| 「月結」 | **底部固定行動列**（`page-actions__cta`） | 頁首 `p-button` | 同   | 同   |

**其餘一切四個寬度相同。**

### 差集

| 簽名                                | 390   | 768 | 1024 | 1504 |
| ----------------------------------- | ----- | --- | ---- | ---- |
| `p-button`（頁首「月結」）          | **0** | 1   | 1    | 1    |
| `page-actions__cta`（底部「月結」） | **1** | 0   | 0    | 0    |
| 合計                                | 5     | 5   | 5    | 5    |

四個寬度都有的：`p-togglebutton` ×2（當日名單 / 區間查詢）、名單日期輸入、日曆鍵。

### 水平溢出

**無**（四個寬度皆然，空狀態下）。

### 觸控目標 < 44px（390）

**被 coarse 抬起來的**：月結 `82×41 → 82×45` ✓、
名單日期 `174×38 → 174×44` ✓、日曆鍵 `40×38 → 44×44` ✓。

**沒被抬起來的**：

| 元素                         | 390 實測  | 真機          |
| ---------------------------- | --------- | ------------- |
| `p-togglebutton`（當日名單） | `98 × 42` | **`98 × 42`** |
| `p-togglebutton`（區間查詢） | `97 × 42` | **`97 × 42`** |

> ⚠️ **`p-togglebutton` 一顆都沒被 coarse 抬起來，而這一批三頁加起來有 9 顆。**
> 成因寫在 `apps/web/src/styles.scss` 的 coarse 區塊註解裡：
> 「`--p-togglebutton-padding-y` 與 `--p-checkbox-*` **不存在**，所以沒寫 ——
> 幽靈 token 不會有任何錯誤訊號，只會靜靜地什麼都不做」。
> **量測把那句註解變成了數字。** 記現況，不在這裡判定是不是缺陷。

### 鍵盤可達性

- 全頁**沒有正數 `tabindex`**；390 下 **Tab 站 5 個 ＝ 可見互動元素 5 個**
- **`cursor: pointer` 但 Tab 不到的元素：0 個**

### 未驗與原因

| 項目                     | 原因                                                                                                          |
| ------------------------ | ------------------------------------------------------------------------------------------------------------- |
| **名單列在窄寬度的版面** | 量測當天沒有排課，候選名單是空的。**要補驗得把日期改到 2026-08-17 ～ 08-22**（查過 DB：全庫 24 筆都在那六天） |
| 「區間查詢」分頁的內容   | 這一輪只量了預設的「當日名單」                                                                                |
| 「月結」按下去           | **會寫入**，零寫入邊界                                                                                        |
| `Escape` / 真的 Tab 鍵   | 需要前景分頁                                                                                                  |

---
title: 通知中心（/admin/notifications）
summary: /admin/notifications 的實際 UI 地圖：發布表單（4 個欄位）與已發布清單；副標還停在「只能發給老師」的舊世界。
category: spec
status: developing
tags: [sitemap, admin]
created: 2026-09-12
updated: 2026-09-13
---

# 通知中心

<!-- generated:route-facts start —— 這一段由 tools/sitemap 生成，不要手改 -->

**路由**：`/admin/notifications`
**角色**：管理員（`admin`）
**選單位置**：（無群組）通知中心
**額外權限**：無

<!-- generated:route-facts end -->

**進入方式**：選單「通知中心」（**無群組，排在儀表板下面**）／直接網址。

**外框**見 [[specs/sitemap/_shared/shell-layout]]。

## 畫面區塊

### 1. 頁首

`通知中心`（h1）+ 副標：

> 發布站內公告**給老師**。公告會出現在**老師的**通知頁，並顯示未讀數。

⚠️ **這句話已經不準了，見下方「刻意記下」。**

### 2. 發布新公告

| 欄位       | 控制項   | 預設       |
| ---------- | -------- | ---------- |
| `標題`     | 文字輸入 | 空         |
| `內容`     | 多行輸入 | 空         |
| `發送對象` | 下拉     | `全體老師` |
| `分校範圍` | 下拉     | `全部分校` |

底部一顆 `發布`。**條件式**：`submitError()` 為真時上方多一條錯誤訊息（未驗）。

### 3. 已發布

每一則：標題 + `YYYY-MM-DD HH:mm · <對象> · <分校>`。實測 5 則。
載入中是 3 條骨架列。

## 互動元素

| 元素       | 類型     | 出現條件 | 按了之後                                                        |
| ---------- | -------- | -------- | --------------------------------------------------------------- |
| `標題`     | 文字輸入 | 永遠     | —                                                               |
| `內容`     | 多行輸入 | 永遠     | —                                                               |
| `發送對象` | 下拉     | 永遠     | 選項：`全體老師` / `全體家長`                                   |
| `分校範圍` | 下拉     | 永遠     | —                                                               |
| `發布`     | 按鈕     | 永遠     | **`[disabled]="!canSubmit()"`** —— 見下；按下去會寫入，**未按** |

### ⚠️ `發布` 還是舊的 disabled 寫法

`[disabled]="!canSubmit()"`（`notifications.component.html:64`）——
**實測剛進頁面時它是 disabled。**

而 PR #664 → #716 → #718 已經把方向定成「**拿掉送出鍵的 disabled，改欄位級錯誤**」，
理由是 `disabled` 讓「按了沒反應」成真。人員／學生／家長三支表單都已對齊。

**這是同一族的第三處，而它不在那三支表單裡**（它是頁面內的表單，不是對話框）。
記成現況，已回報計畫席。

## 刻意記下：副標說「給老師」，而它早就能發給家長

- `audienceOptions = ['all_teachers', 'all_parents']`（`component.ts:72`）
- 已發布清單裡**實際存在**一則 `· 全體家長 ·` 的公告
- 解鎖是 PR #636（`8d2eb7c9`，2026-09-12）做的，**而它逐行沒有碰過那句副標**
  （`git show 8d2eb7c9 -- notifications.component.html | grep 給老師` 零命中）

**#636 自己的標題就是「家長端 09-04 就接上了，而註解沒被回頭檢查」** ——
它回頭檢查了註解，**沒回頭檢查使用者看得到的那句話**。

> 這是本 repo 記過的那一族：**寫了「因為 X 所以這樣做」的東西，
> 在 X 不再成立時也要被回頭檢查** —— 而「使用者看得到的文案」比註解更該被檢查。

已開 issue 回報。地圖記現況、不調和。

## 狀態

| 狀態     | 畫面                             |
| -------- | -------------------------------- |
| 載入中   | 3 條骨架列（已發布區）           |
| 發布失敗 | 表單下方一條錯誤訊息（**未驗**） |

## 驗證紀錄

- **狀態**：已驗
- **前端版本**：最新 main，自架 `:4210`
- **取樣器**：方法頁版本 + `[tabindex]:not([tabindex="-1"])`
- **兩向比對**：可見 **7** = 1（標題）+ 1（內容）+ 2（發送對象的兩種表示）
  - 2（分校範圍的兩種表示）+ 1（`發布`）。**0 差異。**
    已發布清單的項目**不是互動元素**（純文字，不可點）。
- **未驗，附原因**：
  - `submitError()` 的錯誤訊息 —— 需要讓發布失敗
  - `發布` 本身 —— **會寫入**
  - 兩個下拉的完整選項：`發送對象` 讀原始碼確認是兩項；`分校範圍` 未展開

## 390px

- **量測**：390 × 844 / 768 × 1024 / 1024 × 768 / 1504 × 752 ／ 前端 `3f2197f8`（4200，主 checkout ＝ `origin/main`）／ 身分 `admin@demo.clessia.app`（量測前後各打一次 `GET /api/me`）
- **手段**：同源 iframe 當 viewport；`< 44px` 的那一欄是**把 `(pointer: coarse)` 區塊注進去重量一次**得到的真機尺寸，不是推測（做法見方法頁 Phase 2）
- **外框**（頂列、側欄 ↔ 底欄）不屬於這一頁，見 [[specs/sitemap/_shared/shell-layout]]

### 版面怎麼變

**沒有變。** 四個寬度掃描全部可見元素的 `display` / `grid-template-columns` /
`flex-direction` / `position` / `overflow-x`，**沒有任何一個容器的版面訊號改變**。
這一頁是單欄的表單 + 底下一份唯讀清單，從 390 到 1504 都是同一個形狀，只有容器變寬。

| 視窗 | `<main>` 內容寬 |
| ---- | --------------- |
| 390  | 390             |
| 768  | 528             |
| 1024 | 784             |
| 1504 | 1264            |

390 下整頁縱向捲動（`scrollHeight` 1004 / `clientHeight` 724）。

### 差集

**零差異。** 四個寬度都是同樣 7 個：標題 `<input>`、內容 `<textarea>`、
發送對象（`p-select-label` + `p-select-dropdown`）、分校範圍（同兩個）、`發布`。

已發布清單的 5 筆**不是互動元素**（純文字、不可點），四個寬度都一樣。

### 水平溢出

**無**（四個寬度皆然）。

### 觸控目標 < 44px

| 元素                                       | 滑鼠指標下（390） | 注入 coarse 後           |
| ------------------------------------------ | ----------------- | ------------------------ |
| `input.admin-notifications__input`（標題） | `300 × 38`        | **`300 × 38`（沒被抬）** |
| `span.p-select-label` ×2                   | `100 × 40`        | `100 × 44` ✓             |
| `div.p-select-dropdown` ×2                 | `40 × 40`         | `40 × 44` ✓              |
| `button.p-button`（發布）                  | `58 × 41`         | `58 × 45` ✓              |

> **只有標題輸入框沒被抬起來，而它旁邊的每一個控制項都被抬了。**
> 成因是 `styles.scss` 的 coarse 區塊改的是 `--p-inputtext-padding-y` 這一族 token，
> 而這顆是專案自刻的 `.admin-notifications__input`、**不是 `.p-inputtext`**，所以吃不到。
> `<textarea>` 因為有多行高度（`300 × 116`）不受影響。
> **記現況，不在這裡判定是不是缺陷。**

### 鍵盤可達性

- 全頁**沒有正數 `tabindex`**；Tab 序列 5 站：標題 → 內容 → 發送對象 → 分校範圍 → 發布
- **兩顆 `p-select-dropdown` 不是獨立的 Tab 站** —— 它們跟自己的 label 是同一個控制項
  （所以可見元素 7 個、Tab 站 5 個，這不是漏報）
- **`cursor: pointer` 但 Tab 不到的元素：0 個**

### 未驗與原因

| 項目                                    | 原因                                                                                      |
| --------------------------------------- | ----------------------------------------------------------------------------------------- |
| 兩個下拉展開後的浮層在 390 的寬度與位置 | 浮層 `appendTo="body"`，不在 `<main>` 裡；而且展開靠動畫，**分頁在背景動畫不跑**（坑 12） |
| `發布` 按下去                           | **會寫入**，零寫入邊界                                                                    |
| 發布失敗的錯誤訊息                      | 同 1504，需要讓請求失敗                                                                   |
| `Escape` / 真的 Tab 鍵                  | 需要前景分頁                                                                              |

## 載入中 / 錯誤

- **量測**：390 × 844 ／ 主 checkout 的 dev server（port 4200）`543e5eda` ／ `admin@demo.clessia.app`
- **手段**：把 XHR 的 URL 從 `:8787` 改指到沒人監聽的 `:8799`（等同 server 掛掉，
  **只影響那一個 iframe**，不動共享資源）。方法全文見[方法頁 Phase 2-D](../README.md)
- **證據**：每一輪都確認攔截清單不是空的（`請求數`），**請求數 0 的那幾輪一律作廢**
- ⚠️ **本輪只量「錯誤」那一半，「載入中」未量**（見下）

### 錯誤（所有 API 都失敗）

| 判定 | 重試鈕 | toast | 攔到的請求數 |
| --- | --- | --- | --- |
| ✅ 誠實 | 否 | 0 | 2 |

主體有失敗字樣。

### 未驗與原因

| 項目 | 原因 |
| --- | --- |
| **載入中的樣子** | 本輪只量錯誤態。**做法已經驗過**（把 XHR 的 `send` 延後 3 秒），只是沒鋪開 —— 已知形狀見 [[specs/sitemap/parent/attendance]]：切篩選時 3 秒內畫面**完全沒有反應**，沒有 skeleton／spinner／disabled |
| **`Escape` / 真滑鼠重試** | 需要前景分頁（方法頁坑 12）。**「重試鈕按了會不會真的重打」沒有驗** |
| 其他寬度 | 錯誤態與寬度無關（是狀態不是版面），只量 390 |

---
title: 課務異動（/admin/changes）
summary: /admin/changes 的實際 UI 地圖：調課／代課／停課的唯讀紀錄表，三個篩選器、零寫入動作。
category: spec
status: developing
tags: [sitemap, admin]
created: 2026-09-12
updated: 2026-09-12
---

# 課務異動

<!-- generated:route-facts start —— 這一段由 tools/sitemap 生成，不要手改 -->

**路由**：`/admin/changes`
**角色**：管理員（`admin`）
**選單位置**：課務管理 › 課務異動
**額外權限**：無

<!-- generated:route-facts end -->

**進入方式**：選單「課務管理 › 課務異動」/ 直接網址

**外框**見 [[specs/sitemap/_shared/shell-layout]]。

## 畫面區塊

### 1. 頁首

`課務異動` + 副標：**`調課、代課、停課的完整紀錄。此頁唯讀，要修改課堂請至課堂管理。`**

> 副標自己講明了這一頁沒有寫入動作 —— 實測也確實只有三個篩選器，**零個會改變資料的元素**。

### 2. 篩選列

三個下拉：**月份**（`2026 年 9 月`）、**異動類型**（`全部異動`）、**分校**（`全部分校`）。

### 3. 紀錄表

依日期分段（`2026-09-30`、`2026-09-11` …），欄位：

`課堂` / `變更內容` / `類型` / `異動時間` / `操作者` / `原因`

實測看到的「類型」值：`停課`、`恢復上課`、`批次`。
「操作者」是人名或角色（實測有 `Demo Admin`、`行政櫃台`）。

### 資料來源

```
GET /api/system-time
GET /api/me
GET /api/campuses
GET /api/sessions/changes?from=<月初>&to=<月底>&page=1&pageSize=20
```

## 互動元素

| 元素（畫面上的字） | 類型         | 出現條件 | 按了之後                               |
| ------------------ | ------------ | -------- | -------------------------------------- |
| 2026 年 9 月       | 下拉（月份） | 永遠     | 換月份，重新取數（改 `from` / `to`）   |
| 全部異動           | 下拉（類型） | 永遠     | 篩選異動類型（**未驗** —— 未展開選項） |
| 全部分校           | 下拉（分校） | 永遠     | 篩選分校（**未驗**）                   |

**整頁只有這三個。** 沒有按鈕、沒有列動作、沒有對話框。

## 狀態

| 狀態             | 畫面                                                                              |
| ---------------- | --------------------------------------------------------------------------------- |
| 空（該月無異動） | **未驗**，但 API 對 2026-06/07/08 回 `total: 0`，所以空狀態一定可達（換月份即可） |
| 載入中 / 錯誤    | **未驗**                                                                          |
| 無權限           | 不適用 —— 這條路由沒有 `permission`                                               |

## 驗證紀錄

- **日期**：2026-09-12 ／ 帳號 `admin@demo.clessia.app` ／ 前端 `c1535d04`（port 4201）
- **資料狀態**：2026 年 9 月，20 筆異動

### 兩向比對

|                          | 數量  |
| ------------------------ | ----- |
| `<main>` 內 DOM 互動元素 | 3     |
| 不可見                   | 0     |
| **可見**                 | **3** |

| 方向        | 結果               |
| ----------- | ------------------ |
| 地圖 ⊆ 畫面 | 3 項都找得到       |
| 畫面 ⊆ 地圖 | 3 個可見元素都有列 |

**差異：0 筆。**

### 分頁器：分不出來，所以不寫結論

畫面上**沒有分頁器**，而本月剛好 20 筆 = `pageSize` 的 20。
**「不需要分頁器」與「沒有分頁器」在這個資料量下長得一模一樣。**

跑了一組有鑑別力的查詢想分開它們：

| 查詢                                        | 結果                  |
| ------------------------------------------- | --------------------- |
| `from=2026-01-01&to=2026-12-31&pageSize=20` | `total: 20`           |
| 同上 `pageSize=100`                         | 回 20 筆、`total: 20` |
| 同上 `page=2&pageSize=20`                   | 回 **0** 筆           |

**結論：整個 2026 年真的只有 20 筆異動**（一度懷疑 `total` 是被 pageSize 截斷的假數字，
`pageSize=100` 這一發證明不是）。

**所以「超過 20 筆時會不會出現分頁器」在本機無法驗** —— 需要造資料，不在本工單範圍。
記成未驗，不寫成「這一頁沒有分頁」。

### 未驗到的

| 項目                     | 原因                                                |
| ------------------------ | --------------------------------------------------- |
| 三個下拉展開後的選項清單 | 漏做，下一輪補（三個都只確認了存在與預設值）        |
| 空狀態                   | 未實際切到 2026-06〜08（API 已證明那幾個月是 0 筆） |
| 超過 20 筆時的分頁行為   | 本機資料量不足，見上                                |
| 載入中 / 錯誤            | 沒有製造手段                                        |

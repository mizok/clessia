---
title: 報名進出（/admin/enrollments）
summary: /admin/enrollments 的實際 UI 地圖：三個篩選、一張唯讀表格，而整頁最主要的互動是取樣器漏掉的那 12 列。
category: spec
status: developing
tags: [sitemap, admin]
created: 2026-09-12
updated: 2026-09-12
---

# 報名進出

<!-- generated:route-facts start —— 這一段由 tools/sitemap 生成，不要手改 -->

**路由**：`/admin/enrollments`
**角色**：管理員（`admin`）
**選單位置**：學務管理 › 報名進出
**額外權限**：無

<!-- generated:route-facts end -->

**進入方式**：選單「學務管理 › 報名進出」／直接網址。

**外框**見 [[specs/sitemap/_shared/shell-layout]]。

## 這一頁是唯讀的，而且它自己說了

副標逐字：「這段期間的報名進出。**此頁唯讀，要調整報名狀態請點進班級。**」

**沒有任何新增／編輯／動作選單，也沒有對話框。** 唯一的出口是點一列進班級詳情。

## 畫面區塊

### 1. 頁首

`報名進出`（h1，取自 `page().label`）+ 上面那句副標。**沒有橘帶、沒有錨點**
—— 跟同群組另外幾頁的版面不同。

### 2. 篩選列（三個下拉，無搜尋框）

| 下拉 | 預設       | 備註                                   |
| ---- | ---------- | -------------------------------------- |
| 期間 | `2026 年 9 月` | 另有 `不限期間`（`ALL_MONTHS`）    |
| 狀態 | `全部狀態` | —                                      |
| 分校 | `全部分校` | 選項由 `campusOptions()` 動態產生      |

### 3. 統計句

`本頁 新報名 N 筆、退班 M 筆，共 T 筆` —— **「本頁」不是「全部」**，
它算的是當前這一頁的列。

### 4. 表格

表頭：`學生`｜`班級`｜`事件`｜`日期`｜`狀態`｜`分校`｜`最後異動`

- `學生`格底下附年級
- `事件`是 chip（新報名／退班）
- `分校`沒有值時顯示 `—`
- **整列可點也可鍵盤操作**：`tabindex="0"` + `(click)` + `(keydown.enter)`

### 5. 分頁器

條件式。實測 12 筆單頁，**不渲染**。

## 互動元素

| 元素           | 類型           | 出現條件 | 按了之後                                            |
| -------------- | -------------- | -------- | --------------------------------------------------- |
| 期間下拉       | 下拉           | 永遠     | 重查                                                |
| 狀態下拉       | 下拉           | 永遠     | 重查                                                |
| 分校下拉       | 下拉           | 永遠     | 重查                                                |
| **每一列**     | 可點列（`tr`） | 每一筆   | `openClass(row)` → `/admin/courses/:courseId/classes/:classId` |

## 狀態

| 狀態                  | 畫面                           |
| --------------------- | ------------------------------ |
| 載入中                | 骨架列                         |
| 查詢失敗              | `查詢失敗，請稍後再試。`       |
| 空 + 有選定月份       | `這個月沒有報名進出。`         |
| 空 + `不限期間`       | `沒有符合條件的報名。`         |

> 兩種空狀態的文案**依 `hasPeriod()` 而異** —— 未驗到（需要造出空結果）。

## ⚠️ 這一頁把方法頁取樣器的一個缺口逼出來了

方法頁那段選擇器取樣這一頁：**3 個**。加上 `[tabindex]:not([tabindex="-1"])`：**18 個**
（多出來的是 3 個下拉的重複 span + **12 個可點列**）。

**而兩向比對照樣會是「0 差異」** —— 地圖照取樣器寫，比對的兩邊來自同一個瞎掉的來源。
一頁只有 3 個互動元素的唯讀頁讀起來甚至很合理。

**這跟當初推翻 `read_page` 是同一族**，只是漏的是選擇器清單而不是無障礙樹。
已回報 labor-1（#709 還開著）。**本頁用加了 `[tabindex]` 的版本取樣。**

前三頁沒撞到是因為 `/admin/students` 與 `/admin/parents` 的列**真的不可點**
（實測 `cursor: auto`、無 handler）—— 那三頁的 0 差異仍然成立。

## 驗證紀錄

- **狀態**：已驗
- **前端版本**：`650d257a`，自架 `:4210`
- **取樣器**：方法頁版本 **+ `[tabindex]:not([tabindex="-1"])`**（理由見上）
- **兩向比對**：可見 **18**（3 下拉 + 3 下拉重複 span + 12 列），扣掉重複表示 = **15 個真元素**。
  地圖列了 3 個下拉 + 每一列 → **0 差異**
- **未驗，附原因**：
  - 兩種空狀態與查詢失敗態 —— 需要造出空結果／斷網
  - `不限期間` 之外的月份切換 —— 只驗了預設月
  - 點列導向的落點頁 —— 屬於 `ADMIN_ACADEMICS`（[[specs/sitemap/admin/courses]] 那一組）

---
title: 考試管理（/admin/grades/exams）
summary: /admin/grades/exams 的實際 UI 地圖：補習班考試與學校考試合併清單、兩條待辦橫幅、每場考試四項動作。
category: spec
status: developing
tags: [sitemap, admin]
created: 2026-09-12
updated: 2026-09-13
---

# 考試管理

<!-- generated:route-facts start —— 這一段由 tools/sitemap 生成，不要手改 -->

**路由**：`/admin/grades/exams`
**角色**：管理員（`admin`）
**選單位置**：考務與成績 › 考試管理
**額外權限**：無

<!-- generated:route-facts end -->

**進入方式**：選單「考務與成績 › 考試管理」/ **`/admin/grades` 轉址過來**（見 [[specs/sitemap/admin/grades]]）/
儀表板「成績待登錄」卡片 / 三條舊路由 redirect

**外框**見 [[specs/sitemap/_shared/shell-layout]]。

## 畫面區塊

### 1. 頁首

`考試管理` + 副標「管理補習班考試與學校考試，統一查看狀態、進入成績登錄」，右側「新增考試」。

### 2. 待辦橫幅（條件式，兩條）

- `有 N 場 進行中的補習班考試一筆成績都還沒登錄`
- `有 N 場補習班考試登到一半`

**兩條都是按鈕**，而且**都只講補習班考試** —— 學校考試沒有對應的橫幅。

資料來源：`GET /api/academy-exams/todo-count`、`GET /api/school-exams/todo-count`

### 3. 篩選列

- 期間 toggle：`近1月` / `近3月` / `近半年` / `全部`
- 型態 toggle：`補習班考試` / `學校考試`
- 三個下拉：`全部分校` / `全部科目` / `全部狀態`
- 搜尋框：`搜尋考試名稱...`

### 4. 考試清單

一場考試一列。實測欄位（補習班考試）：
`考試名` · `類別（小考…）` · `分校` · `科目` · `型態` · `日期` · `已登錄/總人數` · `狀態`。

學校考試那一列的形狀不同：`段考 · 第二次` · `學校考試` · `日期` · `學校名 · 全科目` · `N 筆` · `狀態`。

實測到的狀態：`進行中`、`已結束`。

資料來源：`GET /api/academy-exams?page=1&pageSize=20`（型態 toggle 切換時換端點）

### 資料來源（進頁面）

```
GET /api/system-time
GET /api/me
GET /api/campuses?pageSize=0&isActive=true
GET /api/subjects
GET /api/schools?isActive=true
GET /api/academy-exams/todo-count
GET /api/school-exams/todo-count
GET /api/academy-exams?page=1&pageSize=20
```

## 互動元素

| 元素（畫面上的字）             | 類型     | 出現條件                                    | 按了之後                                       |
| ------------------------------ | -------- | ------------------------------------------- | ---------------------------------------------- |
| 新增考試                       | 按鈕     | 永遠（另有一顆手機版在 DOM 但桌機不可見）   | **未驗**                                       |
| 有 N 場…一筆成績都還沒登錄     | 橫幅按鈕 | **有這種考試時**                            | **未驗**                                       |
| 有 N 場…登到一半               | 橫幅按鈕 | **有這種考試時**                            | **未驗**                                       |
| 近1月 / 近3月 / 近半年 / 全部  | 切換鈕   | 永遠                                        | 改期間，重新取數                               |
| 補習班考試 / 學校考試          | 切換鈕   | 永遠                                        | 切換清單型態（實測：補習班 11 列 → 學校 6 列） |
| 全部分校 / 全部科目 / 全部狀態 | 下拉     | 永遠                                        | 篩選（**未展開選項**）                         |
| 搜尋考試名稱…                  | 搜尋框   | 永遠（另有一顆手機版不可見）                | 過濾清單                                       |
| 篩選                           | 按鈕     | **只有手機版**（桌機寬度下在 DOM 但不可見） | —                                              |
| 考試名稱                       | 按鈕     | 每場一顆                                    | **未驗**                                       |
| 每列 ⋮                         | 按鈕     | 每場一顆                                    | 開動作選單（見下）                             |

### 每場考試 ⋮ 的動作選單

`PopupMenuComponent`（`.popup-menu__panel`）。**四項，第三項隨狀態改變**：

| 項目         | 出現條件        | 按了之後                                                                  |
| ------------ | --------------- | ------------------------------------------------------------------------- |
| 進入成績登錄 | 永遠            | 導向 [[specs/sitemap/admin/grades-exams-type-id-scores]]                  |
| 編輯基本資料 | 永遠            | **未驗**                                                                  |
| **結束考試** | 狀態 = `進行中` | **未驗**（會寫入）                                                        |
| **重新開啟** | 狀態 = `已結束` | **未驗**（會寫入）                                                        |
| 刪除         | 永遠            | 開確認對話框（**未驗**，推測是 [[specs/sitemap/_shared/confirm-dialog]]） |

**選單只隨狀態變，不隨考試型態變** —— 三個樣本：
補習班/進行中、補習班/已結束、**學校/進行中**（學校那一場的選單與補習班/進行中逐字相同）。

## 狀態

| 狀態               | 畫面                                |
| ------------------ | ----------------------------------- |
| 載入中 / 空 / 錯誤 | **未驗**                            |
| 無權限             | 不適用 —— 這條路由沒有 `permission` |

## 驗證紀錄

- **日期**：2026-09-12 ／ 帳號 `admin@demo.clessia.app` ／ 前端 `897d691f`（port 4201）
- **資料狀態**：補習班考試 11 場（含 進行中 / 已結束）、學校考試 6 場

### 兩向比對

|                                            | 數量   |
| ------------------------------------------ | ------ |
| `<main>` 內 DOM 互動元素                   | 38     |
| 不可見（手機版：新增考試、搜尋框、篩選鈕） | 3      |
| **可見**                                   | **35** |

35 = 新增考試 1 + 待辦橫幅 2 + 期間 4 + 型態 2 + 下拉 3 + 搜尋 1 + 考試名 11 + ⋮ 11。

| 方向        | 結果                      |
| ----------- | ------------------------- |
| 地圖 ⊆ 畫面 | 表列元素都找得到          |
| 畫面 ⊆ 地圖 | 35 個可見元素都有對應條目 |

**差異：0 筆。**

### 未驗到的

| 項目                                 | 原因                                     |
| ------------------------------------ | ---------------------------------------- |
| 新增考試、兩條待辦橫幅、考試名稱按鈕 | 本輪把時間給了選單的狀態維度；四者都沒按 |
| 「結束考試」「重新開啟」「刪除」     | 會寫入                                   |
| 「編輯基本資料」對話框               | 漏開                                     |
| 三個下拉的選項                       | 未展開                                   |
| 載入中 / 空 / 錯誤                   | 沒有製造手段                             |

## 390px

- **量測**：390 × 844 / 768 × 1024 / 1024 × 768 / 1504 × 752 ／ 前端 `3f2197f8`（4200，主 checkout ＝ `origin/main`）／ 身分 `admin@demo.clessia.app`（量測前後各打一次 `GET /api/me`）
- **手段**：同源 iframe 當 viewport；`< 44px` 的「真機」欄是**把 `(pointer: coarse)` 區塊注進去重量一次**得到的，不是推測（方法頁 Phase 2）
- **外框**（頂列、側欄 ↔ 底欄）不屬於這一頁，見 [[specs/sitemap/_shared/shell-layout]]

### 版面怎麼變：**這一頁有一支真正的手機專用工具列**

整批 admin 到目前為止，**只有這一頁在窄寬度換上了另一個元件**，不是把同一個元件壓扁：

| 容器                                                    | 390    | 768    | 1024   | 1504   |
| ------------------------------------------------------- | ------ | ------ | ------ | ------ |
| `exams__mobile-toolbar`                                 | **在** | **在** | 不存在 | 不存在 |
| `exams__toolbar` / `__toolbar-top` / `__toolbar-bottom` | 不存在 | 不存在 | **在** | **在** |

桌機工具列裡的東西（3 個 `p-select` + 6 個 `p-togglebutton` 的類型／狀態切換）
**在 ≤768 完全不存在**，取而代之的是手機工具列上的一顆 `篩選` 按鈕（`72 × 34`）。

其餘的變化跟這一批其他頁同形：

| 東西                            | 390                    | 768      | 1024    | 1504                         |
| ------------------------------- | ---------------------- | -------- | ------- | ---------------------------- |
| 表格欄位                        | **收合**（展開鍵 ×20） | 收合     | 收合    | **不收合**                   |
| 「新增考試」                    | **底部固定行動列**     | 頁首按鈕 | 同      | 同                           |
| 分頁器                          | compact                | compact  | compact | **完整**（首末頁 + 頁碼 ×5） |
| 考試名稱連結 `exams__name-link` | **不顯示**             | 顯示 ×20 | ×20     | ×20                          |
| 狀態欄 `status-dot`             | 收在展開區內           | 同       | 同      | **列上顯示**                 |

### 差集

| 簽名                                   | 390    | 768    | 1024      | 1504  |
| -------------------------------------- | ------ | ------ | --------- | ----- |
| `button.exams__name-link`              | **0**  | 20     | 20        | 20    |
| `responsive-table__expand-button`      | **20** | **20** | **20**    | 0     |
| `p-button-outlined`（手機的「篩選」）  | **1**  | **1**  | 0         | 0     |
| `p-select-label` / `p-select-dropdown` | 0      | 0      | **3 / 3** | 3 / 3 |
| `p-togglebutton`（類型 ×2 + 狀態 ×4）  | 0      | 0      | **6**     | 6     |
| `p-button`（頁首「新增考試」）         | 0      | **1**  | 1         | 1     |
| `page-actions__cta`（底部）            | **1**  | 0      | 0         | 0     |
| `p-paginator-page`                     | 0      | 0      | 0         | **5** |
| 合計                                   | 47     | 67     | 78        | 65    |

四個寬度都有的：搜尋框、列動作鍵 ×20、兩條待辦橫幅、上一頁／下一頁。

> **合計在 1024 是最高點（78），1504 反而降到 65** —— 因為 20 顆展開鍵在 1504 消失，
> 而攤開的欄位本身不是互動元素。跟 [[specs/sitemap/admin/sessions]] 同一個形狀。

⚠️ **「考試名稱在 390 點不到」值得單獨記**：`exams__name-link` 是一顆 `<button>`，
在 390 完全不在可見清單裡。它在窄寬度是收在展開區裡、還是整個不渲染，**這一輪沒分辨出來**
（展開要動畫，而分頁在背景不跑 —— 坑 12）。**列動作鍵 ×20 在 390 是在的**，
所以「進得去成績登錄」這條路沒有斷。

### 水平溢出

**無**（四個寬度皆然）。

### 觸控目標 < 44px（390）

**被 coarse 抬起來的**：展開鍵 ×20 `26×26 → 44×44` ✓、列動作鍵 ×20 `40×40 → 44×44` ✓、
新增考試 `114×41 → 114×45` ✓、手機「篩選」鈕 `72×34 → 72×44` ✓。

**沒被抬起來的**：

| 元素                        | 390 實測   | 真機       |
| --------------------------- | ---------- | ---------- |
| `input.exams__search-input` | `184 × 33` | `184 × 37` |
| 分頁器上一頁 / 下一頁       | `40 × 40`  | `40 × 40`  |

搜尋框是這一批唯一一個「有被 coarse 動到、但**動完還是不足 44**」的元素（33 → 37）。

### 鍵盤可達性

- 全頁**沒有正數 `tabindex`**；390 下 Tab 站 42 / 可見互動元素 47
- **`cursor: pointer` 但 Tab 不到的元素：0 個**

### 未驗與原因

| 項目                                             | 原因                                                                                  |
| ------------------------------------------------ | ------------------------------------------------------------------------------------- |
| 手機「篩選」按鈕展開後的面板                     | 展開靠動畫，**分頁在背景不跑**（坑 12）；面板可能 `appendTo="body"`，不在 `<main>` 內 |
| `exams__name-link` 在 390 到底是收起來還是不渲染 | 同上 —— 分辨這件事要先把展開區打開                                                    |
| 展開區內容在 390 的版面                          | 同上。**狀態欄在窄寬度住在這裡面**                                                    |
| `Escape` / 真的 Tab 鍵                           | 需要前景分頁                                                                          |

## 載入中 / 錯誤

- **量測**：390 × 844 ／ 主 checkout 的 dev server（port 4200）`543e5eda` ／ `admin@demo.clessia.app`
- **手段**：把 XHR 的 URL 從 `:8787` 改指到沒人監聽的 `:8799`（等同 server 掛掉，
  **只影響那一個 iframe**，不動共享資源）。方法全文見[方法頁 Phase 2-D](../README.md)
- **證據**：每一輪都確認攔截清單不是空的（`請求數`），**請求數 0 的那幾輪一律作廢**
- **載入中的量測**：390 × 844 ／ 主 checkout 的 dev server（port 4200）`7e9da649` ／
  `admin@demo.clessia.app`（permissions `["*"]`，量測前後各打一次 `/api/me`）
- **手段（載入中）**：同一個 XHR 包裝，把 `send` 用 `setTimeout` 延後 3 秒。
  ⚠️ **背景分頁的 `setTimeout` 被節流**，實際延遲 ≥ 3 秒（見[方法頁](../README.md)）
- **導航**：`ng.ɵgetRouterInstance(...)` 拿 Router 本人再 `navigateByUrl()`（見[方法頁](../README.md)）

### 載入中（3 秒延遲）

| skeleton | spinner | `<main>` 互動元素 | 判定 |
| --- | --- | --- | --- |
| **6**（`div.skeleton-list 284×192`） | 0 | 17 → 81 | ✅ 誠實 |

延遲期間標題、「新增考試」與篩選列都在；兩條提示帶（「有 2 場…一筆成績都還沒登錄」）
要等資料回來才出現。

⚠️ 骨架的 `animation-name` 是 `skeleton-wave`、`opacity` 是 `1` —— **看得見，只是波紋不會動**（坑 12）。

### 錯誤（所有 API 都失敗）

| 判定 | 重試鈕 | toast | 攔到的請求數 |
| --- | --- | --- | --- |
| 🔴 謊稱沒資料 | **有** | 2 | 6 |

空狀態說沒資料，**但頁面上有重試** —— 本輪唯一「謊稱沒資料卻給得出重試」的一頁。

### 未驗與原因

| 項目 | 原因 |
| --- | --- |
| **`Escape` / 真滑鼠重試** | 需要前景分頁（方法頁坑 12）。**「重試鈕按了會不會真的重打」沒有驗** |
| 其他寬度 | 錯誤態與寬度無關（是狀態不是版面），只量 390 |

---
title: 學生管理（/admin/students）
summary: /admin/students 的實際 UI 地圖：橘帶錨點、三個篩選、學生表格與列動作選單，兩支對話框。
category: spec
status: developing
tags: [sitemap, admin]
created: 2026-09-12
updated: 2026-09-12
---

# 學生管理

<!-- generated:route-facts start —— 這一段由 tools/sitemap 生成，不要手改 -->

**路由**：`/admin/students`
**角色**：管理員（`admin`）
**選單位置**：學務管理 › 學生管理
**額外權限**：無

<!-- generated:route-facts end -->

**進入方式**：選單「學務管理 › 學生管理」／直接網址。
**列不可點** —— 進學生詳情只能走列尾 `⋮` 的「學生詳情」（實測 `cursor: auto`、無 click handler）。

**外框**見 [[specs/sitemap/_shared/shell-layout]]。

## 畫面區塊

### 1. 橘帶

眉標 `學務管理`（`page().group.label`）、大標 `學生管理`、副標
「管理學生基本資料、家長關聯與學習紀錄」。

右側：`新增學生` 主要動作，以及**條件式**錨點 ——
`N 名學生 ／ 在籍 N ・ 停用 M`。

- 錨點**只在 `!loading() && students().length > 0`** 時出現
- `・ 停用 M` 那一段**只在 `inactiveStudentCount() > 0`** 時出現
  （實測資料 69 名全部在籍，所以**這一段未驗到**）

### 2. 篩選列

搜尋框 `搜尋姓名、學校或家長姓名...` + 兩個下拉：

| 下拉 | 佔位字   | 選項                                                       |
| ---- | -------- | ---------------------------------------------------------- |
| 年級 | 全部年級 | `全部年級`／小一…小六／國一…國三／高一…高三（**13 項**） |
| 狀態 | 全部狀態 | `啟用中`／`已停用`（**2 項**，「全部」是佔位字不是選項）   |

搜尋走 `debounceTime(300)` + `distinctUntilChanged` + `switchMap`（#659／#661）。

### 3. 學生表格

表頭：`姓名`｜`年級`｜`學校`｜`關聯家長`｜`狀態`｜（動作，無標題）

一列一名學生：

- **姓名**格底下條件式顯示分校名（`student.campusNames.length > 0`）
- **學校**格：沒有就顯示空狀態字樣（`student.school?.name` 為空時）
- **關聯家長**：多筆以「、」串接；沒有時顯示 `無關聯家長`
- **狀態**：`在籍`（`done`）／`停用`（`inactive`）
- **列尾 `⋮`**：開列動作選單（見下）

### 4. 分頁器

`<<`｜`<`｜頁碼｜`>`｜`>>`，每頁 20 筆。實測 69 筆 / 4 頁；
**第 1 頁時 `<`（Previous Page）是 `disabled`**。

### 資料來源

`StudentsService.list({ search, grade, page, pageSize, isActive })` —— 一支，
**所有取數（換頁、換篩選、搜尋）都走同一條 `switchMap` 管線**。

## 互動元素

| 元素（畫面上的字）           | 類型     | 出現條件                                       | 按了之後                                                  |
| ---------------------------- | -------- | ---------------------------------------------- | --------------------------------------------------------- |
| `新增學生`（橘帶）           | 按鈕     | 永遠                                           | 開 [[specs/sitemap/_shared/student-form-dialog]]（新增模式，**有家長欄**） |
| `搜尋姓名、學校或家長姓名...` | 文字輸入 | 永遠                                           | 300ms 後重查，回第 1 頁                                    |
| 年級下拉                     | 下拉     | 永遠                                           | 重查，回第 1 頁                                            |
| 狀態下拉                     | 下拉     | 永遠                                           | 重查，回第 1 頁                                            |
| `清除篩選`                   | 文字鈕   | **只在空結果且有篩選條件時**（空狀態內）       | 清掉搜尋與年級（**不清狀態篩選** —— 見下方「刻意記下」）   |
| `新增學生`（空狀態內）       | 按鈕     | **只在空結果且無篩選條件時**                   | 同橘帶那顆                                                 |
| 列尾 `⋮`                     | 圖示鈕   | 每一列                                         | 開列動作選單                                               |
| 分頁鈕                       | 按鈕     | 永遠                                           | 換頁；**第 1 頁時 `<` 與 `<<` disabled**                   |

### 列動作選單（`.popup-menu__panel`）

**不是 PrimeNG 選單** —— 專案自己的 `PopupMenuComponent`，項目是 `.popup-menu__item`，
沒有 `role="menuitem"`。

| 項目                       | 出現／狀態條件                        | 按了之後                                       |
| -------------------------- | ------------------------------------- | ---------------------------------------------- |
| `學生詳情`                 | 永遠                                  | 導向 `/admin/students/:id`                     |
| `編輯`                     | 永遠                                  | 開學生表單對話框（編輯模式，**無家長欄**）     |
| `停用`                     | **只在 `student.isActive`**           | 開確認對話框                                   |
| `刪除學生`                 | `!student.hasEnrollments`             | 開確認對話框                                   |
| `已有報名紀錄，無法刪除`   | `student.hasEnrollments` → **disabled** | 不可按（`popup-menu__item--disabled`）        |

> **最後兩列是同一個項目**：`hasEnrollments` 同時換掉標籤與 `disabled`。
> **實測兩種列狀態都取到**（`傅千惠` 有報名 → disabled；`劉靖雯` 無報名 → 可按）。
>
> 這跟 `/admin/courses` 班級選單的「`已有歷史課堂，無法刪除`」是同一個 pattern ——
> **兩頁獨立出現，代表它是這個 repo 的慣例不是個案**（見 [[specs/sitemap/admin/courses]]）。

## 狀態

| 狀態         | 畫面                                                                       |
| ------------ | -------------------------------------------------------------------------- |
| 載入中       | 5 條骨架列（橘帶錨點同時隱藏）                                             |
| 空 + 有篩選  | `找不到符合的學生` / 「請嘗試調整搜尋條件或篩選條件」 + `清除篩選`         |
| 空 + 無篩選  | `尚未有學生資料` / 「學生資料可能由報名流程自動建立，也可以直接在這裡手動新增」 + `新增學生` |
| 載入失敗     | **toast**「載入失敗 / 無法載入學生列表」，**列表維持原狀，不是整頁錯誤態** |

## 子頁面

- [[specs/sitemap/_shared/student-form-dialog]] —— 新增／編輯（3 頁共用，**能力依開啟點而異**）
- [[specs/sitemap/_shared/confirm-dialog]] —— 停用與刪除各一種文案：
  - 停用：`確認停用` / 「確定要停用「X」嗎？停用後該學生將不會出現在預設篩選結果中。」/ `取消`＋`停用`（warn）
  - 刪除：`確認刪除` / 「確定要刪除「X」嗎？此操作無法復原。」/ `取消`＋`刪除`（danger）

## 刻意記下

**`清除篩選` 不清狀態篩選。** 它逐字是 `onSearchChange(''); onGradeChange(null)` ——
狀態下拉留在原值。所以「已停用 + 找不到」的空狀態按了清除篩選之後**可能仍然是空的**。
記成現況，未開 issue（`specs/` 的規格沒講這一欄該不該清，**地圖不調和差異**）。

## 驗證紀錄

- **狀態**：已驗
- **前端版本**：`650d257a`
- **dev server**：自架 `:4210`（**`:4200` 服的是主 checkout**，在那裡量等於量別人的分支）
- **兩向比對**：
  - 畫面可見互動元素 **32**（總 33，唯一不可見的是 `page-actions__cta` 的手機版「新增學生」）
  - 32 = 1（新增學生）+ 1（搜尋）+ 2（兩個下拉）+ 20（每列一顆 `⋮`）+ 8（分頁器）
  - **地圖 ⊆ 畫面、畫面 ⊆ 地圖，0 差異**
- **未驗，附原因**：
  - **停用列的選單內容** —— 展示資料 69 名**全部在籍**（`activeCount === total`），
    沒有 `isActive === false` 的列可取。**繫結存在但這輪量不到，是「未驗」不是「不會發生」。**
  - `・ 停用 M` 那段錨點 —— 同上
  - 兩個空狀態 —— 需要清空資料或造出無結果的篩選組合，本輪未做
  - 送出寫入的一切（建立／儲存／停用／刪除的確認鍵）—— **地圖驗證不做寫入**
- **一件排除掉的「缺陷」**：列動作選單按 Escape 關不掉 → 實際是
  **我把 `keydown` 派送到 `document`**，而 `PopupMenuComponent` 走 CDK 的
  `overlayRef.keydownEvents()`（`popup-menu.component.ts:89-90`），事件要落在 overlay pane 上。
  派到 pane 上**立刻關閉**，點 backdrop 也關得掉。**不是缺陷，是量測方式。**

---
title: 成績登錄（/admin/grades/exams/:type/:id/scores）
summary: 成績登錄的實際 UI 地圖：逐生輸入的表格，考試已結束時整頁唯讀，離開前有未存變更攔截。
category: spec
status: developing
tags: [sitemap, admin]
created: 2026-09-12
updated: 2026-09-13
---

# 成績登錄

<!-- generated:route-facts start —— 這一段由 tools/sitemap 生成，不要手改 -->

**路由**：`/admin/grades/exams/:type/:id/scores`
**角色**：管理員（`admin`）
**選單位置**：**選單不露出**（只能從別頁導過來或直接打網址）
**額外權限**：無

<!-- generated:route-facts end -->

**權限變體**：任何 admin 角色都進得去（不論 `permissions`），頁內的寫入鈕也不會因為缺權限而消失 —— 按下去由 API 擋。逐權限的量測見 [[specs/sitemap/_shared/permission-matrix]]。

**進入方式**：

| 來源                                 | 入口                                        |
| ------------------------------------ | ------------------------------------------- |
| [[specs/sitemap/admin/grades-exams]] | 每場考試 ⋮ ›「進入成績登錄」                |
| 直接網址                             | `:type` 實測值 `academy`；`:id` 是考試 UUID |

**外框**見 [[specs/sitemap/_shared/shell-layout]]。

## ⚠️ 這一頁有路由守衛，而且是原生對話框

`app.routes.ts` 給這條路由掛了 `canDeactivate`，實作在 `score-entry.component.ts:370-373`：

```ts
canDeactivate(): boolean {
  if (!this.dirty()) return true;
  return window.confirm('有尚未儲存的成績變更，確定要離開嗎？');
}
```

**是 `window.confirm()`，不是應用程式自己的對話框。** 兩個後果：

1. 它長的是**瀏覽器原生的樣子**，不受本專案的樣式與 `_shared` 對話框規範管轄
2. **自動化不能觸發它** —— 原生 modal 會凍住整個瀏覽器擴充，後續指令全部收不到。
   本輪因此**刻意沒有讓它出現**（見驗證紀錄）

**只有 router 導覽會經過它。** 整頁重新載入（改網址列）不會 ——
`beforeunload` 全庫零註冊（`grep -rn beforeunload apps/web/src/app` 無命中），
所以**直接打網址離開，未存的成績會靜靜消失，沒有任何提示**。

## 畫面區塊

### 1. 麵包屑

`考試管理 › 成績登錄`

### 2. 考試資訊卡

- 考試名 + **狀態徽章**（實測 `進行中` / `已結束`）
- 一行麵包屑式描述：`<分校> › <課程> › <班級> · <類別> · <科目> · <日期>`
- 四格統計：`已登錄` / `平均` / `最高` / `最低`（沒有成績時後三格顯示 `—`）

### 3. 成績表格

一位學生一列：`學生`、`年級`、`分數 /100`（數字輸入）、`狀態`（下拉，預設 `正常`）、`備註`（文字輸入）。

**手機版是另一套 DOM**（`academy-score-editor__mo…` 的卡片），桌機寬度下在 DOM 裡但不可見。

### 4. 儲存列

**乾淨狀態下整頁一顆按鈕都沒有。** 模板是：

```
@if (!isClosed() && canSave()) { … <span>儲存成績</span> … }
```

而 `canSave = dirty() && !saving() && !isClosed()`（`score-entry.component.ts:177-179`）。

**所以「儲存成績」只在有未存變更、且考試未結束時存在。**

### 資料來源

進頁面打 `GET /api/system-time`、`GET /api/me`，以及該考試的成績資料
（型態依 `:type` 分流 academy / school）。

## 互動元素

| 元素（畫面上的字） | 類型                          | 出現條件                                                             | 按了之後                                                                    |
| ------------------ | ----------------------------- | -------------------------------------------------------------------- | --------------------------------------------------------------------------- |
| 考試管理（麵包屑） | 連結                          | 永遠                                                                 | 回 [[specs/sitemap/admin/grades-exams]]；**有未存變更時會先跳原生 confirm** |
| 分數 /100          | 數字輸入（`role=spinbutton`） | 每位學生一個；**考試已結束時 `disabled`**                            | 改分數 → 頁面進入 dirty                                                     |
| 狀態               | 下拉                          | 每位學生一個；**考試已結束時 `p-disabled` + `pointer-events: none`** | 改出缺考狀態                                                                |
| 備註               | 文字輸入                      | 每位學生一個；**考試已結束時 `disabled`**                            | 填備註                                                                      |
| 儲存成績           | 按鈕                          | **`dirty` 且未 `saving` 且考試未結束**（乾淨狀態下不存在）           | 送出（**未實按** —— 會寫入）                                                |

## 狀態

| 狀態                         | 畫面                                                                                                                                                                            |
| ---------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **考試已結束（`isClosed`）** | **整頁唯讀**：所有分數欄與備註欄 `disabled`，所有狀態下拉 `p-disabled` / `aria-disabled=true` / `pointer-events: none`，**沒有儲存列**。統計格有值（實測 8 已登錄 / 67.3 平均） |
| 考試進行中、尚未輸入         | 欄位可編輯，**沒有任何按鈕**，統計格 `0 / — / — / —`                                                                                                                            |
| 有未存變更                   | 出現「儲存成績」（**未驗**，見下）                                                                                                                                              |
| 儲存中（`saving`）           | 「儲存成績」消失（`canSave` 為 false）—— **未驗**                                                                                                                               |
| 載入中 / 錯誤                | **未驗**                                                                                                                                                                        |
| 無權限                       | 不適用 —— 這條路由沒有 `permission`                                                                                                                                             |

## 驗證紀錄

- **日期**：2026-09-12 ／ 帳號 `admin@demo.clessia.app`（`GET /api/me` → `["admin"]`，量測前後一致）
- **前端版本**：`a4b10a23`（worktree dev server，port 4201）
- **寬度**：1568 CSS px

### 量了兩場考試（兩種狀態）

|                      | 進行中（`第一次小考`，8 位學生） | 已結束（`第一次數學小考`，10 位學生） |
| -------------------- | -------------------------------- | ------------------------------------- |
| DOM 互動元素         | 33                               | 41                                    |
| 不可見（手機版卡片） | 8                                | 10                                    |
| **可見**             | **25**                           | **31**                                |
| 其中**可操作**       | 25                               | **1**（只有麵包屑）                   |

**已結束那一場是這條 `disabled` 規則最乾淨的標本**：31 個可見元素裡 30 個按不動。
只記「可見 31」而不記 `disabled`，地圖會把一頁唯讀的東西寫成可編輯的表單。

| 方向        | 結果                                                   |
| ----------- | ------------------------------------------------------ |
| 地圖 ⊆ 畫面 | 表列元素在兩種狀態下都找得到（「儲存成績」除外，見下） |
| 畫面 ⊆ 地圖 | 兩種狀態的可見元素（25 / 31）全部有對應條目            |

**差異：0 筆**（「儲存成績」已標未驗）。

### ⚠️ 沒有讓 `window.confirm` 出現，這是刻意的

`canDeactivate` 用原生 `window.confirm()`。**原生 modal 會凍住整個瀏覽器擴充**，
之後所有指令都收不到，得由人去瀏覽器手動關掉。所以本輪**沒有製造未存變更後嘗試離開**。

逃生路徑其實是有的（整頁導覽不經過 router guard，且沒有 `beforeunload`），
但既然「儲存成績」本身也沒量到，冒這個險換不到東西。

### 「儲存成績」為什麼沒量到

要讓它出現必須讓頁面進入 `dirty`，而 `dirty` 由子元件的 `dirtyChange` 推上來 ——
**合成的 `input` 事件設不動它**（試過：DOM 的 `value` 變成 88，但沒有任何按鈕出現）。

改用真的點擊 + 鍵盤也沒成功：輸入框**確實可編輯**
（`disabled: false`、`readOnly: false`、`pointer-events: auto`，這是量到的），
但在我的點擊與後續按鍵之間**焦點掉了**（`document.activeElement` 回到 `BODY`）——
這是方法頁坑「探測動作本身會關掉你要看的東西」的一個變形：
**探測用的工具呼叫本身把焦點弄丟了。**

試了三輪就停手（照方法頁「不要鑽洞」）。**條件寫在地圖上是從原始碼讀的，明確標為未驗。**

### 未驗到的

| 項目                             | 原因                                                                   |
| -------------------------------- | ---------------------------------------------------------------------- |
| 「儲存成績」按鈕的實際外觀與位置 | 見上 —— 做不出 dirty 狀態                                              |
| `saving` 中的畫面                | 需要先能儲存                                                           |
| **未存變更的離開攔截**           | 原生 `window.confirm` 會凍住自動化，**刻意不觸發**                     |
| 直接打網址離開會不會掉資料       | 從「沒有 `beforeunload`」推得，**沒有實測**（要實測就得先製造 dirty）  |
| `:type` = `school` 的版面        | 只走過 `academy`；學校考試用的是 `school-score-editor`（另一支子元件） |
| 載入中 / 錯誤                    | 沒有製造手段                                                           |

## 390px

- **量測**：390 × 844 / 768 × 1024 / 1024 × 768 / 1504 × 752 ／ 前端 `3f2197f8`（4200，主 checkout ＝ `origin/main`）／ 身分 `admin@demo.clessia.app`（量測前後各打一次 `GET /api/me`）
- **手段**：同源 iframe 當 viewport；`< 44px` 的「真機」欄是**把 `(pointer: coarse)` 區塊注進去重量一次**得到的，不是推測（方法頁 Phase 2）
- **外框**（頂列、側欄 ↔ 底欄）不屬於這一頁，見 [[specs/sitemap/_shared/shell-layout]]

- **量的那一筆**：`數學小考 第1回`（academy，9 位學生已登錄）—— 挑它是因為**成績列有資料**（方法頁第 0 步）

### 版面怎麼變：**整個編輯器換掉，不是壓扁**

這是整批 admin 裡**差異最大的一頁**。≤768 與 ≥1024 用的是兩個不同的元件：

| 視窗        | 編輯器                                                                                         |
| ----------- | ---------------------------------------------------------------------------------------------- |
| 390 / 768   | `academy-score-editor__mobile-list` —— **一位學生一列**（`332 × 45` 的按鈕），點下去開底部抽屜 |
| 1024 / 1504 | `academy-score-editor__table` —— 真的 `<table>`，**每列三個欄位直接就地編輯**                  |

| 容器                             | 390                | 768                | 1024               | 1504               |
| -------------------------------- | ------------------ | ------------------ | ------------------ | ------------------ |
| `__mobile-list` / `__mobile-row` | **在**             | **在**             | 不存在             | 不存在             |
| `__table`（`display: table`）    | 不存在             | 不存在             | **在**             | **在**             |
| `__table-wrap`                   | —                  | —                  | `overflow-x: auto` | `overflow-x: auto` |
| `score-entry__stats`             | `grid 144px 144px` | `grid 193px 193px` | `flex row wrap`    | `flex row wrap`    |

**桌機是就地打字，手機是一列一列點開** —— 同一個任務兩種操作模型。

### 差集

| 簽名                                                    | 390   | 768   | 1024      | 1504   |
| ------------------------------------------------------- | ----- | ----- | --------- | ------ |
| `button.academy-score-editor__mobile-row`               | **9** | **9** | 0         | 0      |
| `input.academy-score-editor__score-field`（分數）       | 0     | 0     | **9**     | **9**  |
| `span.p-select-label` / `div.p-select-dropdown`（狀態） | 0     | 0     | **9 / 9** | 9 / 9  |
| `input.academy-score-editor__notes-input`（備註）       | 0     | 0     | **9**     | **9**  |
| 合計                                                    | 10    | 10    | **37**    | **37** |

桌機一屏 27 個輸入欄位，手機 9 個列按鈕。**四個寬度都有的只有麵包屑一項。**

### 手機的底部抽屜（實按過，已關閉並輪詢斷言）

點 `__mobile-row` 會開一支 `p-drawer`（`390 × 327`），裡面四個東西：

| 元素                                     | 390 尺寸   |
| ---------------------------------------- | ---------- |
| 關閉鍵 `p-button-icon-only`              | `40 × 40`  |
| 分數 `p-inputnumber-input`               | `344 × 38` |
| 狀態 `p-select-dropdown`                 | `40 × 40`  |
| 備註 `academy-score-editor__sheet-notes` | `344 × 38` |

**抽屜四個控制項有三個高度 < 44px。**（沒有注入 coarse 重量 —— 那要在抽屜開著時做，
而抽屜的開關本身就是一次互動，這一輪沒有把兩件事疊在一起。）

- **關閉鍵實按有效**：按下去到 `.p-drawer` 從 DOM 消失**花了 1818ms** ——
  跟方法頁坑 9 量到的「1.8–2.0 秒」一致，**不要用固定等待去斷言**
- `.p-drawer-mask` 關閉後仍殘留 —— **那是坑 12（背景分頁動畫不跑），是環境不是產品**

> ⚠️ **這一頁的 `canDeactivate` 用原生 `window.confirm`**
> （`score-entry.component.ts:372`，labor-1 的警告：原生 modal 會凍住整個瀏覽器擴充）。
> **這一輪沒有觸發到它**：量測是整個 iframe 銷毀重建、不走 router 導覽，
> 而且沒有在任何欄位打字，表單不會 dirty。**下一個人要動這一頁時先讀那一條。**

### 水平溢出

**無** —— 四個寬度的 `documentElement.scrollWidth` 都等於 `innerWidth`。
桌機那張表有自己的 `overflow-x: auto` 容器（`__table-wrap`），那是設計不是溢出。

### 觸控目標 < 44px（390）

**主清單上只有一個**：`breadcrumb__item--link` `56 × 21`（coarse 後不變）。
**9 個 `__mobile-row` 是 `332 × 45`**，本來就夠。

（抽屜內的三個 < 44 見上一節。）

### 鍵盤可達性

- 全頁**沒有正數 `tabindex`**；390 下 **Tab 站 10 個 ＝ 可見互動元素 10 個**
- **`cursor: pointer` 但 Tab 不到的元素：0 個**

### 未驗與原因

| 項目                                              | 原因                                                      |
| ------------------------------------------------- | --------------------------------------------------------- |
| 抽屜內控制項注入 coarse 之後的尺寸                | 要在抽屜開著時注入，這一輪沒把兩件事疊在一起              |
| 儲存成績 / 任何寫入                               | **會寫入**，零寫入邊界                                    |
| `school` 型別的成績登錄（這一輪量的是 `academy`） | 兩種型別走不同的子元件（`school-score-editor`），要另外量 |
| `Escape` 關抽屜                                   | 需要前景分頁                                              |

### 2026-09-13 #848 之後（版面健全度重掃）

> 上面那幾段**沒有錯，只是量不到版面壞掉** —— 兩向比對量的是「元素在不在、可不可見」，
> 而使用者在 390 實機看到的是字被拆開、按鈕被擠、首欄不是名稱。
> 方法頁補了第 4 條手機專屬檢查，這一節是它跑出來的結果（修在 #860）。
>
> **重掃環境**：390 × 844 ／ 前端 `fix/848-mobile-layout-390`（4201，自己的 worktree）
> ／ 身分 `admin@demo.clessia.app`。展開鍵那一欄一樣是**把 `(pointer: coarse)` 注進去量的**。

| 症狀 | 修前 | 修後 |
| --- | --- | --- |
| 水平內距 | **90px ＝ 23.1%**（`score-entry` 16 ＋ `__header-card` 17，左右各計） | 58px ＝ 14.9% |

⚠️ **這一頁差點漏掉**：我從「根 padding 是 `--space-6`」的 grep 清單去找要改的檔，
而 `score-entry` 是 `--space-4`，**所以它不在清單上**。選擇器太窄 ——
是重掃把它揪出來的，不是靜態掃描。

## 載入中 / 錯誤

- **量測**：390 × 844 ／ 主 checkout 的 dev server（port 4200）`7e9da649` ／
  `admin@demo.clessia.app`（permissions `["*"]`，量測前後各打一次 `/api/me`）
- **手段**：同一個 XHR 包裝 —— 錯誤態把 URL 從 `:8787` 改指到沒人監聽的 `:8799`，
  載入中把 `send` 用 `setTimeout` 延後 3 秒（**只影響那一個 iframe，不動 8787**）。
  方法全文見[方法頁 Phase 2-D](../README.md)
- **證據**：每一輪都確認攔截清單不是空的，**請求數 0 的一律作廢**（例外要自己附正控）
- **導航**：`ng.ɵgetRouterInstance(...)` 拿 Router 本人再 `navigateByUrl()`

**量測用的路徑**：`/admin/grades/exams/academy/70000000-0000-0000-0000-000000000005/scores`
（數學分級測驗，12 筆成績）。**這一頁先前量不到，因為它有兩個路由參數。**

### 載入中（3 秒延遲）

| skeleton | spinner | `<main>` 互動元素 | 判定 |
| --- | --- | --- | --- |
| 0 | **2**（`div.score-entry__loading 334×114` + `i.pi.pi-spinner 16×16`） | 1 | ✅ 誠實（兩段都有文案） |

主體逐字「**載入考試資料中...**」；考試資料回來之後換成「**載入學生名單...**」——
**兩段取數各有自己的載入文案**，這一頁是全站少數把鏈式載入講清楚的。

⚠️ spinner 的 `animation-name` 是 `fa-spin` —— 看得見，不會轉（坑 12）。

### 錯誤（所有 API 都失敗）

| 判定 | 重試鈕 | toast | 攔到的請求數 |
| --- | --- | --- | --- |
| ⚠️ **失敗時把你踢出這一頁** | 落地頁上有 | 1（落地頁的） | 5 |

**網址從 `/admin/grades/exams/academy/…/scores` 變成 `/admin/grades/exams`**
（`router().url` 與 `location.pathname` 兩邊都確認過）。落地的考試管理頁自己顯示
「**載入失敗／沒有讀到考試資料，可能是連線問題**」+ 重試鈕，而畫面上唯一的 toast 是
「**學校清單載入失敗 請稍後重試**」—— **那是落地頁的另一支請求，不是你剛才想開的那場考試。**

機制在原始碼上（`score-entry.component.ts:210-218`，`:228-241` 是學校考試的同一份）：

```ts
error: () => {
  this.messageService.add({ severity: 'error', summary: '載入失敗', detail: '無法載入考試資料' });
  this.loading.set(false);
  this.router.navigate(['/admin/grades/exams']);   // ← 把使用者導走
},
```

> **這是刻意的設計，不是 bug** —— 但它的代價要記下來：**離開的那一刻使用者失去了重試的對象**。
> 落地頁的「重試」重的是考試**清單**，不是他點進去的那一場。

### 未驗與原因

| 項目 | 原因 |
| --- | --- |
| 「載入失敗／無法載入考試資料」那則 toast | **沒有捕捉到** —— 讀取時畫面上只剩落地頁自己的 toast。機制在原始碼上確定存在，但**畫面上有沒有真的出現過、停留多久，是未驗的** |
| 重試鈕按了會不會真的重打 | 需要真滑鼠（坑 12） |
| 其他寬度 | 只量 390 |

## 寫入實按（Phase 2-B 第 6 輪，2026-09-13）

環境與基線見 [[specs/sitemap/admin/grades-exams]] 同名一節。

**兩種成績登錄器是兩套 UI，不是同一套換資料**：

| | 補習班（`academy-score-editor`） | 學校（`school-score-editor` + `score-edit-dialog`） |
| --- | --- | --- |
| 版面 | **一張表**，每列一名學生（分數／狀態／備註都在列上） | **每名學生一張卡片**，點開對話框逐**科目**登分 |
| 儲存 | 右下 **FAB「儲存成績」** | 對話框內的 `儲存` |
| 儲存鈕何時存在 | `@if (!isClosed() && canSave())` —— **有 dirty 才渲染**（`score-entry.component.html:105`） | 對話框一開就在 |
| 一次寫幾列 | 每名學生 1 列 `academy_scores` | 每名學生 × 每科目 1 列 `school_scores` |
| 篩選 | 無 | 分校 / 年級 / 全部・待登錄・已登錄・缺考・補考 |

實按結果：補習班登 3 名學生 → `academy_scores` +3；
學校登 1 名學生 × 3 科 → `school_scores` +3。兩邊都 toast「已更新 3 筆成績」，
audit 各一筆 `*.scores.upsert`，`details` 是 `{"affected": 3}`。

### 分數欄是 `p-inputnumber`，而「原生 setter + 派 input」**在這裡有效**

方法頁把 `p-inputnumber` 跟 `p-datepicker` 一起列在「合成值無效」那一欄。
**本輪實測：分數欄吃得下** —— `setNative(el, '85')` 之後派一個 `blur`，
`dirtyCount()` 立刻變 1、FAB 出現、送出去的值也是 85（DB 確認）。

> **訂正範圍**：無效的是 `p-datepicker`（含 `[timeOnly]`）。
> `p-inputnumber` 這個用法（`[(ngModel)]` + blur 提交）吃得下合成值。
> ⚠️ 但**不要因此推論所有 `p-inputnumber` 都行** —— 差別可能在有沒有 `blur` 這一步，
> 本輪只驗了這兩處（成績欄、學校考試的學年度）。

### 補習班成績表的一個假象

填進去之後 `input.value` 是 `85`，**但 `main` 的 `innerText` 讀不到它**
（`p-inputnumber` 的值在 `<input value>` 上，不在文字節點裡）。
**用 `innerText` 驗「填進去了沒有」會得到一個假的空結果** —— 讀 `input.value`，或直接查 DB。

### 未驗

| 項目 | 原因 |
| --- | --- |
| 狀態下拉（正常／缺考／補考…）的寫入 | 只驗了 `正常`；改狀態會走 `status !== 'scored'` 那條分支，**那是唯一能繞過「清空無效」的路**，但它寫進去的仍是一列成績 |
| 學校登錄頁的四個篩選 | 唯讀，非寫入鈕 |
| 已結束考試的成績頁 | `isClosed()` 時 FAB 不渲染，本輪沒在結束狀態下開過成績頁 |

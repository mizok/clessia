---
title: 我要報名（/enrollment）
summary: /enrollment 的實際 UI 地圖：目前是純佔位殼，<main> 內零互動元素，只有標題與「開發中」提示。
category: spec
status: developing
tags: [sitemap, public]
created: 2026-09-12
updated: 2026-09-13
---

# 我要報名

<!-- generated:route-facts start —— 這一段由 tools/sitemap 生成，不要手改 -->

**路由**：`/enrollment`
**角色**：公開（未登入可進）
**選單位置**：（無群組）我要報名
**額外權限**：無

<!-- generated:route-facts end -->

**進入方式**：公開頁外框的「我要報名」連結 / **`/login` 在 `?error=signup_disabled` 時出現的
「還沒報名？前往報名」連結**（見 [[specs/sitemap/public/login]]）/ 直接網址。
**沒有任何 guard** —— 登入與否都進得去。

**外框**見 [[specs/sitemap/_shared/public-shell]]。

> ## ⚠️ 這一頁是佔位殼
>
> 模板 9 行、元件 11 行且不注入任何東西。`<main>` 裡**一個互動元素都沒有** ——
> 沒有報名表單、沒有欄位、沒有送出鍵。
>
> **這一點對改版特別重要**：`/login` 有一條連結指過來，而那條連結的情境是
> 「這個 LINE 帳號還沒有被登記」—— **被導過來的人在這裡什麼都做不了。**
> 現況如此，不是遺漏記錄。

## 畫面區塊

### 1. 標頭（`auth-content__header`）

三行純文字，寫死在模板裡，**沒有資料來源**：

- 眉標 `報名服務`
- 標題 `我要報名`
- 副標 `填寫表單完成報名流程`

### 2. 開發中提示

`app-inline-notice`，`severity="info"`、`detail="報名頁面開發中"`、**`[dismissible]="false"`**
（所以連 ✕ 都不渲染）。

## 互動元素

**沒有。** `<main>` 內可見互動元素 0 個（實測）。

| 元素（畫面上的字） | 類型 | 出現條件 | 按了之後 |
| ------------------ | ---- | -------- | -------- |
| （無）             | —    | —        | —        |

## 狀態

| 狀態     | 畫面                                                      |
| -------- | --------------------------------------------------------- |
| 唯一狀態 | 標頭三行 + 藍底「報名頁面開發中」提示。**沒有其他狀態。** |
| 空 / 載入中 / 錯誤 | 不適用 —— 這一頁不取資料、沒有非同步            |
| 無權限   | 不適用 —— 路由沒有任何 guard                              |

## 驗證紀錄

- **日期**：2026-09-12 ／ **前端版本**：`9f08061a`（主 checkout 的 dev server，port 4200）
- **視窗**：1504 × 695
- **登入狀態**：帶著一個 admin session（這一頁無 guard、元件不注入 `AuthService`）

### 兩向比對

|                          | 數量  |
| ------------------------ | ----- |
| `<main>` 內 DOM 互動元素 | 0     |
| 其中不可見               | 0     |
| **可見**                 | **0** |

`<main>` 的全部文字：`報名服務 我要報名 填寫表單完成報名流程 報名頁面開發中`

| 方向        | 結果                       |
| ----------- | -------------------------- |
| 地圖 ⊆ 畫面 | 地圖沒有列任何元素，恆真 ✓ |
| 畫面 ⊆ 地圖 | 畫面上 0 個可見互動元素 ✓  |

**差異：0 筆。**

### 未驗到的

| 項目             | 原因                     |
| ---------------- | ------------------------ |
| 手機寬度         | 本輪只量 1504px 桌機寬度 |

## 390px

- **量測**：390 × 844 ／ 768 × 1024 ／ 1024 × 768 ／ 1504 × 752（同一輪四個寬度）
- **前端**：主 checkout 的 dev server（port 4200），量測期間從 `40c84b20` 前進到 `fce2aefd` ——
  **`git diff 40c84b20 fce2aefd -- apps/web packages` 是空的**，前端零改動
- **身分**：`admin@demo.clessia.app`（量測前後各打一次 `GET /api/me`，兩次都是 `roles:["admin"]`）
- **手段**：同源 iframe 當 viewport（`resize_window` 在這台機器上是空操作，見方法頁 Phase 2）

**這一頁是佔位殼，四個寬度完全一樣**：`<main>` 內 0 個互動元素、0 個區塊隨寬度改變、
水平溢出四個寬度皆無（`documentElement.scrollWidth === innerWidth`）、
`<main>` 內可 Tab 元素 0 個、觸控目標 0 個。

**版面、差集、觸控目標、Tab 序列全部由外框產生** → [[specs/sitemap/_shared/public-shell]]。
（那一頁有四個寬度的品牌面尺寸與堆疊／並排切換點。**這裡不抄**——抄了會漂，c11。）

### 未驗與原因

| 項目                   | 原因                                                                                                                                                                                                                                    |
| ---------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 焦點環看不看得見       | **這個環境量不到**：`document.hasFocus()` 恆 `false` → `:focus` 偽類不成立 → `outline` 永遠回 `none`。宿主頁與 iframe 對照過（兩邊一樣），`osascript activate` 之後也沒變。專案**零自訂 `:focus` 樣式**（CSSOM 命中的 5 條全是 PrimeNG 的），所以吃瀏覽器預設環 |
| `Escape` / 真的 Tab 鍵 | 需要前景分頁（方法頁坑 12）                                                                                                                                                                                                            |
| `(1024, 1280]`         | 四個量測寬度跳過了它；這一頁沒有自己的斷點，外框的最近斷點是 1024（兩側都量了）                                                                                                                                                        |

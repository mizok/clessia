---
title: 選擇角色（/select-role）
summary: /select-role 的實際 UI 地圖：一個薄殼路由，長相在彈窗裡；單一角色會直接轉走、零角色只留一句說明。
category: spec
status: developing
tags: [sitemap, public]
created: 2026-09-12
updated: 2026-09-13
---

# 選擇角色

<!-- generated:route-facts start —— 這一段由 tools/sitemap 生成，不要手改 -->

**路由**：`/select-role`
**角色**：要登入，但不綁角色（`authGuard`）
**選單位置**：**選單不露出**（只能從別頁導過來或直接打網址）
**額外權限**：無

<!-- generated:route-facts end -->

**進入方式**：

1. 登入後的落地點（`guest.guard` / `role.guard` / LINE OAuth 的 `callbackURL` 都指這裡）
2. `/link-line` 的「稍後再說」
3. 直接網址（要先登入）

**它不在公開頁外框的四條連結裡**（`showInMenu: false`）。

**外框**見 [[specs/sitemap/_shared/public-shell]]。

## ⚠️ 這一頁「三個角色帳號會看到三種完全不同的東西」

元件（`select-role.component.ts`）在建構時就分岔，**而分岔點是使用者有幾個角色**：

| 角色數     | 發生什麼                                                                                        |
| ---------- | ----------------------------------------------------------------------------------------------- |
| **1 個**   | **不開彈窗，直接 `navigateToRoleShell()` 導去該角色的 shell** —— 這一頁使用者根本看不到         |
| **0 個**   | **不開彈窗、也不導向**，留在這一頁顯示一句 `這個帳號目前沒有可用的身分，請聯絡補習班櫃檯開通。` |
| **≥ 2 個** | `await import()` 進 PrimeNG 的 `DialogService` + `RolePickerComponent`，開一個**關不掉**的彈窗  |

**所以「這一頁長什麼樣」這個問題沒有單一答案**，改版時三條路都要算進去。

## 畫面區塊

### 1. 字標（唯一恆存在的東西）

`Clessia`（`select-role__wordmark`），純文字，**不是連結**。

### 2. 沒有身分的說明（條件式）

`@if (noRoles())` —— `auth.roles().length === 0` 時才渲染，純文字一段。

元件註解寫明為什麼要有它：彈窗不開的話使用者只會看到一個**只剩字標的空白頁**，
那跟「卡在載入中」長得一模一樣。

## 互動元素

**`<main>` 內沒有任何互動元素。** 字標不是連結，說明文字不是連結。
唯一的互動在子頁面（彈窗）裡。

| 元素（畫面上的字） | 類型 | 出現條件 | 按了之後 |
| ------------------ | ---- | -------- | -------- |
| （無）             | —    | —        | —        |

## 狀態

| 狀態       | 畫面                                                                    |
| ---------- | ----------------------------------------------------------------------- |
| 1 個角色   | **看不到這一頁** —— 立刻被導去該角色的 shell（實測）                    |
| 0 個角色   | 字標 + `這個帳號目前沒有可用的身分，請聯絡補習班櫃檯開通。`（**未驗**） |
| ≥ 2 個角色 | 字標 + 蓋在上面的角色選擇彈窗（**未驗**，見下）                         |
| 載入中     | 彈窗是 `await import()` 進來的，載入期間畫面上只有字標（**未驗**）      |
| 未登入     | **進不來** —— `authGuard` 擋下（**未驗**）                              |

## 子頁面：角色選擇彈窗（RolePicker）

> **2026-09-13 已補驗**（seed `#746` 造出多重角色帳號之後）。
> 下面的內容原本是讀原始碼寫的，**現在每一條都對照過畫面**，差異在該處標出。

**開啟方式**：不是按出來的 —— 使用者有 ≥ 2 個角色時，進頁就自己開。

**它刻意關不掉**：`closable: false`、`closeOnEscape: false`、`dismissableMask: false`、
`showHeader: false`、`modal: true`。理由是**沒選角色就沒有下一步**。
寬度 `400px`，`breakpoints: { '640px': '90%' }`（憲法 c6：不用 `vw`）。

內容（`role-picker.component.html`）：

- 眉標 `選擇身分`
- 標題 `{{ displayName() }}，你好`
- 副標 `這個帳號有多個身分，請選擇要進入的介面`
- 一排角色按鈕，每個是：圖示 + 角色名 + 一行說明 + 右側 `pi-chevron-right`；
  目前的 `activeRole()` 那一顆帶 `--active`
- 按下去 → 關窗 → `auth.navigateToRoleShell(role)`

## 驗證紀錄

- **日期**：2026-09-12 ／ **前端版本**：`9f08061a`（主 checkout 的 dev server，port 4200）
- **視窗**：1504 × 695
- **角色帳號**：`admin@demo.clessia.app`（Demo Admin，**單一角色**）

### 實測到的：單一角色這條路

直接開 `http://localhost:4200/select-role`，3 秒後量：

```
url        = http://localhost:4200/admin/dashboard
.p-dialog          → null
.role-picker-dialog → null
.select-role       → null
```

`.select-role` 連 DOM 都不在 —— **元件已經導航離開，這一頁對單一角色使用者等於不存在**。
從 `/link-line` 按「稍後再說」進來也是同一個結果（見 [[specs/sitemap/public/link-line]]）。

### 兩向比對

單一角色這條路上，`<main>` 內可見互動元素 **0 個**（頁面已被換掉）。
地圖的互動元素表也是空的。

| 方向        | 結果                                   |
| ----------- | -------------------------------------- |
| 地圖 ⊆ 畫面 | 地圖沒有列任何 `<main>` 內元素，恆真 ✓ |
| 畫面 ⊆ 地圖 | 0 個可見互動元素 ✓                     |

**差異：0 筆**（**但只涵蓋三條路裡的一條** —— 另外兩條見下）。

### 2026-09-13 補驗：彈窗

`#746` 的 seed 給 `teacher0001@demo.clessia.app` 加上 `admin` 角色（權限只有 `view_reports`），
**它是全庫第一個多重角色帳號** —— 在那之前這個彈窗**任何帳號都開不出來**。

| 量到什麼                   | 結果                                                                                                    |
| -------------------------- | ------------------------------------------------------------------------------------------------------- |
| 彈窗                       | `.p-dialog.role-picker-dialog` ✓                                                                        |
| 眉標                       | `選擇身分` ✓                                                                                            |
| 標題                       | `王宥廷，你好`（`displayName()`）✓                                                                      |
| 副標                       | `這個帳號有多個身分，請選擇要進入的介面` ✓                                                              |
| 選項                       | **2 個**：`管理者 跨分校管理、系統設定、日常營運`（帶 `--active`）、`任課老師 課表、點名、學生學習紀錄` |
| `showHeader: false` 的效果 | **沒有 `.p-dialog-header`、沒有 `.p-dialog-close-button`** ✓                                            |
| 遮罩                       | `.p-dialog-mask.p-overlay-mask` 存在 ✓                                                                  |
| `<main>` 內可見互動元素    | **0**（殼只有字標，所有互動都在彈窗裡）                                                                 |

#### 「刻意關不掉」也驗了

| 試了什麼                                                        | 結果                               |
| --------------------------------------------------------------- | ---------------------------------- |
| 合成 `Escape`（`document` / `body` / `.p-dialog` 三個目標都送） | **彈窗還在**                       |
| 點遮罩                                                          | **彈窗還在**                       |
| 左上角 `elementFromPoint(20,20)`                                | 回傳的是遮罩，**底下的頁面碰不到** |

跟 `closable:false` / `closeOnEscape:false` / `dismissableMask:false` 一致。

> ⚠️ **`Escape` 是合成事件** —— 當下分頁在背景（`document.visibilityState === 'hidden'`），
> 真鍵盤送不進頁面（[[specs/sitemap/README|方法頁]]坑 12）。
> **「合成 Escape 關不掉」對 `closeOnEscape: false` 成立，但不等於真鍵盤也試過。**

### 還是沒驗到：0 角色的那句說明

`select count(*) from ba_user u where not exists (select 1 from user_roles r where r.user_id = u.id)` ——
本機唯一那個 0 角色的 `ba_user` 是 `0912-345-678@phone.internal`，
**而它是用手機號當帳號的既有資料，不是為這件事造的**。要驗得用它登入一次。
`#746` 的 seed 刻意沒有造 0 角色帳號（那會撞 c2 的 `ba_*` 豁免上限）。

### 未驗到的

| 項目                             | 原因                                                                       |
| -------------------------------- | -------------------------------------------------------------------------- |
| ~~≥ 2 角色的彈窗（整個子頁面）~~ | **已於 2026-09-13 補驗**（`teacher0001@`，見上）                           |
| 0 角色的說明文字                 | 同上 —— 每個 demo 使用者至少有一個角色                                     |
| ~~彈窗「關不掉」~~               | **已補驗**（合成 Escape 與點遮罩都關不掉）；**真鍵盤仍未試** —— 分頁在背景 |
| ~~`displayName()` 實際顯示什麼~~ | **已補驗**：`王宥廷，你好`                                                 |
| 未登入被 `authGuard` 擋掉        | 本輪全程帶著 session                                                       |
| 手機寬度（`640px` 斷點）         | 本輪只量 1504px 桌機寬度                                                   |

## 390px

- **量測**：390 × 844 ／ 768 × 1024 ／ 1024 × 768 ／ 1504 × 752，**外加 640 / 641 的邊界對照**（見下）
- **前端**：主 checkout 的 dev server（port 4200），量測期間從 `40c84b20` 前進到 `fce2aefd` ——
  `git diff 40c84b20 fce2aefd -- apps/web packages` 是空的，前端零改動
- **身分**：`teacher0001@demo.clessia.app`（`roles:["teacher","admin"]`，**多重角色** ——
  量測前後各打一次 `GET /api/me`，兩次相同）。四個寬度的 `location.pathname` 都確認是
  `/select-role`，沒有被 `navigateToRoleShell()` 導走
- **手段**：同源 iframe 當 viewport

> **這一頁非量多重角色帳號不可。** admin 是單一角色，開 `/select-role` 會**直接導去
> `/admin/dashboard`** —— 而元素清單看起來完全正常（只是那是儀表板的元素）。
> 本輪一開始就踩過一次，是 `env().url` 擋下來的。

### 版面怎麼變

**`<main>` 永遠只有字標**（四個寬度的 `main.innerText` 都是 `Clessia`）。
會變的是彈窗，而**它有自己的斷點，不是專案的 `_breakpoints.scss`**：

`role-picker` 開窗時帶 `breakpoints: { '640px': '90%' }`（PrimeNG 自己的機制）。實測：

| 視窗寬 | `.p-dialog` | `.role-picker__option` | 走哪一條 |
| ------ | ----------- | ---------------------- | -------- |
| 390    | `351 × 287` | `301 × 62`             | **90%**（`390 × 0.9 = 351`） |
| **640**| `576 × 287` | `526 × 62`             | **90%**（`640 × 0.9 = 576`）—— **仍在斷點內** |
| **641**| `400 × 287` | `350 × 62`             | 固定 `400px` —— **跳出斷點** |
| 768    | `400 × 287` | `350 × 62`             | 固定 `400px` |
| 1024   | `400 × 287` | `350 × 62`             | 固定 `400px` |
| 1504   | `400 × 287` | `350 × 62`             | 固定 `400px` |

**邊界含等號**（`max-width: 640px`），跟專案自己的 `respond-to` 同向 —— 640 是手機側、641 是桌機側。
**640 / 641 那兩列是特地為了分辨邊界量的**：只量 390 與 768 的話兩邊都對，
但「斷點在哪一格」這件事**完全沒有被證明**。

⚠️ **`640 × 0.9 = 576` 比桌機的 `400` 還寬。** 這不是「手機版壓縮」，
是兩套獨立的寬度規則在 640 這一點交接，而交接處是**不連續的**（576 → 400）。

### 差集（1504 ↔ 390）

**零差異** —— 元素集合四個寬度完全相同（`<main>` 0 個、彈窗 2 個角色按鈕），只有尺寸不同。

### 水平溢出

**無。** 四個寬度 `documentElement.scrollWidth === innerWidth`，撐出界的元素 0 個。
整頁**不產生捲動容器**（彈窗內容 287 高，390 × 844 塞得下）。

### 觸控目標 < 44px

**角色按鈕 0 筆** —— 兩顆在所有寬度都是 `62` 高。

> ⚠️ **取樣器會多報兩個 `span.p-hidden-accessible.p-hidden-focusable`（`1 × 1`）** ——
> 那是 PrimeNG 的焦點陷阱哨兵，不是使用者看得到的東西。
> 照方法頁那條「多報看得見、漏報看不見」的判準**留著不濾**，在這裡標明就好。
> （`<main>` 的可見互動元素因此在 body scope 下算出 9 個：外框 5 + 哨兵 2 + 角色 2。）

### 鍵盤可達性

- 全頁**沒有正數 `tabindex`** → Tab 序列 = DOM 序。
- **Tab 序列**：外框 5 條 → 哨兵 → `管理者` → `任課老師` → 哨兵。
  兩個哨兵把彈窗夾在中間，那正是 PrimeNG 焦點陷阱的形狀。
- **click-only 而鍵盤到不了**：`<main>` 內 **0 筆**；彈窗內 0 筆
  （列出的 `span.role-picker__option-*` 全部在 `<button>` 裡面，**不是缺口**）。

### 未驗與原因

| 項目                       | 原因                                                                                                                                            |
| -------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| **按下角色按鈕之後**       | 那會關窗並導向該角色的 shell —— **本輪沒按**（會離開這一頁，而我還要量它的其他寬度）。Phase 1 已在 1504 驗過這條路 |
| 0 個角色那條路（390）      | 沒有零角色的帳號。**與寬度無關**（是一段純文字）                                                                                                 |
| 彈窗 `await import()` 的載入態 | 動態載入在 390 一樣快到量不到；而**動畫在這個環境不會跑**（方法頁坑 12）                                                                     |
| 焦點環看不看得見           | **這個環境量不到**：`document.hasFocus()` 恆 `false` → `:focus` 偽類不成立 → `outline` 回 `none`。專案零自訂 `:focus` 樣式                       |
| `Escape`                   | 這個彈窗 `closeOnEscape: false`，**本來就不該關** —— 但「按了真的沒關」仍然未驗（需要真鍵盤）                                                   |
| `(1024, 1280]`             | 四個量測寬度跳過了它；彈窗 641 起就是固定 `400px`，**不需要補量**                                                                               |

## 載入中 / 錯誤

- **量測**：390 × 844 ／ dev server（port 4200）`543e5eda` ／ 身分 `parent03@demo.clessia.app`
- **手段**：把 XHR 的 URL 從 `:8787` 改指到沒人監聽的 `:8799`，再用 **SPA 導航**（點外框連結）
  進入這一頁；方法見[方法頁 Phase 2-D](../README.md)

**未量。** 本輪身分 `parent03@demo.clessia.app` 是**單一角色**，開 `/select-role`
會**直接被導去 `/parent/dashboard`**（實測，與 Phase 1 一致）—— 量不到這一頁。

要量它得用多重角色帳號（`teacher0001@demo.clessia.app`）。

**預期**：這一頁的資料來自 `auth.roles()`（已經在 session 裡），彈窗是 `await import()`
進來的 —— **沒有列表 API**，所以應該沒有錯誤態。**但那是推的，不是量的。**

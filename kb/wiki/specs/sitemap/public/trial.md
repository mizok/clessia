---
title: 試聽申請（/trial）
summary: /trial 的實際 UI 地圖：目前是純佔位殼，<main> 內零互動元素，只有標題與「開發中」提示。
category: spec
status: developing
tags: [sitemap, public]
created: 2026-09-12
updated: 2026-09-12
---

# 試聽申請

<!-- generated:route-facts start —— 這一段由 tools/sitemap 生成，不要手改 -->

**路由**：`/trial`
**角色**：公開（未登入可進）
**選單位置**：（無群組）試聽申請
**額外權限**：無

<!-- generated:route-facts end -->

**進入方式**：公開頁外框的「試聽申請」連結 / 直接網址。**沒有任何 guard** —— 登入與否都進得去。

**外框**見 [[specs/sitemap/_shared/public-shell]]（橘色品牌面 + 四條連結）。

> ## ⚠️ 這一頁是佔位殼
>
> 模板只有 9 行（`trial.component.html`），元件只有 11 行且**沒有任何 signal、沒有任何注入**。
> `<main>` 裡**一個互動元素都沒有** —— 沒有表單、沒有欄位、沒有送出鍵。
>
> **地圖記的是現在的樣子。** 不要照 `specs/` 底下的試聽規格來讀這一頁，
> 那份記的是「應該」，這一頁記的是「現在」。

## 畫面區塊

### 1. 標頭（`auth-content__header`）

三行純文字，全部寫死在模板裡，**沒有資料來源**：

- 眉標 `試聽服務`
- 標題 `試聽申請`
- 副標 `填寫表單預約試聽課程`

### 2. 開發中提示

一個 `app-inline-notice`，`severity="info"`、`detail="試聽頁面開發中"`、
**`[dismissible]="false"`**。

`dismissible` 是 `false` 表示**連關閉的 ✕ 都不會渲染**
（`inline-notice.component.html` 用 `@if (dismissible())` 包住那顆按鈕）——
這就是 `<main>` 內互動元素為 0 的原因。

## 互動元素

**沒有。** `<main>` 內可見互動元素 0 個（實測，見驗證紀錄）。

| 元素（畫面上的字） | 類型 | 出現條件 | 按了之後 |
| ------------------ | ---- | -------- | -------- |
| （無）             | —    | —        | —        |

## 狀態

| 狀態     | 畫面                                                       |
| -------- | ---------------------------------------------------------- |
| 唯一狀態 | 標頭三行 + 藍底「試聽頁面開發中」提示。**沒有其他狀態。**  |
| 空狀態   | 不適用 —— 這一頁不取資料                                   |
| 載入中   | 不適用 —— 沒有非同步                                       |
| 錯誤     | 不適用 —— 沒有請求會失敗                                   |
| 無權限   | 不適用 —— 路由沒有任何 guard                               |

## 驗證紀錄

- **日期**：2026-09-12 ／ **前端版本**：`9f08061a`（主 checkout 的 dev server，port 4200）
- **視窗**：1504 × 695
- **登入狀態**：帶著一個 admin session（**這一頁無 guard、元件不注入 `AuthService`，
  所以登入狀態不影響它**；外框亦同，已與外框那一頁交叉比對過）

### 兩向比對

|                          | 數量  |
| ------------------------ | ----- |
| `<main>` 內 DOM 互動元素 | 0     |
| 其中不可見               | 0     |
| **可見**                 | **0** |

`<main>` 的全部文字：`試聽服務 試聽申請 填寫表單預約試聽課程 試聽頁面開發中`

| 方向        | 結果                           |
| ----------- | ------------------------------ |
| 地圖 ⊆ 畫面 | 地圖沒有列任何元素，恆真 ✓     |
| 畫面 ⊆ 地圖 | 畫面上 0 個可見互動元素 ✓      |

**差異：0 筆。**

### 未驗到的

| 項目               | 原因                                     |
| ------------------ | ---------------------------------------- |
| 手機寬度           | 本輪只量 1504px 桌機寬度                 |
| 登出狀態下的長相   | 元件不讀登入狀態，且路由無 guard；未另量 |

---
title: 綁定 LINE（/link-line）
summary: /link-line 的實際 UI 地圖：一次性連結兌換後的落地頁，兩顆按鈕（綁定 LINE / 稍後再說）。需要登入。
category: spec
status: developing
tags: [sitemap, public]
created: 2026-09-12
updated: 2026-09-13
---

# 綁定 LINE

<!-- generated:route-facts start —— 這一段由 tools/sitemap 生成，不要手改 -->

**路由**：`/link-line`
**角色**：要登入，但不綁角色（`authGuard`）
**選單位置**：**選單不露出**（只能從別頁導過來或直接打網址）
**額外權限**：無

<!-- generated:route-facts end -->

**進入方式**：

1. **一次性連結（magic link）兌換完的落地頁** —— 這是主要入口
   （`.../api/auth/magic-link/verify?token=...&callbackURL=http://localhost:4200/link-line`）
2. 直接網址（但要先登入）

**它不在公開頁外框的四條連結裡**（`showInMenu: false`）。

**外框**見 [[specs/sitemap/_shared/public-shell]]（橘色品牌面 + 白面；跟 `/login` 同一個外框）。

## 畫面區塊

### 1. 標頭

- 眉標 `最後一步`
- 標題 `綁定 LINE`

### 2. 說明文字

`綁定之後，以後用 LINE 就能直接登入，不必再向補習班索取連結。`

### 3. 錯誤提示（條件式）

`app-inline-notice severity="error"`，**只在 `error()` 非 null 時存在**。
可關閉（`dismissible` 預設 true），關掉會把 `error` 設回 `null`。
兩種來源：`auth.linkLine()` 回傳的錯誤訊息，或整個呼叫拋例外時的
`無法連線到 LINE，請稍後再試`。

### 4. 兩顆按鈕

綠底主鍵 `綁定 LINE 帳號`，底下是文字鍵 `稍後再說`。

**沒有資料來源** —— 這一頁不打任何列表 API，只在按下去時才呼叫 `auth.linkLine()`。

## 互動元素

| 元素（畫面上的字） | 類型            | 出現條件                               | 按了之後                                                                                   |
| ------------------ | --------------- | -------------------------------------- | ------------------------------------------------------------------------------------------ |
| 綁定 LINE 帳號     | 按鈕（主要）    | 永遠（`submitting()` 時 `disabled`）   | 呼叫 `auth.linkLine()` → **整頁離開去 LINE 授權頁**。（**未驗，見下**）                     |
| 稍後再說           | 按鈕（文字鍵）  | 永遠（`submitting()` 時 `disabled`）   | `router.navigate(['/select-role'])`。**實按驗過** —— 單一角色帳號會被 `/select-role` 再轉一次，最後落在該角色的 shell |
| ✕（關閉訊息）      | 按鈕            | **只在錯誤提示存在時**                 | 關掉錯誤提示（`error.set(null)`）（**未驗** —— 沒有製造錯誤的手段）                         |

### 送出中的樣子

`submitting()` 為 true 時，**兩顆按鈕都 `disabled`**，且主鍵的內容換成
旋轉圖示 + `前往 LINE...`（文字從「綁定 LINE 帳號」變掉）。

## 狀態

| 狀態     | 畫面                                                                              |
| -------- | --------------------------------------------------------------------------------- |
| 預設     | 標頭 + 說明 + 兩顆按鈕，**沒有錯誤提示**（實測就是這個狀態）                      |
| 送出中   | 兩鍵 disabled、主鍵變成 `前往 LINE...`（**未驗** —— 會真的跳去 LINE）             |
| 錯誤     | 上方多一條可關閉的紅色 `app-inline-notice`（**未驗**）                            |
| 載入中   | 不適用 —— 進頁不取資料                                                            |
| 未登入   | **進不來** —— `authGuard` 擋下（**未驗**，本輪全程帶著 session）                  |

## ⚠️ 「稍後再說」不是死路，是刻意留的合法出口

元件註解寫得很清楚：破窗進來的人（供應商幫客戶處理問題）不會想把自己的 LINE 綁上客戶的帳號，
**「沒綁定」是合法且永久的狀態**。改版時不要把它當成「還沒做完的跳過鍵」拿掉。

## 驗證紀錄

- **日期**：2026-09-12 ／ **前端版本**：`9f08061a`（主 checkout 的 dev server，port 4200）
- **視窗**：1504 × 695
- **角色帳號**：`admin@demo.clessia.app`（Demo Admin，單一角色）
  —— **這一頁的內容與角色無關**：元件只注入 `AuthService` 與 `Router`，模板裡沒有任何
  依角色分支的東西，也不顯示使用者名稱

### 兩向比對

|                          | 數量  |
| ------------------------ | ----- |
| `<main>` 內 DOM 互動元素 | 2     |
| 其中不可見               | 0     |
| **可見**                 | **2** |

兩個是：`button.link-line__btn`（綁定 LINE 帳號，`disabled=false`）、
`button.link-line__skip`（稍後再說，`disabled=false`）。

`<main>` 全部文字：
`最後一步 綁定 LINE 綁定之後，以後用 LINE 就能直接登入，不必再向補習班索取連結。 綁定 LINE 帳號 稍後再說`

| 方向        | 結果                                                                       |
| ----------- | -------------------------------------------------------------------------- |
| 地圖 ⊆ 畫面 | 3 項中 2 項直接可見；第 3 項（錯誤提示的 ✕）是條件式，已標未驗 ✓            |
| 畫面 ⊆ 地圖 | 2 個可見元素全部有列 ✓                                                     |

**差異：0 筆。**

### 導向驗證（實按一次）

按「稍後再說」→ 網址列最終停在 `http://localhost:4200/admin/dashboard`，
`document.querySelector('.p-dialog')` 為 `null`。

**這是兩段導向的結果，不是一段**：`稍後再說` 只把人送到 `/select-role`，
而 `/select-role` 對單一角色帳號**不開彈窗、直接再導去該角色的 shell**
（見 [[specs/sitemap/public/select-role]]）。這個帳號是 admin，所以落在 `/admin/dashboard`。
**家長帳號按同一顆鍵會落在 `/parent/dashboard`。**

### 未驗到的

| 項目                 | 原因                                                                             |
| -------------------- | -------------------------------------------------------------------------------- |
| **按「綁定 LINE 帳號」** | **寫入類動作，一律不按** —— 它會把一個真的 LINE 帳號綁到這個 demo 使用者上，而且會整頁離開到 line.me |
| 送出中的畫面         | 同上，看不到就要先按那顆鍵                                                       |
| 錯誤提示與它的 ✕     | 沒有製造 `linkLine()` 失敗的手段                                                 |
| 未登入時被 `authGuard` 擋掉的實際行為 | 本輪全程帶著 session；留給驗 `/login` 的那一輪               |
| 手機寬度             | 本輪只量 1504px 桌機寬度                                                         |

## 390px

- **量測**：390 × 844 ／ 768 × 1024 ／ 1024 × 768 ／ 1504 × 752（同一輪四個寬度）
- **前端**：主 checkout 的 dev server（port 4200），量測期間從 `40c84b20` 前進到 `fce2aefd` ——
  **`git diff 40c84b20 fce2aefd -- apps/web packages` 是空的**，前端零改動
- **身分**：`admin@demo.clessia.app`（量測前後各打一次 `GET /api/me`，兩次都是 `roles:["admin"]`）
  —— 這一頁的內容與角色無關（Phase 1 已查證）
- **手段**：同源 iframe 當 viewport

### 版面怎麼變

**`<main>` 內沒有任何斷點**。兩顆按鈕從頭到尾都是 `width: 100%` 貼著卡片走，
所以「版面變化」全部來自卡片寬度：

| 寬度 | 兩顆按鈕   |
| ---- | ---------- |
| 390  | `350 × 44` |
| 768  | `480 × 44` |
| 1024 | `480 × 44` |
| 1504 | `480 × 44` |

**390 是唯一不是 480 的那個，而它不是斷點造成的。** 按鈕是 `width: 100%`
（`link-line.component.scss:21`），外層表單是 `max-width: 480px`
（`features/public/shared/_auth-form.scss:68`）—— 所以 390 下是被視窗夾住的 350，
**768 起就已經觸到那個上限**，於是 768 / 1024 / 1504 在這一頁完全相同。
（`350 = 390 − 40`，兩側各 20 的外框內距；**這一句是從兩個量到的數字推的**，
沒有回去對哪一條 padding 規則。）

外框的版面切換（上下堆疊 ↔ 左右並排）見 [[specs/sitemap/_shared/public-shell]]。

### 差集（1504 ↔ 390）

**`<main>` 內零差異** —— 兩顆按鈕四個寬度都在、都可見、都不 disabled。
只有尺寸不同（上表）。外框的差集見 `_shared/public-shell`。

### 水平溢出

**無。** 四個寬度 `documentElement.scrollWidth === innerWidth`，撐出界的元素 0 個。
整頁**不產生捲動容器**（`main.public-shell__main` 的 `scrollHeight === clientHeight`）。

### 觸控目標 < 44px

**`<main>` 內 0 筆** —— 兩顆按鈕在四個寬度都是 `44` 高，**不靠 `(pointer: coarse)`**：
高度是寫死的（`link-line.component.scss:22` 的 `height: 44px`、`:48` 的 `min-height: 44px`），
而 CSSOM 查過兩顆都沒有被任何 coarse 規則命中。

> 這一頁是本輪 public 六頁裡**唯一不需要 coarse 規則就達標**的一頁。
> 外框那四條 `public-shell__link` 是 `23` 高且沒有 coarse 接住 —— 見 `_shared/public-shell`。

### 鍵盤可達性

- 全頁**沒有正數 `tabindex`** → Tab 序列 = DOM 序。
- **`<main>` 內的 Tab 序列**：`綁定 LINE 帳號` → `稍後再說`（2 個，與 DOM 序一致，四個寬度相同）。
- **click-only 而鍵盤到不了**：`<main>` 內 **0 筆**。

### 未驗與原因

| 項目                         | 原因                                                                                                                                                                                        |
| ---------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 錯誤提示與它的 ✕ 在 390 的樣子 | 同 Phase 1 —— 沒有製造 `linkLine()` 失敗的手段。**它是 `<main>` 內唯一還沒被量到的元素**                                                                                                   |
| 送出中（兩鍵 disabled）      | 要按下「綁定 LINE 帳號」才看得到，而那是寫入類動作（會整頁離開到 line.me）                                                                                                                 |
| 焦點環看不看得見             | **這個環境量不到**：`document.hasFocus()` 恆 `false` → `:focus` 偽類不成立 → `outline` 回 `none`。宿主頁與 iframe 對照過（兩邊一樣），`osascript activate` 後也沒變。專案零自訂 `:focus` 樣式 |
| `Escape` / 真的 Tab 鍵       | 需要前景分頁（方法頁坑 12）                                                                                                                                                                |
| `(1024, 1280]`               | 四個量測寬度跳過了它；這一頁 768 起就已經是最終樣子，**不需要補量**                                                                                                                        |

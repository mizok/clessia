---
title: 公告收件匣（家長端 / 老師端共用）
summary: 通知中心的本體：未讀計數、全部標為已讀、逐則展開；展開會順帶把該則標成已讀（寫入）。
category: spec
status: developing
tags: [sitemap, _shared, parent, teacher]
created: 2026-09-12
updated: 2026-09-13
---

# 公告收件匣（AnnouncementInbox）

**元件**：`@shared/components/announcement-inbox/announcement-inbox.component`

**兩個頁面用它**（`grep -rl app-announcement-inbox`，排除元件自己）：

- [[specs/sitemap/parent/notifications]]（`/parent/notifications`）
- [[specs/sitemap/teacher/notifications]]（`/teacher/notifications`）—— **已補驗（labor-4）**

兩邊的頁面元件都只有一行 `<app-announcement-inbox [heading]="page().label" />`，
**除了標題文字以外完全相同**。所以它獨立成這一頁，兩邊只放連結（c11）。

> ✅ **老師端已補驗（labor-4，2026-09-12）** —— **版面與互動元素與家長端完全一致，
> 資料不同**：老師端 4 則（1 未讀），其中 3 則帶 `· 示範分校01`。
> 上一版擔心的正是這個，而擔心是對的：**`campusName` 那一行家長端根本沒出現過。**
> 逐項見 [[specs/sitemap/teacher/notifications]]。

## 畫面區塊

### 1. 標頭

- 標題：由外面傳進來的 `heading()`（家長端是 `通知中心`）
- **未讀徽章**：`N 則未讀` —— **只在 `unread() > 0` 時存在**
- **「全部標為已讀」按鈕** —— **只在 `unread() > 0` 時存在**

> 元件註解寫了為什麼是條件式：「**沒有未讀就沒有可做的事 —— 不放一顆按了不會發生任何事的按鈕**」。

### 2. 內容

四選一：

| 條件                           | 畫面                                             |
| ------------------------------ | ------------------------------------------------ |
| `loading()`                    | 三條 `skeleton-bar`（`aria-label="公告載入中"`） |
| `loadError()`                  | `查詢失敗，請稍後再試。`                         |
| `announcements().length === 0` | `目前沒有公告。`                                 |
| 其餘                           | `<ul>` 清單                                      |

每一則是一顆按鈕（`.announcement-inbox__summary`），內容：

- **未讀紅點**（`.announcement-inbox__dot`，`aria-label="未讀"`）—— **只在 `!item.isRead` 時**
- 標題
- 右側：`MM/dd HH:mm` 的發布時間 + `· <校區名>`（**只在有 `campusName` 時**）

未讀的 `<li>` 會加上 `--unread` 修飾類別。

**展開的內容**（`.announcement-inbox__body`）**只在 `openId() === item.id` 時渲染**：
內文一段 + `—— <發布者>`（**只在有 `createdByName` 時**）。

**資料來源**：`GET /api/announcements/inbox`（實測攔到，200）。

## 互動元素

| 元素（畫面上的字） | 類型           | 出現條件           | `disabled` | 按了之後                                   |
| ------------------ | -------------- | ------------------ | ---------- | ------------------------------------------ |
| 全部標為已讀       | 按鈕           | **`unread() > 0`** | 無繫結     | `markAllRead()` —— **寫入，未按**          |
| `<公告標題>`       | 按鈕（toggle） | 每則一顆           | 無繫結     | 展開／收合該則（**並把它標成已讀**，見下） |

**這個元件的模板沒有任何 `[disabled]` 繫結**（`grep` 零命中）——
所以「無繫結」是結構性的，這兩顆按鈕不會進入 disabled 狀態。

**一次只能展開一則**（`openId()` 是單一值，不是集合）——
展開第二則會收掉第一則。**已在老師端驗到**（4 則公告，`.announcement-inbox__body`
自始至終只有 1 個）。

## ⚠️ 展開一則公告會把它標成已讀 —— 這是寫入

**實測**：按下公告標題之前，標頭是 `通知中心 1 則未讀 全部標為已讀`、該則帶著未讀紅點。
按一次之後：內文展開，**同時未讀徽章、「全部標為已讀」按鈕、未讀紅點三個都消失了**。

也就是說 `toggle(item)` 不只是展開，它還送出了「標為已讀」。

> **對 UI 地圖的意義**：這一頁**沒有純讀取的展開動作**。
> 「只開啟不寫入」在這個元件上做不到 —— 想看內文就一定會改狀態。
>
> **本輪確實因此寫入了一筆**（`parent01` 的那則 `#636 兩端走通驗證` 被標成已讀）。
> 那是驗證「展開之後長什麼樣」的必要代價，寫出來而不是藏起來。
> **「全部標為已讀」沒有按** —— 那一顆的影響範圍是全部，且展開單則已經證明了寫入行為。

改版時要知道：**未讀狀態是被「看一眼」消耗掉的，不是被明確的「標為已讀」消耗掉的。**

## 驗證紀錄

- **日期**：2026-09-12 ／ **前端版本**：`9f08061a`（主 checkout 的 dev server，port 4200）
- **視窗**：1504 × 695
- **角色帳號**：`parent01@demo.clessia.app`（家長 林志明）
- **身分斷言**：量測前後各打一次 `GET /api/me`，兩次都是 `parent01@demo.clessia.app / ["parent"]`
- **資料狀態**：1 則公告，未讀

### 兩向比對（在 `/parent/notifications` 上量）

|                          | 數量  |
| ------------------------ | ----- |
| `<main>` 內 DOM 互動元素 | 2     |
| 其中不可見               | 0     |
| **可見**                 | **2** |

`button.announcement-inbox__mark-all`（`全部標為已讀`）、
`button.announcement-inbox__summary`（`#636 兩端走通驗證 140231 09/12 14:02`）。

`<main>` 全文（展開前）：`通知中心 1 則未讀 全部標為已讀 #636 兩端走通驗證 140231 09/12 14:02`

| 方向        | 結果                   |
| ----------- | ---------------------- |
| 地圖 ⊆ 畫面 | 2 類元素都量到 ✓       |
| 畫面 ⊆ 地圖 | 2 個可見元素全部有列 ✓ |

**差異：0 筆。**

### 展開一則（實按一次）

| 量的東西                        | 展開前     | 展開後                                                    |
| ------------------------------- | ---------- | --------------------------------------------------------- |
| `.announcement-inbox__body`     | 不存在     | 存在                                                      |
| `.announcement-inbox__text`     | —          | `這是 review-steward 為 #636 發的驗證公告,發給全體家長。` |
| `.announcement-inbox__author`   | —          | 存在（發布者名字）                                        |
| `.announcement-inbox__badge`    | `1 則未讀` | **不存在**                                                |
| `.announcement-inbox__mark-all` | 存在       | **不存在**                                                |
| `.announcement-inbox__dot`      | 存在       | **不存在**                                                |

### 未驗到的

| 項目                                   | 原因                                                     |
| -------------------------------------- | -------------------------------------------------------- |
| **「全部標為已讀」**                   | **寫入類動作，不按**（展開單則已經證明了寫入行為）       |
| ~~再按一次收合~~                       | **已在老師端驗到** —— `body` 變 0，徽章與紅點不變        |
| ~~一次只能展開一則~~                   | **已在老師端驗到**（4 則公告）                           |
| `目前沒有公告。` 空狀態                | 本機有公告；要驗得換一個收不到公告的帳號                 |
| `查詢失敗，請稍後再試。`               | 沒有製造手段                                             |
| 載入中的 skeleton                      | 本機回應太快                                             |
| ~~帶 `campusName` 的那一行~~           | **已在老師端驗到**（`· 示範分校01`）                     |
| ~~老師端（`/teacher/notifications`）~~ | **已補驗** —— 見 [[specs/sitemap/teacher/notifications]] |
| 手機寬度                               | 本輪只量 1504px 桌機寬度                                 |

## 補驗紀錄（labor-4，2026-09-12）

- **帳號**：`teacher0003@demo.clessia.app`（老師 張品妍）
- **前端版本**：port 4202（labor-4 worktree），`7622b0c4` ／ **寬度**：1504 CSS px
- **在 `/teacher/notifications` 上量**：DOM 5、可見 5（`全部標為已讀` + 4 則公告），**差異 0 筆**

**怎麼在不寫入的前提下補驗那三項**：老師端 4 則裡有 **3 則已讀**，
而「展開會標成已讀」只對未讀的那一則成立 —— **展開已讀的公告不產生任何寫入**。
於是拿第 2、3 則（都已讀）來驗：

| 動作              | 結果                                                        |
| ----------------- | ----------------------------------------------------------- |
| 展開第 2 則       | 內文出現；未讀徽章仍 `1 則未讀`、紅點仍 1 顆 → **沒有寫入** |
| 接著展開第 3 則   | `body` 仍只有 1 個、內容換掉 → **一次只能展開一則**         |
| 對第 3 則再按一次 | `body` 變 0，徽章與紅點不動 → **再按一次是收合**            |

也量到了 `.announcement-inbox__author` 的實際樣子：`—— Demo Admin`。

> **這個做法可以再用**：要驗一個「會寫入的 toggle」的**非寫入面向**，
> 找一筆**已經在目標狀態**的資料去按它 —— 行為一樣，但沒有副作用。

## 390px

- **量測**：390 × 844 與 1504 × 752（對話框只記「版面怎麼變」，不重抄元素表）
- **前端**：主 checkout 的 dev server（port 4200），`543e5eda`
- **手段**：同源 iframe 當 viewport；**開啟一律用 `element.click()`（合成事件，坑 6 的等級標記）**，
  關閉後用輪詢斷言消失（坑 9），疊層時比對**數量**不是 `=== null`

### 版面怎麼變

**它不是對話框，是通知中心頁的內容**，所以沒有「全螢幕／關閉鈕」這一組問題。

**兩端各量過一次，四個寬度的尺寸逐字相同**：

| 元素 | 390 | 768 | 1024 | 1504 |
| --- | --- | --- | --- | --- |
| `button.announcement-inbox__summary` | `334 × 47` | `432 × 47` | `560 × 47` | `560 × 47` |
| `button.announcement-inbox__mark-all` | `108 × 44` | 同左 | 同左 | 同左 |

**1024 起達到上限 `560`。**

> 兩端的量測見 [[specs/sitemap/parent/notifications]] 與 [[specs/sitemap/teacher/notifications]]
> ——**兩邊各量一次而數字完全一致，所以這支共用元件的響應式行為是量過的，不是假設的。**

### 水平溢出 / 觸控目標 / 鍵盤可達性

- **溢出**：無（四個寬度）
- **觸控目標**：**0 筆低於 44**（`44` 與 `47`）——本輪 `_shared` 八支裡**唯一全部達標的一支**
- **Tab 序列**：`全部標為已讀` → 公告摘要（2 個，四寬度相同）；click-only 0 筆

### 未驗與原因

| 項目 | 原因 |
| --- | --- |
| **按「全部標為已讀」／展開摘要** | **寫入類動作**（會改 `announcement_reads`），一律不按 |
| 未讀 0 則 / 多則 | 本機兩端各只有 1 則未讀。**要造更多只能寫入** |

## 載入中 / 錯誤

- **量測**：390 × 844 ／ 主 checkout 的 dev server（port 4200）`7e9da649`
- **手段**：同一個 XHR 包裝 —— 錯誤態把 URL 從 `:8787` 改指到沒人監聽的 `:8799`，
  載入中把 `send` 用 `setTimeout` 延後 3 秒（**只影響那一個 iframe，不動 8787、零寫入**）。
  方法全文見[方法頁 Phase 2-D](../README.md)
- **證據**：每一輪都確認攔截清單不是空的，**請求數 0 的一律作廢**（例外要自己附正控）

**三個開啟點**：`/admin/notifications`、`/teacher/notifications`、`/parent/notifications`
（`grep -rl AnnouncementInbox`，排除元件自己與 spec）。三頁都量過，結果一致。

攔到 `GET /api/announcements/inbox` 一支。

### 載入中（3 秒延遲）

| skeleton | spinner | 判定 |
| --- | --- | --- |
| **4**（`div.skeleton-list 334×120` + `span.skeleton-bar 286×16` ×3） | 0 | ✅ 誠實 |

延遲期間**「N 則未讀」與「全部標為已讀」都不在** —— 不會出現「先說沒有未讀、資料回來
又冒出一則」那種閃動。骨架的 `aria-label` 是 `公告載入中`。

⚠️ `animation-name: skeleton-wave` —— 看得見，波紋不動（坑 12）。

### 錯誤（API 失敗）

| 判定 | 重試鈕 | toast | 攔到的請求數 |
| --- | --- | --- | --- |
| ✅ **誠實**（四態齊全） | 否 | 0 | 1 |

模板是完整的四態（`announcement-inbox.component.html:18-28`）：

| 狀態 | 畫面 |
| --- | --- |
| `loading()` | 骨架 |
| `loadError()` | **「查詢失敗，請稍後再試。」** |
| `announcements().length === 0` | 「目前沒有公告。」 |
| 其餘 | 清單 |

> **「查詢失敗」與「目前沒有公告」是兩句不同的話** —— 這正是
> [[specs/sitemap/_shared/subject-manager]] 與 [[specs/sitemap/_shared/audit-log-dialog]] 缺的那一格。
> 三支都是 `shared/components/` 底下的元件，**寫法不一致是現況**。

### 未驗與原因

| 項目 | 原因 |
| --- | --- |
| 「全部標為已讀」失敗的樣子 | 寫入類動作，一律不按 |
| 展開單則公告時的取數 | 展開是純前端（`openId()`），沒有額外請求 |

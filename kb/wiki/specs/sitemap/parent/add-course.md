---
title: 加選課程（/parent/add-course）
summary: /parent/add-course 的實際 UI 地圖：目前是 EmptyState 佔位頁，<main> 內零互動元素。
category: spec
status: developing
tags: [sitemap, parent]
created: 2026-09-12
updated: 2026-09-13
---

# 加選課程

<!-- generated:route-facts start —— 這一段由 tools/sitemap 生成，不要手改 -->

**路由**：`/parent/add-course`
**角色**：家長（`parent`）
**選單位置**：行政服務 › 加選課程
**額外權限**：無

<!-- generated:route-facts end -->

**進入方式**：選單「行政服務 › 加選課程」／ 直接網址

**外框**見 [[specs/sitemap/_shared/shell-layout]]（家長的選單是 2 個不分組項目 + 3 個群組，
右上角色徽章是「家長」）。

> ## ⚠️ 這一頁是佔位頁
>
> 整支元件只有一個 `<app-empty-state>`，**沒有注入任何 service、沒有打任何 API**：
>
> ```ts
> template: `
>   <app-empty-state
>     [icon]="'pi ' + page().icon"
>     [title]="page().label"
>     description="這個功能還在準備中，完成後就會出現在這裡。"
>   />
> `,
> ```
>
> **家長端有六頁長得一模一樣**（見下方「同形的六頁」）——
> 六頁的模板現在**逐字相同**，差別完全來自投影進去的 `page()`。
>
> 這裡記的是**現在的樣子**，不是應該有什麼。

## 畫面區塊

置中的空狀態，三個部分由上而下：

1. 圖示 `pi pi-plus-circle` —— 來自 `page().icon`，跟選單用的是同一個（#708）
2. 標題 **加選課程** —— 來自 `page().label`，也就是 `RoutesCatalog` 的路由標籤
3. 說明 `這個功能還在準備中，完成後就會出現在這裡。`

`EmptyStateComponent` 還有一個 `<ng-content>` 的動作區（`.empty-state__action`），
**這一頁沒有投影任何東西進去**，所以它是空的 —— 這就是互動元素為 0 的原因。

**沒有資料來源。** 這一頁不打任何 API。

## 互動元素

**沒有。** `<main>` 內可見互動元素 0 個（實測）。

| 元素（畫面上的字） | 類型 | 出現條件 | 按了之後 |
| ------------------ | ---- | -------- | -------- |
| （無）             | —    | —        | —        |

> **注意這一頁連孩子切換器都沒有。** 家長端有內容的四頁（儀表板、到班紀錄、成績查閱、
> 繳費紀錄）頂上都有一條橘色 `app-page-band` 帶著
> [[specs/sitemap/_shared/parent-child-switcher|孩子切換器]]，**這六頁沒有** ——
> 它們連「現在在看哪個孩子」都不顯示。

## 狀態

| 狀態               | 畫面                                            |
| ------------------ | ----------------------------------------------- |
| 唯一狀態           | 上面那三行。**沒有其他狀態。**                  |
| 空 / 載入中 / 錯誤 | 不適用 —— 不取資料、沒有非同步                  |
| 無權限             | 這條路由沒有 `permission`；`roleGuard` 擋非家長 |

## 驗證紀錄

- **日期**：2026-09-12 ／ **前端版本**：`9f08061a`（主 checkout 的 dev server，port 4200）
  - `origin/main` 當時已前進到 `2684a1e0`（#691 搜尋管線），但 `git show --stat` 確認
    **它沒有動到 `features/parent` 或 `features/public` 任何檔案**
- **視窗**：1504 × 695
- **角色帳號**：`parent01@demo.clessia.app`（家長 林志明，3 個孩子）

### 兩向比對

|                          | 數量  |
| ------------------------ | ----- |
| `<main>` 內 DOM 互動元素 | 0     |
| 其中不可見               | 0     |
| **可見**                 | **0** |

`<main>` 的全部文字：`加選課程 這個功能還在準備中，完成後就會出現在這裡。`
空狀態圖示的實際 class：`pi pi-plus-circle`（**#708 修正前**；現在是 `pi pi-plus-circle`）

| 方向        | 結果                       |
| ----------- | -------------------------- |
| 地圖 ⊆ 畫面 | 地圖沒有列任何元素，恆真 ✓ |
| 畫面 ⊆ 地圖 | 畫面上 0 個可見互動元素 ✓  |

**差異：0 筆。**

### 圖示從路由來（#708 之後）

`[icon]="'pi ' + page().icon"` —— **跟標題同源**（標題是 `page().label`）。
#708 之前圖示是寫死的，六頁裡有四頁跟選單不一致；那段歷史記在下表底下的引言裡。

## 同形的六頁

這六頁的元件**逐字同構**（只有投影進去的 `page()` 不同）：

| 路由                 | 標題     | 圖示（= `RoutesCatalog` 的選單圖示） |
| -------------------- | -------- | ------------------------------------ |
| `/parent/schedule`   | 課表查看 | `pi-calendar-plus`                   |
| `/parent/trial`      | 試聽申請 | `pi-headphones`                      |
| `/parent/enrollment` | 報名申請 | `pi-user-plus`                       |
| `/parent/add-course` | 加選課程 | `pi-plus-circle`                     |
| `/parent/renewal`    | 續課資訊 | `pi-refresh`                         |
| `/parent/meals`      | 餐費紀錄 | `pi-dollar`                          |

**六頁全部實測過，每一頁 `<main>` 內可見互動元素都是 0。**
那個「0」有一支測試釘著（`parent-placeholder-pages.spec.ts`）——
之後有人往 `EmptyState` 的 `<ng-content>` 投影一顆按鈕進去，**地圖會過期而測試會紅**。

> **2026-09-12 之前這張表有兩欄，而且四列打著 ❗**（#708）：
> 圖示原本寫死在元件的 `template` 字串裡，`schedule` / `trial` / `enrollment` / `meals`
> 四頁跟選單圖示不同 —— **選單上是耳機、點進去是星星**。
> 現在六頁都是 `[icon]="'pi ' + page().icon"`，**兩欄合併成一欄**，
> 而「它們相同」由上面那支測試守著，不是靠下一個人記得。

### 未驗到的

| 項目     | 原因                     |
| -------- | ------------------------ |
| 手機寬度 | 本輪只量 1504px 桌機寬度 |

## 390px

- **量測**：390 × 844 ／ 768 × 1024 ／ 1024 × 768 ／ 1504 × 752
- **前端**：主 checkout 的 dev server（port 4200），`fce2aefd`
- **身分**：`parent03@demo.clessia.app`（張建國，`roles:["parent"]`，3 個孩子）——
  量測前後各打一次 `GET /api/me`，兩次相同
- **取樣器**：#768 的四道濾網版，**外加 ④a 的修正**（`inView()` 以捲動容器的 rect
  為可視區，不是視窗）與 ④b 的 `visibleFast`。兩者都在本輪查出來，已回寫方法頁
- **手段**：同源 iframe 當 viewport

### 版面怎麼變

**不適用 —— 這一頁是佔位殼。** `<main>` 內 **0 個互動元素**，四個寬度都一樣
（390 與 1504 實測，768 / 1024 未量：**沒有元素就沒有斷點**）。

### 差集 / 水平溢出 / 觸控目標 / 鍵盤可達性

- **差集**：零（兩邊都是 0 個）
- **水平溢出**：無（`documentElement.scrollWidth === innerWidth`）
- **觸控目標**：`<main>` 內不產生任何觸控目標
- **鍵盤可達性**：`<main>` 內可 Tab 元素 0 個、click-only 0 個

**外框**（側欄 ↔ 底欄的切換）見 [[specs/sitemap/_shared/shell-layout]]，這裡不抄（c11）。

> ⚠️ **這一頁連孩子切換器都沒有。** 家長端只有 `dashboard` / `attendance` / `grades` /
> `payments` 四頁掛 `app-child-scope-gate`，其餘六個佔位頁與通知中心都沒有 ——
> 所以「家長端每頁都看得到自己在看哪個孩子」**不成立**。

### 未驗與原因

| 項目                   | 原因                                                                                                     |
| ---------------------- | -------------------------------------------------------------------------------------------------------- |
| 焦點環看不看得見       | **這個環境量不到**：`document.hasFocus()` 恆 `false` → `:focus` 偽類不成立 → `outline` 回 `none`         |
| `Escape` / 真的 Tab 鍵 | 需要前景分頁（方法頁坑 12）                                                                              |
| 768 / 1024             | `<main>` 內 0 個元素，沒有量                                                                             |

## 載入中 / 錯誤

**不適用 —— 這一頁不取任何資料。** 實測：從別頁 SPA 導航過來並攔截 XHR，
**攔到的請求數是 0**，所以沒有載入中、也沒有錯誤態可量。

（量法與其餘家長端頁面相同，見 [[specs/sitemap/parent/attendance]]。）

---
title: 繳費紀錄（/parent/payments）
summary: /parent/payments 的實際 UI 地圖：待付款／已付款兩段清單、頁首待繳金額錨點、帳單明細抽屜。
category: spec
status: developing
tags: [sitemap, parent]
created: 2026-09-12
updated: 2026-09-13
---

# 繳費紀錄

<!-- generated:route-facts start —— 這一段由 tools/sitemap 生成，不要手改 -->

**路由**：`/parent/payments`
**角色**：家長（`parent`）
**選單位置**：生活與繳費 › 繳費紀錄
**額外權限**：無

<!-- generated:route-facts end -->

**進入方式**：選單「生活與繳費 › 繳費紀錄」／ 直接網址

**外框**見 [[specs/sitemap/_shared/shell-layout]]。

## 畫面區塊

### 1. 橘色頁首（`app-page-band`）

- 左上：[[specs/sitemap/_shared/parent-child-switcher|孩子切換器]]
- 標題：`繳費紀錄`
- 右側（`bandAside`）：**待繳金額錨點** —— 大數字 + `元待繳`，底下 `N 張待付款`
  **只在 `!loading() && invoices().length > 0` 時存在**

### 2. 內容區

四選一（`@if` 鏈，順序就是判斷順序）：

| 條件                                   | 畫面                                                 |
| -------------------------------------- | ---------------------------------------------------- |
| `loading() && invoices().length === 0` | 三條 `skeleton-bar`                                  |
| `failed()`                             | 空狀態 `載入失敗 / 沒有讀到繳費紀錄，可能是連線問題` |
| `invoices().length === 0`              | 空狀態 `目前沒有帳單紀錄`（無說明文字）              |
| 其餘                                   | 待付款區 + 已付款區 + 「載入更多」                   |

有資料時分兩段，**各自只在該段非空時才渲染**：

- **待付款**（`groups().pending`）：每列是一顆按鈕 —— `#<id 前 8 碼>`、金額（`total - netPaid`）、
  `期限 <dueDate>`（**只在有 `dueDate` 時**）、狀態標籤
- **已付款**（`groups().paid`）：每列 `#<id 前 8 碼>`、`total`、最後一次付款日期
  （**只在查得到時**）、`已付款`

### 3. 帳單明細抽屜

`p-drawer`，`position="bottom"`、`modal`、`appendTo="body"`。見下方子頁面。

**資料來源**：`GET /api/me/billing?childId=<id>&page=1&pageSize=20`（實測攔到）。

## 互動元素

| 元素（畫面上的字） | 類型           | 出現條件                                                         | 按了之後                                           |
| ------------------ | -------------- | ---------------------------------------------------------------- | -------------------------------------------------- |
| `<孩子名字>` ⌄     | 按鈕（toggle） | 見 [[specs/sitemap/_shared/parent-child-switcher]]               | 開孩子下拉                                         |
| 帳單列（待付款）   | 按鈕           | `groups().pending.length > 0`                                    | 開「帳單明細」抽屜                                 |
| 帳單列（已付款）   | 按鈕           | `groups().paid.length > 0`                                       | 開「帳單明細」抽屜（**未驗**，本機沒有已付款帳單） |
| 載入更多           | 按鈕           | **`hasMore()`**（`loading()` 時 `disabled`，文字變 `載入中...`） | 取下一頁（**未驗**，本機資料不足一頁）             |

**`<main>` 內沒有其他東西** —— 沒有篩選器、沒有排序、沒有搜尋。

## 狀態

| 狀態         | 畫面                                            | 驗了嗎                                        |
| ------------ | ----------------------------------------------- | --------------------------------------------- |
| 有待付款帳單 | 頁首錨點 + 待付款區                             | ✓（王柏翰，2 張）                             |
| 完全沒有帳單 | `目前沒有帳單紀錄`，**頁首沒有錨點**            | ✓（林子璿，0 張）                             |
| 載入失敗     | `載入失敗 / 沒有讀到繳費紀錄，可能是連線問題`   | ⚠️ 只在**別席把我踢掉**時意外看到，見驗證紀錄 |
| 載入中       | 三條 skeleton                                   | **未驗**（本機太快）                          |
| 有已付款帳單 | 多一段「已付款」                                | **未驗**（本機三個孩子的帳單全是 `unpaid`）   |
| 超過一頁     | 底部「載入更多」                                | **未驗**                                      |
| 無權限       | 這條路由沒有 `permission`；`roleGuard` 擋非家長 | 未驗                                          |

## 子頁面：帳單明細（抽屜）

**開啟方式**：按任何一列帳單。
**選擇器**：`.p-drawer`（**不是 `.p-dialog`**）。實測開啟時的完整 class 是
`drawer-auto p-component p-drawer p-drawer-bottom p-drawer-open payments__detail-sheet`。

從畫面底部升起，內容（實測 `#413224d0`）：

| 區塊     | 實測內容                                                                                                               |
| -------- | ---------------------------------------------------------------------------------------------------------------------- |
| 標題     | `帳單 #413224d0`（id 前 8 碼）                                                                                         |
| 狀態     | `待付款`                                                                                                               |
| 明細列   | `學費 3,600 元`（一列一個 `invoice.items`，型別標籤 + 金額；有 `periodMonth` 時加括號）                                |
| 合計     | `應繳 3,600 元`                                                                                                        |
| 繳費期限 | `繳費期限 2026-09-21`（**只在有 `dueDate` 時**）                                                                       |
| 付款說明 | **只在 `status !== 'paid'` 時**：`付款方式：現金或轉帳，帳戶資訊請洽補習班行政人員。` / `完成付款後請通知補習班確認。` |
| 付款紀錄 | **只在 `status === 'paid'` 時**：逐筆日期 / 方式 / 金額（**未驗**）                                                    |

**抽屜裡有兩顆按鈕**：PrimeNG 自己的圓形 ✕（`p-button-icon-only p-button-rounded`）
與自訂的 `關閉`（`.payments__detail-close`）。

> **抽屜的內容不在 `<main>` 裡**（`appendTo="body"`），所以頁面的兩向比對量不到它 ——
> 要另外查。這是「畫面 ⊆ 地圖」容易漏的一類。

**關閉**：按 `關閉` → `.p-drawer` 變成 `null` ✓（`.p-dialog`、`.cdk-overlay-backdrop` 也都是 null）。

### ~~關閉之後 `.p-drawer-mask` 還留在 DOM 裡~~ —— **是量測環境，不是產品**（2026-09-13 結案）

關閉後 7 秒以上，`.p-drawer-mask` 仍然存在，卡在 `p-overlay-mask-leave-active`，
`display:block`、`visibility:visible`、`opacity:1`、`pointer-events:auto`、
尺寸 1504×695（整個 viewport）。**`document.elementFromPoint()` 打在帳單列上回傳的是遮罩，不是列。**

**2026-09-13 證實了：是環境。** 現象在 `parent02@` 上第二次重現（遮罩卡在
`p-overlay-mask-leave-active`、`pointer-events:auto`、`elementFromPoint` 打在帳單列上
回傳的是遮罩），當下量到：

```js
document.visibilityState; // → "hidden"
document.hasFocus(); // → false
```

**對照實驗**（建一個 200ms 的 CSS transition）：

```
rafRan: 0                  ← requestAnimationFrame 1.9 秒內一次都沒跑
transitionendFired: 0
opacityAfter1500ms: "1"    ← 200ms 的 transition 連開始都沒有
```

**背景分頁裡 CSS 動畫不會跑**，而 PrimeNG 靠**離場動畫結束**才移除遮罩 ——
動畫沒跑完，元素就永遠留著。**同一個成因也解釋了整個 session 的「真滑鼠點擊失效」**
（[[specs/sitemap/README|方法頁]]坑 6 → 坑 12）。

**沒有開 issue，而且現在有理由不開**：要在前景分頁重現才算產品缺陷。

## 驗證紀錄

- **日期**：2026-09-12 ／ **前端版本**：`9f08061a`（主 checkout 的 dev server，port 4200）
  - `origin/main` 當時已前進到 `2684a1e0`（#691），`git show --stat` 確認**沒有動到 `features/parent`**
- **視窗**：1504 × 695
- **角色帳號**：`parent01@demo.clessia.app`（家長 林志明，3 個孩子）
- **身分斷言**：每次量測**前後各打一次 `GET /api/me`**，兩次都是
  `parent01@demo.clessia.app / ["parent"]`
- **資料狀態**：林子璿 0 張帳單、王柏翰 2 張（皆 `unpaid`，3600 元，期限 09-21 / 09-12）、
  盧安琪 1 張（`unpaid`）。**三個孩子沒有任何一張已付款的帳單。**

### 兩向比對（兩種資料狀態各量一次）

| 孩子           | `<main>` 可見互動元素 | 是哪幾個                                     |
| -------------- | --------------------- | -------------------------------------------- |
| 林子璿（0 張） | **1**                 | 孩子切換器徽章                               |
| 王柏翰（2 張） | **3**                 | 孩子切換器徽章 + 兩顆 `button.payments__row` |

兩種狀態的不可見元素都是 0。

王柏翰那次的 `<main>` 全文：
`王柏翰 繳費紀錄 7200 元待繳 2 張待付款 待付款 #413224d0 3,600 元 期限 2026-09-21 待付款 #3e54ae49 3,600 元 期限 2026-09-12 待付款`

| 方向        | 結果                                                                                                     |
| ----------- | -------------------------------------------------------------------------------------------------------- |
| 地圖 ⊆ 畫面 | 4 項中 2 項量到（切換器、待付款列）；另 2 項（已付款列、載入更多）是條件式且本機沒有那個資料，已標未驗 ✓ |
| 畫面 ⊆ 地圖 | 兩種狀態下的可見元素全部有列 ✓                                                                           |

**差異：0 筆。**

### ⚠️ 一個差點被寫成缺陷的「載入失敗」

切到王柏翰之後畫面顯示「載入失敗」，而 DB 明明有 2 張帳單 —— 看起來就是一支缺陷。

**真相是別席在那幾秒間登入了 admin**（auth cookie 是 host 層級、全席共用同一個 Chrome），
`GET /api/me/billing` 回 `403 NOT_PARENT`。**而頂列徽章還寫著「家長 / 林志明」** ——
shell 不會因為 cookie 變了而重繪。重登之後同一支請求對三個孩子全部 200。

**那次量測已作廢並重做。** 教訓寫進 [[specs/sitemap/README|方法頁]]：
**身分斷言要打 `GET /api/me`，不能看畫面。**

### 2026-09-13 補驗（`#746` seed 之後）

| 原本未驗的                       | 結果                                                                                                                    |
| -------------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| **「載入更多」**                 | `parent03@` / 張宇軒（26 張）→ **20 筆 + 按鈕**；按一次 **20 → 26**、**按鈕消失**。頁首錨點「94590 元待繳 26 張待付款」 |
| **已付款帳單的列與明細**         | `parent02@` / 李思妤 → 區段只有 **「已付款」**，列是 `#187346ba 4,800 元 2026-08-30 已付款`                             |
| ↳ 抽屜內容                       | 標題 `帳單 #187346ba`、狀態 **`已付款`**、明細 `學費（2026-09-01） 4,800 元`                                            |
| ↳ **`status === 'paid'` 的分支** | **付款說明區（現金或轉帳…）不顯示** ✓；改顯示**付款紀錄**：`2026-08-30 現金 4,800 元`                                   |

> 這一筆同時把「已付款」那一整條分支走完了 —— 地圖原本只驗過 `unpaid` 那半。

#### 0 個孩子的家長在這一頁看到什麼（#749）

**#749 修正後**：內容區改渲染「**這個帳號還沒有綁定任何學生**」，
不再出現「目前沒有帳單紀錄」—— 那句話會被讀成「我不欠錢」。
機制見 [[specs/sitemap/_shared/parent-child-switcher]]。

### 未驗到的

| 項目                         | 原因                                                                                                       |
| ---------------------------- | ---------------------------------------------------------------------------------------------------------- |
| 已付款帳單的列與明細         | **本機三個孩子的帳單全是 `unpaid`**，製造一張已付款的要寫 DB                                               |
| ~~「載入更多」~~             | **已於 2026-09-13 補驗**（20→26）                                                                          |
| 載入中的 skeleton            | 本機回應太快                                                                                               |
| 真的 `failed()` 狀態         | 意外看到過，但成因是量測環境不是產品；**不算驗過**                                                         |
| 抽屜的圓形 ✕（PrimeNG 內建） | 只驗了自訂的「關閉」；✕ 沒按                                                                               |
| `.p-drawer-mask` 殘留        | 排除不掉量測環境（見上），需要乾淨的瀏覽器 session 重驗                                                    |
| 手機寬度                     | 本輪只量 1504px 桌機寬度（**這一頁的抽屜是 `position="bottom"`，行動優先的設計，桌機寬度看不出它的重點**） |

## 390px

- **量測**：390 × 844 ／ 768 × 1024 ／ 1024 × 768 ／ 1504 × 752
- **前端**：主 checkout 的 dev server（port 4200），`fce2aefd`
- **身分**：`parent03@demo.clessia.app`（張建國，`roles:["parent"]`）——
  量測前後各打一次 `GET /api/me`，兩次相同
- **取樣器**：#768 的四道濾網版，**外加 ④a 的修正**與 ④b 的 `visibleFast`（兩者本輪查出，已回寫方法頁）
- **手段**：同源 iframe 當 viewport

> ⚠️ **量之前先換孩子（坑 8）。** `parent03` 的預設孩子「張品妍」**一張帳單都沒有**
> （畫面是「目前沒有帳單紀錄」，`<main>` 可見互動元素 **1 個**，只剩徽章）。
> 本輪切到**張宇軒**（26 張帳單）才量。切換用 `element.click()`（合成事件，坑 6 的等級標記）。

### ⚠️ 這一頁是本輪查出取樣器缺口的地方 —— 早期的 390 數字少一列

第一次量到 **390 = 21 / 768 = 22**，看起來是一筆差集。追下去**不是差集，是取樣器的洞**：

```
那一列 rect      top 763 / bottom 811
w.innerHeight    844        → 舊的 inView() 說「在裡面」→ 不觸發 scrollIntoView
main 的可視區    56 – 780   ← 真正的邊界
nav.bottom-bar   780 – 844 / position: **static**   ← 所以濾網④ 的 fixed/sticky 判定不命中
```

修正後 **四個寬度一致（22）**，帳單列 20 顆，跟畫面文字「20 張待付款」對上。
全文與正控負控見方法頁的 **④a**。

> **`/admin/courses` 在 #768 裡記的那個「19 / 20 是量測限制」很可能是同一個 bug**，
> 不是量測位置 —— 已經回報給 labor-6。

### 版面怎麼變

**沒有斷點** —— 元素集合四個寬度相同（22 個），只有列寬在變：

| 元素 | 390 | 768 | 1024 | 1504 |
| --- | --- | --- | --- | --- |
| `button.payments__row` × 20 | `334 × 48` | `432 × 48` | `688 × 48` | `768 × 48` |
| `button.payments__load-more`（載入更多） | `334 × 46` | `432 × 46` | `688 × 46` | `768 × 46` |
| 孩子切換器徽章 | `74 × 26` | 同左 | 同左 | 同左 |

### 差集（1504 ↔ 390）

**零差異**（修正取樣器之後；修正之前會誤報一筆「只在 1504 可見的帳單列」）。

### 水平溢出

**無**（四個寬度）。垂直捲動容器 `main.shell-content`：390 下 `724 → 1468`。

### 觸控目標 < 44px

| 元素 | 量到 | `(pointer: coarse)` 有沒有接住 |
| --- | --- | --- |
| 孩子切換器徽章 | `74 × 26` | **沒有** |

**20 顆帳單列（48）與「載入更多」（46）全部達標。** 這一頁只有徽章不足。

### 鍵盤可達性

- 全頁**沒有正數 `tabindex`** → Tab 序列 = DOM 序。
- **Tab 序列（390，張宇軒）**：徽章 → 20 顆帳單列 → 載入更多，共 **22 個**。
- **click-only 而鍵盤到不了**：`<main>` 內 **0 筆**。

### 未驗與原因

| 項目 | 原因 |
| --- | --- |
| **按「載入更多」** | 會再取一頁資料 —— 純讀取，但**本輪範圍只到頁面初始狀態**，沒按 |
| 按帳單列開詳情對話框（390） | Phase 1 在 1504 驗過；390 未重按。對話框的 Phase 2 留給 `_shared` |
| 已收款 / 逾期等其他列狀態 | 張宇軒的 20 張全部是「待付款」。**全庫有 9 張 `netPaid > 0`**（查過 DB），但不在這個孩子身上 |
| 焦點環看不看得見 | **這個環境量不到**（`document.hasFocus()` 恆 `false`） |
| `Escape` / 真的 Tab 鍵 | 需要前景分頁（方法頁坑 12） |

## 載入中 / 錯誤

- **量測**：390 × 844 ／ dev server（port 4200）`543e5eda`
- **手段**：把 XHR 的 URL 從 `:8787` 改指到沒人監聽的 `:8799`（等同 server 掛掉，
  **只影響那一個 iframe**，不動共享資源）。方法見[方法頁 Phase 2-D](../README.md)
- **證據**：每一輪都確認攔截清單不是空的（**請求數 0 的一律作廢**）
- ⚠️ **本輪只量「錯誤」那一半**

### 錯誤（所有 API 都失敗）

| 判定 | 重試鈕 | toast | 攔到的請求數 |
| --- | --- | --- | --- |
| ✅ 誠實 | 否 | 0 | 1 |

`app-empty-state` 逐字「載入失敗／沒有讀到繳費紀錄，可能是連線問題」。

### 未驗與原因

| 項目 | 原因 |
| --- | --- |
| 載入中的樣子 | 本輪只量錯誤態（做法已驗過：把 XHR 的 `send` 延後 3 秒） |
| 重試鈕按了會不會真的重打 | 需要真滑鼠（方法頁坑 12） |
| 其他寬度 | 錯誤態與寬度無關（是狀態不是版面），只量 390 |

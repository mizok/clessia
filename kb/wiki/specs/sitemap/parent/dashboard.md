---
title: 儀表板（/parent/dashboard）
summary: /parent/dashboard 的實際 UI 地圖：橘色頁首 + 孩子切換器，本文目前是「更多內容還在準備中」的佔位空狀態。
category: spec
status: developing
tags: [sitemap, parent]
created: 2026-09-12
updated: 2026-09-13
---

# 儀表板

<!-- generated:route-facts start —— 這一段由 tools/sitemap 生成，不要手改 -->

**路由**：`/parent/dashboard`
**角色**：家長（`parent`）
**選單位置**：（無群組）儀表板
**額外權限**：無

<!-- generated:route-facts end -->

**進入方式**：選單「儀表板」／ **`/parent` 轉址過來**（見 [[specs/sitemap/parent/index]]）／
登入後的落地頁 ／ 直接網址。

**外框**見 [[specs/sitemap/_shared/shell-layout]]。

> ## ⚠️ 半個殼：頁首是真的，本文是佔位
>
> **這一頁跟家長端那六個純佔位頁不一樣，也跟 admin 儀表板完全不一樣。**
> 它有真的橘色頁首與可用的孩子切換器，但**本文是一個 `app-empty-state`**。
>
> 元件註解寫出了為什麼是這個形狀：
>
> > 佔位內容。用 `app-empty-state` 而不是自己排版：這一頁的其餘部分之後會整頁重做，
> > 現在寫的任何樣式都是白工，而家長**現在就看得到這一頁**。孩子切換器不算白工——
> > 它是家長端授權模型的第一個消費端，03 片直接複用。
>
> 也就是說：**頁首那半是刻意保留的，本文那半是刻意不做的。** 改版時這兩半的處置不同。

## 畫面區塊

### 1. 橘色頁首（`app-page-band`）

- 左上：[[specs/sitemap/_shared/parent-child-switcher|孩子切換器]]
- 標題：`儀表板`（來自 `page().label`）
- **右側沒有東西** —— 這一頁沒有 `bandAside`（成績查閱與繳費紀錄有）

### 2. 佔位空狀態

- 圖示 `pi pi-home`
- 標題 `更多內容還在準備中`
- 說明 `這個功能還在準備中，完成後就會出現在這裡。`

**這一頁自己不打任何資料 API。** `ngOnInit` 只呼叫 `ChildScopeService.load()`，
也就是 `GET /api/me/children`（孩子清單，給切換器用）。
註解說明它是「家長端授權模型的第一個消費端試點」。

## 互動元素

| 元素（畫面上的字） | 類型           | 出現條件                                           | 按了之後   |
| ------------------ | -------------- | -------------------------------------------------- | ---------- |
| `<孩子名字>` ⌄     | 按鈕（toggle） | 見 [[specs/sitemap/_shared/parent-child-switcher]] | 開孩子下拉 |

**就這一個。** 空狀態沒有投影任何按鈕進 `<ng-content>`。

## 狀態

| 狀態                  | 畫面                                                  | 驗了嗎                      |
| --------------------- | ----------------------------------------------------- | --------------------------- |
| 一般（≥ 2 個孩子）    | 頁首（可切換徽章）+ 佔位空狀態。**可見互動元素 1 個** | ✓                           |
| 只有 1 個孩子         | 徽章變靜態 `<span>`，**可見互動元素 0 個**            | **未驗**（測試帳號有 3 個） |
| 沒有孩子              | 切換器整個不渲染，**可見互動元素 0 個**               | **未驗**                    |
| 孩子清單讀取失敗      | 徽章變紅色 `讀不到孩子資料`，**可見互動元素 0 個**    | **未驗**                    |
| 載入中 / 錯誤（本文） | 不適用 —— 本文不取資料                                | —                           |
| 無權限                | 這條路由沒有 `permission`；`roleGuard` 擋非家長       | 未驗                        |

**注意這一頁的互動元素數量完全由切換器決定** —— 三種形態裡只有一種是可互動的。

## 驗證紀錄

- **日期**：2026-09-12 ／ **前端版本**：`9f08061a`（主 checkout 的 dev server，port 4200）
  - `origin/main` 當時已前進到 `2684a1e0`（#691），`git show --stat` 確認**沒有動到 `features/parent`**
- **視窗**：1504 × 695
- **角色帳號**：`parent01@demo.clessia.app`（家長 林志明，3 個孩子；預設 林子璿）

### 兩向比對

|                          | 數量  |
| ------------------------ | ----- |
| `<main>` 內 DOM 互動元素 | 1     |
| 其中不可見               | 0     |
| **可見**                 | **1** |

唯一那個是 `button.child-switcher__badge.child-switcher__badge--interactive`，文字 `林子璿`。

`<main>` 全部文字：`林子璿 儀表板 更多內容還在準備中 這個功能還在準備中，完成後就會出現在這裡。`

| 方向        | 結果               |
| ----------- | ------------------ |
| 地圖 ⊆ 畫面 | 1 項找到 ✓         |
| 畫面 ⊆ 地圖 | 1 個可見元素有列 ✓ |

**差異：0 筆。**

### 未驗到的

| 項目                 | 原因                                                                        |
| -------------------- | --------------------------------------------------------------------------- |
| 切換器的另外三種形態 | 見 [[specs/sitemap/_shared/parent-child-switcher]]（本機帳號固定 3 個孩子） |
| 手機寬度             | 本輪只量 1504px 桌機寬度                                                    |

## 390px

- **量測**：390 × 844 ／ 768 × 1024 ／ 1024 × 768 ／ 1504 × 752
- **前端**：主 checkout 的 dev server（port 4200），`fce2aefd`
- **身分**：`parent03@demo.clessia.app`（張建國，`roles:["parent"]`，3 個孩子）——
  量測前後各打一次 `GET /api/me`，兩次相同
- **取樣器**：#768 的四道濾網版，**外加 ④a 的修正**與 ④b 的 `visibleFast`（兩者本輪查出，已回寫方法頁）
- **手段**：同源 iframe 當 viewport

### 版面怎麼變

**這一頁是佔位殼 + 一顆孩子切換器徽章**，四個寬度完全一樣：

| 元素 | 390 | 768 | 1024 | 1504 |
| --- | --- | --- | --- | --- |
| `button.child-switcher__badge--interactive` | `74 × 26` | `74 × 26` | `74 × 26` | `74 × 26` |

`<main>` 的其餘內容是「更多內容還在準備中／這個功能還在準備中，完成後就會出現在這裡。」

**尺寸一格都沒變** —— 這一頁沒有任何響應式行為。外框（側欄 ↔ 底欄）見
[[specs/sitemap/_shared/shell-layout]]。

### 差集（1504 ↔ 390）

**零差異。**

### 水平溢出

**無**（四個寬度 `documentElement.scrollWidth === innerWidth`，撐出界的元素 0 個）。
整頁不產生捲動容器。

### 觸控目標 < 44px

| 元素 | 量到 | `(pointer: coarse)` 有沒有接住 |
| --- | --- | --- |
| 孩子切換器徽章 | `74 × 26` | **沒有** |

**這一顆是家長端四頁共用的**（`dashboard` / `attendance` / `grades` / `payments`），
所以它是**整個家長端最常出現、而且每一頁都低於 44px 的觸控目標**。
它同時是「切換孩子」這個家長端最重要動作的唯一入口。**記現況，不判缺陷。**

> 高度 26px 是**多重角色才有的互動版**（`--interactive`）。單孩家長渲染成靜態 `<span>`，
> 那時它不是觸控目標 —— 本輪的帳號有 3 個孩子，量到的是互動版。

### 鍵盤可達性

- 全頁**沒有正數 `tabindex`** → Tab 序列 = DOM 序。
- `<main>` 內可 Tab 元素 **1 個**（徽章）。
- **click-only 而鍵盤到不了**：`i.child-switcher__badge-icon` 在 `<button>` 裡面，**不是缺口**。

### 未驗與原因

| 項目 | 原因 |
| --- | --- |
| 徽章按開之後的下拉（`.child-switcher-overlay`） | 屬於 [[specs/sitemap/_shared/parent-child-switcher]]，留給 `_shared` 的 Phase 2 |
| 單孩 / 無孩家長的樣子 | 本輪帳號有 3 個孩子 |
| 焦點環看不看得見 | **這個環境量不到**（`document.hasFocus()` 恆 `false`） |
| `Escape` / 真的 Tab 鍵 | 需要前景分頁（方法頁坑 12） |

## 載入中 / 錯誤

**不適用 —— 這一頁不取任何資料。** 實測：從別頁 SPA 導航過來並攔截 XHR，
**攔到的請求數是 0**，所以沒有載入中、也沒有錯誤態可量。

（量法與其餘家長端頁面相同，見 [[specs/sitemap/parent/attendance]]。）

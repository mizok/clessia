## Speckit + Superpowers 整合指南

### 何時使用什麼工具 (When to Use Which)

| 情境 | 工具 |
|-----------|------|
| 討論系統架構、設計決策 | `superpowers:brainstorming` → 寫入 `specs/system/` |
| 定義新功能規格 | `/speckit.specify` |
| 建立功能的實作計畫 | `/speckit.plan` |
| 拆解任務 | `/speckit.tasks` |
| 撰寫程式碼 | `superpowers:TDD` (紅-綠-重構) |
| 除錯問題 | `superpowers:systematic-debugging` |
| 宣佈完成前 | `superpowers:verification-before-completion` |
| 審查程式碼品質 | `superpowers:code-reviewer` |
| 合併規格至 canonical | `/speckit.integrate` |

### 工作流整合 (Workflow Integration)

```text
┌─────────────────────────────────────────────────────────────┐
│                    規劃階段 (PLANNING PHASE)                 │
├─────────────────────────────────────────────────────────────┤
│  系統級設計             → superpowers:brainstorming         │
│                           → specs/system/*.md               │
│                                                             │
│  功能規格               → /speckit.specify                  │
│                           → specs/features/[x]/spec.md      │
│                                                             │
│  實作計畫               → /speckit.plan                     │
│                           → specs/features/[x]/plan.md      │
│                                                             │
│  任務拆解               → /speckit.tasks                    │
│                           → specs/features/[x]/tasks.md     │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                  實作階段 (IMPLEMENTATION PHASE)             │
├─────────────────────────────────────────────────────────────┤
│  針對每個任務:                                              │
│    1. superpowers:TDD         → 先寫測試                    │
│    2. Implement               → 讓測試通過                  │
│    3. Refactor                → 清理程式碼                  │
│                                                             │
│  如果卡住:                                                  │
│    → superpowers:systematic-debugging                       │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                   完成階段 (COMPLETION PHASE)               │
├─────────────────────────────────────────────────────────────┤
│  1. superpowers:verification-before-completion              │
│     → 確保所有測試通過                                      │
│                                                             │
│  2. superpowers:code-reviewer                               │
│     → 審查程式碼品質                                        │
│                                                             │
│  3. /speckit.integrate                                      │
│     → 合併至 specs/canonical/                               │
│     → 記錄於 specs/changelog/                               │
└─────────────────────────────────────────────────────────────┘
```

### 重要規則 (Important Rules)

1. **腦力激盪輸出位置**: 始終寫入 `specs/system/`，**不要**寫入 `docs/plans/`
2. **功能規格使用 speckit**: 功能實作計畫請勿使用 `superpowers:writing-plans`，請改用 `/speckit.plan`
3. **TDD 是強制的**: 所有實作必須遵循 TDD 工作流
4. **必須整合**: 在規格合併入 `specs/canonical/` 之前，功能不算完成

---

## 指令情境驗證 (Command Context Validation)

### 執行指令前，先驗證情境

當使用者呼叫規劃/規格指令時，**不要立即執行**。先檢查是否符合正確的情境：

| 指令 | 正確情境 | 錯誤情境 |
|---------|-----------------|---------------|
| `/speckit.specify` | 定義新功能 | 討論系統架構 |
| `/speckit.plan` | 在 `specs/features/` 中建立功能的實作計畫 | 系統級設計討論 |
| `/speckit.tasks` | 將功能計畫拆解為任務 | 尚未有規格或計畫 |
| `superpowers:brainstorming` | 系統架構、設計決策、發想點子 | 功能已經有 spec.md |
| `superpowers:writing-plans` | **請勿使用** - 改用 `/speckit.plan` | 任何功能規劃 |
| `superpowers:TDD` | 實作任務程式碼 | 尚未定義任務 |

### 驗證流程 (Validation Flow)

```text
使用者呼叫指令
        │
        ▼
┌───────────────────┐
│ 對照規則檢查情境   │
└───────────────────┘
        │
        ├── 情境相符 → 執行指令
        │
        └── 情境不符 → 停止並:
                │
                ▼
        ┌───────────────────────────────────┐
        │ 1. 解釋 CLAUDE.md 中的規則         │
        │ 2. 解釋為什麼這樣是錯的            │
        │ 3. 建議正確的指令                  │
        │ 4. 詢問使用者是否確認              │
        └───────────────────────────────────┘
```

### 常見誤用情境 (Common Misuse Scenarios)

| 使用者說 | 問題 | 正確做法 |
|-----------|-------|------------------|
| `/speckit.plan` 但沒有 `spec.md` | 計畫需要先有規格 | 先執行 `/speckit.specify` |
| `/speckit.plan` 用於系統級設計 | 系統設計 ≠ 功能計畫 | 使用 `superpowers:brainstorming` → `specs/system/` |
| `superpowers:writing-plans` 用於功能 | 重複了 speckit 工作流 | 使用 `/speckit.plan` |
| `superpowers:brainstorming` 用於已有規格的功能 | 規格已存在 | 審查現有規格，或使用 `/speckit.clarify` |
| `/speckit.tasks` 但沒有 `plan.md` | 任務需要先有計畫 | 先執行 `/speckit.plan` |
| `superpowers:TDD` 但沒有定義任務 | 沒有明確範圍 | 先執行 `/speckit.tasks` |

### 誤用時的回應範本 (Response Template for Misuse)

當偵測到情境不符時，請回應：

```
⚠️ 指令情境不符

根據 CLAUDE.md 的規範：
- `[invoked command]` 應該用於 `[correct context]`
- 目前的情境是 `[current context]`

**建議改用**：`[correct command]`

**原因**：`[explanation]`

是否要改用建議的方式？
```

### 禁止的指令 (Forbidden Commands)

| 指令 | 原因 | 替代方案 |
|---------|--------|-------------|
| `superpowers:writing-plans` | 與 speckit 工作流衝突 | `/speckit.plan` |

若使用者明確要求使用禁止的指令，請解釋衝突點並建議替代方案。未經使用者確認請勿執行。

---

## 自然語言技能呼叫 (Natural Language Skill Invocation)

### 當使用者透過自然語言表達意圖時

當使用者描述任務或意圖而未使用明確指令時，Claude 應該：

1. **分析意圖** - 判斷使用者想完成什麼
2. **識別適用技能** - 將意圖對應到可用的技能/指令
3. **解釋判斷** - 告訴使用者將呼叫哪個技能及其原因
4. **等待確認** - 在使用者確認前不要執行

### 回應範本 (Response Template)

當自然語言觸發技能時，請回應：

```
📋 **意圖分析**

根據您的描述：「[user's statement]」

**我的判斷**：
- 這是 [type of task]
- 適用的工具：`[skill/command name]`

**原因**：[brief explanation]

**確認後我會**：[what will happen]

是否要執行？
```